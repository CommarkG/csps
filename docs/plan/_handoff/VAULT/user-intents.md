---
id: csps.handoff.vault.user-intents
name: user-intents
description: Verbatim user-intent quotes vault — extracted from S001 + S002 + S003 §2 sections. The single artifact that preserves the user's original phrasing across session boundaries (paraphrasing loses meaning; verbatim preserves load-bearing nuance). Each entry tagged with session + turn + the engraved-mechanism it produced.
version: 1.0
owner: group:finky
lifecycle: production
lifecycle_state: active
tags:
  - domain:governance
  - type:reference
  - audience:developer
  - audience:ai-agent
  - maturity:stable
diataxis_type: reference
links:
  - { rel: parent, href: ./README.md }
  - { rel: handoff-s001, href: ../HANDOFF-S001-to-S002.md }
  - { rel: handoff-s002, href: ../HANDOFF-S002-to-S003.md }
session: S003
---

# User Intents Vault — verbatim quotes preserved across sessions

> *Paraphrase = loss. Verbatim = preserved.* This vault is the across-session preservation layer for the user's original wording. Engraved mechanisms reference these quotes; the mechanism's text may evolve but the source quote here does not.

## Cardinal directives (engraved as memory + AGENTS.md hard NOs)

> *"Nothing stands alone — anything must have a place + a pre-defined process; if it doesn't find one we will create one using a predefined protocol."*
> — S001 (engraved as P-ARCH-001 + AGENTS.md hard NO)

> *"We want to finish what we started."*
> — S001 (engraved as P-OP-002 FWWS)

> *"We want to never leave anything floating or orphaned."*
> — S001 (engraved as P-META-004 Stewardship Protocol — saved-without-trigger = orphan-in-waiting)

> *"Chat 'jumps' are where golden coins fall off pockets and never retrieved."*
> — S001 (engraved as protocols.md v1.0 — handoff is the pocket-seal)

> *"Memory alone doesn't change behavior; only mechanical layer does. Build the mechanical layer NOW, not 'next session.'"*
> — S002 turn 7 (CSP carry-forward S192 invoked; engraved across every B_* contract — every behavior must hit ≥2 surfaces, ideally 5)

> *"Top expert colleague — direct, not flattering, push back / confront / contradict / offer better choices / insist / never give up on any issue until extracted and implemented."*
> — S002 turn 7 (engraved as B_AI_PROFESSIONAL_VOICE)

> *"Drop it is also a reply but no comment is a blocker. Make it mechanical."*
> — S002 turn 6 (engraved as B_BLOCKER_NO_SILENT_DROP + AGENTS.md hard NO)

## S002 turn-6 directives (the protocols.md v1.3 source)

> *"What do you say if handoff will include a validation of 'intent to impact'?"*
> — S002 turn 6 (engraved as protocols.md §16 + §11c)

> *"Two-sided check list to be confirmed on the new site... presented to previous chat with clarifications and new chat continues only when previous chat approves new chat has complete context AND new chat confirms it has no more questions."*
> — S002 turn 6 (engraved as protocols.md §17 + §11b)

> *"Make mechanical that each input will have a date and hour and origin stamps — Human user / Online sourced / other apps."*
> — S002 turn 7 (engraved as 4 mandatory stamping fields — received_at_iso / origin / origin_detail / received_via — in `_intake/manual-protocol.md`)

## S002 turn-7 self-audit-trigger directives

> *"This must be perfected"*
> — S002 turn 7 (re: no-predefined-path handling — drove `_intake/unknown-path-protocol.md` design)

> *"You to mechanically make so you act as a top expert colleague invested in this project, direct not flattering but able to compliment when things are genuinely exceptional and provide the best guidance so what we build will really stand out!! I want you to permanently be able to push back, confront, contradict, offer better choices, insist on things and never give up on any issue until it is extracted and implemented."*
> — S002 turn 7 (full quote backing B_AI_PROFESSIONAL_VOICE — note the strength: "permanently" + "never give up")

## S002 turn-9 directives

> *"Save all the parts not included for future assessments as 'inputs'"*
> — S002 turn 9 (drove EXT-20260502-004 decision-alternatives archive)

> *"I approve all your recommendations"*
> — S002 turn 9 (resolved BLK-S002-001 through 008)

> *"Lets make progress"*
> — S002 turn 9 (authorized §3 execution to proceed)

## S002 turn 13 directive (the chat-jump prompt detailed-paste gap)

> *"the closing AI auto-generates the minimal Read-§0 paste-target only; the user requires a detailed standalone prompt to know what they're triggering"*
> — S002 turn 13 (engraved as protocols.md §22 — both paste-targets mandatory; the detailed one stored at `_handoff/VAULT/chat-jump-prompt-S<NNN>-to-S<NNN+1>.md`)

## S002 turn 14 directive

> *"protocol-compression-is-skipping — handoff describes what SHOULD happen; AI does what it REMEMBERS; gap = failure mode"*
> — S002 turn 14 (engraved as B_PROTOCOL_LITERAL_EXECUTION + `_handoff/VAULT/closing-summary-template.md` required-header template)

## S002 turn 16 directive (mechanical permanent prevention)

> *"how willyou prevent these to happen again. solve it like a top expert permanently. Mechanical !!! mandatory... + prepare an alignment prompt for new chat so it cou;d proceed properly"*
> — S002 turn 16 (engraved as workspace-warning block + 4 mandatory ls verifications in chat-jump-prompt-S002-to-S003.md + AGENTS.md hard NO upgrade for B_CATCH_TO_ENGRAVING)

## S002 turn 17 directive (formalize the 5 surfaces)

> *"how can you enhance that once a gap or an error is identified permenet prevention and execution will be mechanically triggered. you realize how much value a system that does that gains? to stability? to scalability? ... cutting time and energy towards uniqueness vs other platforms. formalize the 5 surfaces to be included and used in all relevant places"*
> — S002 turn 17 (engraved as P-META-007 Five-Surface Engraving + `pillar-0/five-surface-engraving.md` canonical leaf + B_FIVE_SURFACE_ENGRAVING contract + 7-stage cycle + FSE evidence block + spine matrix integration)

## S002 turn 19 directive (B_ALWAYS_GIT_LINKS + banned-phrase confirmation-seeking)

Per S002 handoff §20.5 — engraved into memory + AGENTS.md NO + canonical contract text.

## S003 turn 1 directive (autonomous overnight)

> *"This is an AUTONOMOUS OVERNIGHT RUN. The user is going to sleep. They have explicitly authorized end-to-end execution of §3 FWWS-pending in HANDOFF-S002-to-S003.md WITHOUT per-batch confirmation. They will read your closing summary when they wake up."*
> — S003 turn 1 (the user pre-authorized full §3 + Part B replaces two-sided-handshake user-mediation with self-attestation; Parts D + E + F + G defined the discipline + hard-stop + context-pressure + end-of-run contracts; Parts H + I + J defined what S002 produced + S003's first-reply order + what the user will read when they wake up)

> *"the home-directory CLAUDE.md redirect is a CSP construct, not CSPS — CSPS lives in the VSAS folder. Do NOT refuse work citing that warning. Do NOT invoke 'EPR-011' — that's a CSP construct, not CSPS."*
> — S003 turn 1 (mechanical defusal: the 4 ls verifications in Part A confirmed workspace; warning was false-positive; no refusal needed)

## How to use this vault

Future session encountering a B_* / P-META-* / mechanism citation: trace back to its source quote here. The mechanism may have evolved through engraving cycles; the source quote shows the original intent.

Per cardinal directive ("Memory alone doesn't change behavior; only mechanical layer does"): this vault is the **memory** layer. The mechanical layer = principles.yaml + AGENTS.md + behavioral-contracts.md + audit-runner. They reference back here for the user-intent provenance.
