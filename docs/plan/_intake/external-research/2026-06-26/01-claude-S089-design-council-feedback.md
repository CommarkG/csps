# [EXT-2026-06-26-01] Claude.ai — CSPS S089 Design Council Feedback

**Source:** Claude.ai (external AI council, round S089)
**Date:** 2026-06-26
**Reviewed brief:** external-review-download/Csps-2026-06-26-01-S089-design-feedback.md
**Tags:** spine:GVRN+ARCH · type:external-review · domain:synergy+humble-engine+adjusting-layer · trust:quarantined→validated-on-merit · status:integrated
**Disposition:** SWIFT-absorb (most) + 3 peer-pushbacks (adapted). Full integration log: OPUS-S089-DESIGN-v2 §10.

## Key recommendations (aligned extract — not a verbatim dump; inward-membrane processed)
1. Adjusting Layer = DDD **Anti-Corruption Layer** (inward) + **Published Language** (outward); minimal = Translator fn.
2. Humble Engine failure modes → CIE returns **numeric similarity**; config thresholds **≥85 enhance / 50–84 PCR / <50 create**; CIE **snapshot** per invocation; PE effort = **TCO** for consolidate + per-option multiplier.
3. Schema = schema.org root + **SPDX** provenance + CSPS-native fields (3-layer).
4. Dispatcher robust iff **closed + versioned** enums + explicit precedence (trust>scope>intent>spine).
5. Bundling: **hard-constraints first**, **coverage matrix**, no raw scores, provenance-per-slot.
6. Missing: M1 exit-condition · **M2 versioned vocab registry (build first)** · M3 ingest=parse/validate/classify.
   Over-built: O1 outward-membrane (rendering) · O2 don't couple engine+dashboard · O3 ComparisonResult=relation.
   Closing: **block** (internal) vs **warn** (user-facing) postures.

## CSPS disposition (peer-evaluated against design §9 baseline)
- **ADOPT:** significance-threshold mechanism (closes the load-bearing gap) · ACL/Published-Language naming ·
  CIE snapshot · dispatcher precedence+versioning · bundling sequence+coverage-matrix · M1/M2/M3 · O3.
- **ADAPT / PUSHBACK (merit, not blind):**
  - outward = Published-Language + thin vocab-check (NOT a full symmetric membrane).
  - keep a **PROV-O-aligned EvidenceDoc** for claim/evidence provenance — SPDX only covers software identity.
  - **ONE engine + TWO policy-profiles** (BLOCK internal / WARN external) — FLAG to Governor (touches "one engine" directive).

## Routing
SWIFT-absorbed into design-v2 §10; the significance-threshold mechanism graduates toward ratify-candidate
(it is the core of the Humble Engine). Pending: Governor confirmation on the one-engine-two-policies synthesis.
