# Provenance — EXT-20260502-005

**Origin:** CSP platform S333 explicit Csps-targeted knowledge-transfer document. Tier-authored per CC-083.

**User's framing (S002 turn 10):**

> "I have another essential ability we developed at CSP that gives enormous value as you will see within this session before we go to a new chat. The challenge here is to have it mechanically enforced automatically but not only on qc sanity and validations. It is a universal issue as ai systems are programmed to:
> * avoid audit and run forwards
> * be satisfied with partial results and implementations
> * declare done when things are not done
> * focus on validation of negative aspect !! and ignore 'Complete extraction' !!
> Complete extraction meant 'Making the most out of anything before running and chasing new things.'"

**Trust-tier rationale:** `tenant_authored` — user is the tenant; CSP doc is user's prior-platform output explicitly shared; trust applies to user's framing of it.

**Risk-profile rationale:** `low` — pattern-based prompt-injection scan ran clean (no "ignore previous instructions" / no invisible Unicode / no b64 blobs).

**Critical user-extension at turn 10:** beyond the CSP-direct RZF (Real Zero Findings = defect verification),
the user explicitly extends the discipline to **Complete Extraction**: when a new principle/insight emerges,
walk the platform iteratively asking "where does the essence of this enhance/improve other elements?"
until same cycle returns 0 new application opportunities.

This is the **positive branch** of zero-findings (RZF being the negative branch). CSPS adopts BOTH under
the umbrella term "Zero-Findings Discipline" with named halves (RZF + CEC) per AI's S002 turn 10
push-back on vocabulary clarity.

**Routing decision:** multi-section parent with 24 sub-IDs (A-X). Sub-IDs A-W absorb CSP-direct RZF
content; sub-ID X absorbs the CEC user-extension. Routes to:
- `pillar-0-governance/zero-findings-discipline.md` (NEW canonical CSPS doc — synthesizes both)
- `pillar-0-governance/behavioral-contracts.md` (extends with B_RZF + B_CEC)
- `pillar-0-governance/ai-behavior-spine.md` (adds 2 discipline rows + updates engraving counts)
- `packages/principles/principles.yaml` (adds P-META-006 Zero-Findings Completion)
- `_intake/proactive-completion.md` (extends F8 → adds F9 CEC propagation cycle)
- `_handoff/VAULT/protocols.md` (RZF in close protocol §10 + CEC at every principle ratification)
- `AGENTS.md` (new hard NOs)
- `~/.claude/projects/.../memory/feedback_*.md` (3 new entries)
- `MEMORY.md` (index update)

**Schema-gap signal:** false. CSPS has the structural framework (audit-runner + behavioral-contracts +
spine + memory directory). This input EXTENDS, not creates-new-leaf. Absorbed into existing pillar-0
schema position.

**Cycle-1 application of the CEC discipline TO THIS DOC:**

Per the very discipline this doc proposes, the AI runs a Complete Extraction Cycle on this input AT
INTAKE TIME. Cycle 1 findings (places to apply RZF + CEC essence within CSPS):
1. principles.yaml — add P-META-006 Zero-Findings Completion
2. behavioral-contracts.md — add B_RZF + B_CEC
3. ai-behavior-spine.md — add 2 discipline rows
4. proactive-completion.md F9 — add propagation cycle
5. manual-protocol.md — add CEC step at every new-principle ratification
6. protocols.md §10 — add RZF mandatory before close
7. AGENTS.md — 3 new hard NOs
8. learning-loop.md — extend K=2 to fold CEC propagation events
9. stewardship-protocol.md — extend lifecycle_state transitions to track RZF cycle counts
10. validation-pass-S002.md template — future passes include RZF coverage column
11. audit-runner.md — add `rzf-coverage` audit kind
12. dashboard-plan.md — add `/admin/intake/rzf` page surfacing cycle counts + findings trend

12 findings Cycle 1. Apply all 12 inline. Cycle 2 will re-walk for new findings.
