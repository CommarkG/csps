---
id: csps.governance.audit-runner
name: audit-runner
description: The recurring audit system — Fact / Check / Scorecard / Run primitives, ~37 registered checks across 8 categories, severity routing, cadence model, drift dashboard, hotspot dashboard. Backstage Tech Insights pattern, native to CSPS. The platform's continuous health signal and the audit-the-audits meta-check.
version: 1.0
owner: group:finky
lifecycle: production
lifecycle_state: active
tags:
  - domain:governance
  - type:reference
  - audience:developer
  - audience:admin
  - maturity:stable
crosscutting:
  - reliability
  - security
  - observability
diataxis_type: reference
links:
  - { rel: parent, href: ./README.md }
  - { rel: principle, href: ./reuse-first-principle.md }
  - { rel: registry, href: ../../../packages/principles/principles.yaml }
  - { rel: rule-registry, href: ./rule-registry.md }
  - { rel: enforcement-architecture, href: ./mechanical-enforcement.md }
domain_path: platform
core_spine: VALD
schema_anchor: pillar_0_governance_leaves
scope_level: S1
---

# The Audit Runner System

> **Check what exists. Enhance the ratified thing. Create new only with a justification.**

## What this document locks

The recurring audit system that runs continuously across CSPS — the schema (Fact / Check / Run / Result), the ~37 registered checks across 8 categories, severity routing, cadence model, dashboard architecture, and the meta-audit that audits the audits.

## Why this exists

Without a recurring audit system, the platform's drift signal is invisible until production breaks. With it, drift is a continuous, visualized signal. Mature platforms (Backstage Tech Insights, Cortex, OpsLevel) all converged on the same primitives: **Facts + Checks + Scorecards + Runs**.

CSPS adopts the pattern natively (per [mechanical-enforcement.md](./mechanical-enforcement.md)) — we don't run Backstage; we copy the pattern. The audit runner is itself an audited slice (per P-ARCH-015 — self-hosting proves the model).

## Schema (Foundation slices 6–9)

The audit system is backed by 4 Foundation slices in the `public` schema:

```prisma
model AuditCheck extends Base {
  slug         String   @unique               // e.g., "schema-zmodel-prisma-drift"
  category     String                          // schema | security | sanity | vocab | quality | catalog | skills | cost | integrity | meta
  severity     String                          // info | warn | error | critical
  weight       Float                           // for weighted health score (0..1)
  slaMinutes   Int?                            // soft SLA for resolution
  cadence      String                          // PR | nightly | weekly | real-time | on-demand
  description  String                          // ≤1024 chars
  enforcerLocation String                      // file path of the check implementation
  @@schema("public")
  @@allow('read', auth().staffRole != null)
}

model AuditRun extends Base {
  startedAt   DateTime @default(now())
  finishedAt  DateTime?
  trigger     String                            // "pr" | "cron-nightly" | "cron-weekly" | "manual" | "deploy" | "webhook"
  commitSha   String?
  results     AuditResult[]
  facts       AuditFact[]
  @@schema("public")
}

model AuditResult extends Base {
  runId        String
  checkSlug    String
  status       String                            // "pass" | "warn" | "error" | "critical" | "skipped"
  score        Float?                            // 0..1; null if check is binary
  evidence     Json                              // structured detail per check
  quickfixUrl  String?                           // link to PR / migration / doc that fixes
  confidence   Float                             // 0..1; only ≥0.9 opens tickets
  run          AuditRun @relation(fields: [runId], references: [id])
  check        AuditCheck @relation(fields: [checkSlug], references: [slug])
  @@schema("public")
  @@deny('update,delete', true)                 // append-only
}

model AuditFact extends Base {
  runId        String
  source       String                            // "git" | "postgres" | "filesystem" | "stripe-api" | "checkly"
  key          String
  value        Json
  computedAt   DateTime @default(now())
  run          AuditRun @relation(fields: [runId], references: [id])
  @@schema("public")
}
```

## The check registry (~37 checks across 10 categories)

Each row in this table corresponds to an `AuditCheck` row registered at bootstrap. The audit runner instantiates them and runs per cadence.

### Schema (6)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `schema-zmodel-prisma-drift` | PR | error | ZenStack regenerates Prisma schema; `git diff prisma/schema.prisma` must be empty |
| `schema-prisma-db-drift` | nightly | error | `prisma migrate status` against each environment; non-zero exit fails |
| `schema-atlas-drift` | nightly | warn | Atlas continuous schema drift detection |
| `schema-payload-vs-zmodel` | nightly | error | Every Payload collection slug has a Prisma model; field names match |
| `migration-safety-squawk` | PR | critical | Squawk linter on every migration file (catches dangerous DDL) |
| `app-schema-isolation` | PR | error | App slices declare `@@schema("app_<slug>")`; foundation slices use `public` |

### Security (4)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `rls-coverage` | nightly | critical | Every tenant-scoped table has `rowsecurity=true` (the #1 production footgun — defaults to off) |
| `permission-policy-drift` | nightly | error | Declared `@@allow` policies match effective grants in `pg_policies` |
| `sandbox-escape-diff` | weekly | critical | Diff of skills with `can_execute_code: true` since last run; require explicit re-approval |
| `openssf-scorecard` | weekly | warn | OpenSSF Scorecard Action; scores supply-chain health |

### Sanity (3)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `playwright-smoke` | nightly | error | Full Playwright suite against staging; smoke critical user flows |
| `ai-smoke-agent` | weekly | warn | Mastra agent that "tries to use the product" end-to-end; catches UX rot |
| `health-endpoints` | real-time | critical | `/api/health` + `/api/ready` on every deployed app respond 200 |

### Vocabulary (5)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `vale-prose` | PR | warn | Vale lints prose against the glossary's accept/reject lists |
| `eslint-naming` | PR | error | `@typescript-eslint/naming-convention` + `id-denylist` from glossary |
| `naming-collisions` | PR | error | Slug uniqueness across DB ↔ SKILL.md ↔ Stripe ↔ Payload ↔ Catalog |
| `glossary-codegen-fresh` | PR | error | Regenerated Vale dict + ESLint rules + Payload options + ZModel @@meta must match committed |
| `db-column-vs-glossary` | PR | error | Every DB column name resolves to a glossary canonical term |

### Quality (5)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `slice-scorecard` | nightly | warn | Every slice scores ≥90% per the slice contract |
| `test-coverage-by-slice` | PR | warn | Test coverage per slice is above threshold |
| `bundle-size-drift` | PR | info | Next.js page bundle sizes vs baseline |
| `page-template-coverage` | PR | error | Every `page.tsx` imports from `@csps/templates` (slice contract check #12) |
| `storybook-coverage` | PR | warn | Every page template has a Storybook story |

### Catalog (6)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `frontmatter-completeness` | PR | error | Every artifact passes the Zod frontmatter schema. S021: council/governor-comments exempt. S022: VLT enums added, .clerk/ exempt, tools/council/ blanket exempt, grandfathered session IDs, ZF false positive fix. Session 0: template files exempt. Session C: apps/template/ exempt. S023: sandbox lifecycle states exempt from next_review_at. New fields: simulation_status, intent_crystallized, threshold_route, ux_principle. S050 STATUS-CONSOLIDATION: stage (intake\|planning\|active\|archived) + quality_state (draft\|validated\|activated\|certified) optional fields validated when present. S050: validate-activation-coverage v2 — BLOCKING for non-exempt unactivated contracts; 24 legacy exempt in tools/config/activation-coverage-exempt.yaml. **S071 PHASE 0:** id dotted-lowercase enforced — `csps.handoff.PLAN-S069-COMMS-AND-JOURNEY` → lowercase. |
| `opus_turn_rzf` | per-session | advisory | NEW S022: ZF mechanical enforcement on Opus advisory output. Every substantive Opus turn in tools/council/opus-turn.md must have "## RZF VERIFICATION" section with cycles_run + ZF ACHIEVED evidence. Exempt: consensus turns + decision-only turns. Governor directive: "make ZF a mechanically enforced thing about your audits and about all of your activity." Enforcement: advisory now → blocking week-4 (K=2 promotion per B_STRUCTURAL_PREVENTION_DISCIPLINE). File: tools/validators/validate-opus-turn-rzf.mjs. Applies B_RZF to Opus's own activity — not just Sonnet's implementation. |
| `sonnet-report` | per-session | advisory | NEW S024: PROTOCOL.md mandate — every Sonnet session writes INTENT ABSORBED + Sonnet Report to sonnet-turn.md. INTENT ABSORBED = Governor intervention window before any file edits. Sonnet Report = structured close for Opus Turn N+1. ADVISORY now → BLOCKING week-4. Validator: validate-sonnet-report.mjs. |
| `gradual-bundling` | per-session | advisory | S025 amendment: added B_SPLIT_THRESHOLD_DISCIPLINE check — plans with >12 open items AND depth-5 get advisory to fork sub-plan. Also: depth field now read from plan (parseInt). split_candidates count in output. |
| `pe-dashboard` | per-session | advisory | S025 Phase 2: added intent quality bonus — plans with goal_statement + failure_signal get +1 adjusted PE (crystallized plans are higher quality). Depth bonus unchanged. |
| `foundation-schema-drift` | PR | blocking | S025 (Layer 3 update): ZMODEL_ONLY_DEFERRED: BudgetCategory + Transaction + BudgetGoal (App #2 Budget Planner) + Habit + HabitLog (App #2 Habit Tracker — S040). ZMODEL_ONLY_DEFERRED_FIELDS: Tenant.budgetCategories + Tenant.transactions + Tenant.budgetGoal + Tenant.habits + Tenant.habitLogs. ZenStack generate PASS. **Updated S040: Habit Tracker models deferred from task-mgmt.** |
| `cdab-enforcement` | per-session | planned | S025 B_CDAB (enforcement_stage: planned) — LIGHTWEIGHT vs COMPREHENSIVE context loading per task. When active: checks that deep_quality/depth≥4 sessions load COMPREHENSIVE context (DNA + arc + PE + full plans) and velocity/depth≤3 sessions load LIGHTWEIGHT (task-specific only). Depends on: MCP get_context not yet built. Promotes to blocking when context-orchestrator.sh Phase 10 activates. |
| `opus-review-flagging` | per-session | advisory | NEW S025: Mechanical detection of when Opus consultation is needed. Checks: depth-5 HANDOFFs without needs_opus_review: true, PE>90 new items not in arc plan, sonnet-turn.md "What Opus should know" content. Shows git commits since last Opus turn. Governor: "I want to see the mechanism deciding what to bring to Opus IN ACTION." Advisory Phase 1; Phase 2: injects prompt into session-open.sh. Validator: validate-opus-review-flagging.mjs. |
| `open-questions` | per-session | advisory | NEW S025: Cross-session open question tracker — surfaces unanswered questions in plan question_register fields (confirmed: false), raw-thoughts-queue PENDING items without pe_score + trigger, HANDOFF alignment questions without answers. Governor: "gap in tracking open things left behind." Phase 2 (S026): BLOCKING for questions > 3 sessions old. Validator: validate-open-questions.mjs. |
| `check-existing-decisions` | per-session | advisory | B_CHECK_EXISTING_DECISIONS_FIRST — check existing decisions before proposing new ones. Enforced by AGENTS.md hard NO + Virtual Opus Audit Q3. Advisory slug registration to eliminate orphan status. |
| `ask-when-filling-gaps` | per-session | advisory | B_ASK_WHEN_FILLING_GAPS — ask narrow questions for under-specified directives; execute + document + surface when reversible/mechanical/no-cross-actor. Enforced by UserPromptSubmit reminder. Advisory slug registration. |
| `protocol-literal-execution` | per-session | advisory | B_PROTOCOL_LITERAL_EXECUTION — every documented protocol step gets explicit per-item execution + evidence. Enforced by closing-summary-template §10.14 TodoWrite mandate + AGENTS.md hard NO. Advisory slug registration. |
| `always-git-links` | PR | advisory | B_ALWAYS_GIT_LINKS — every path mention is a clickable link. Enforced by post-stop-link-discipline.sh (LIVE Level 1). Advisory slug registration to eliminate orphan. |
| `ai-collaborative-discipline` | per-session | advisory | B_AI_COLLABORATIVE_DISCIPLINE — AI proactively registers insights, catches, improvements without being asked. Enforced by AGENTS.md hard NO + B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2. Advisory slug registration. |
| `participant-declared` | PR + per-session | advisory | NEW S025: PACP — every new element declares target_participant (14 types: governor.primary / developer.platform / developer.app / developer.api / user.solo / user.team.member / user.team.admin / user.enterprise / user.trial / ai.sonnet / ai.opus / ai.haiku / ai.agent / ai.external). DNA Element 17. Governor directive: "Hidden gap — treat all participant types formally." Advisory Phase 1; BLOCKING S026 for new elements. Validator: validate-participant-declared.mjs. |
| `completeness-coverage` | per-session | advisory | S026 Phase 2 update: All 6 completeness contracts must reference completeness-module.md. S051: updated to read from shard files (behavioral-contracts-VALD.md for B_RZF/B_CEC/B_PRE_CLOSE; behavioral-contracts-OPER.md for B_PROTOCOL_LITERAL/B_CATCH_TO_ENGRAVING/B_QC_AUDIT) since monolith is now an index. Validator: validate-completeness-coverage.mjs. |
| `template-grade` | PR + per-session | advisory | NEW S025: Template Grade A/B/C/D enforcement (Opus Turn 9 ratified). Grade A templates must have research_ref: field. 14 existing templates need grade assignment (Turn 10 retroactive). Phase 2 (S027): BLOCKING for Grade A without research_ref. Validator: validate-template-grade.mjs. |
| `contract-harmonization` | per-session | advisory | NEW S025: B_* behavioral contract health check — contradiction detection, near-duplicate detection, orphan detection. S052: fixed to read from 5 shard files (64 contracts, was 0 after S051 sharding). Found: 9 orphans + 1 near-duplicate (B_VALIDATE_BEFORE_ASSUME amendment). Phase 2 (S027): LLM-assisted contradiction scoring → BLOCKING. Validator: validate-contract-harmonization.mjs. |
| `question-coverage` | PR + per-session | blocking | S026 Phase 2: BLOCKING for S025+ deep_quality plans missing question_register field (P-META-023 mandatory). Advisory for pre-S025 plans + wizard template G-type gaps. Phase 1 was advisory-only. Original: NEW S025: Question Protocol enforcement (Phase 1). Checks active deep_quality plans have C-type (goal_statement) + Z-type (done_criteria) questions. WizardTemplates have G-type clarifying_questions. Phase 2 (S026): question_register field mandatory. Governor: "Questions are the strongest context preservation tool — mandatory in every step." Validator: validate-question-coverage.mjs. |
| `moat-coverage` | per-session | advisory | S025: M-18 (Question Protocol) now covered by threshold-intake-protocol.md (SSoT) + intent_crystallized + routing_declared + sonnet_report validators. Total: 18/18 moat components covered. Validator: validate-moat-coverage.mjs. |
| `pe-dashboard` | per-session | advisory | NEW S025: Priority Engine auto-compute dashboard. S052: added MDPE column (blast_radius × future_enablement × readiness × simplicity_bonus multiplier). 5 plan items now have mdpe_dimensions; top MDPE score: DOCUMENTATION-IN-SCHEMA-COMPLETION=253 (classic=97). Phase 2 (S026): + gate status Bn + ZF gate per level. Validator: validate-pe-dashboard.mjs. |
| `boundary-alignment` | per-session | advisory | NEW S024: B_BOUNDARY_ALIGNMENT_PROTOCOL Phase 1 (Type E AI→human + Type B AI→subagent). Checks sonnet-turn.md Sonnet Report for ALIGNMENT CHECK block (match: YES/PARTIAL/NO) + closing-summary §10.0r drift field filled. ADVISORY now → Type B BLOCKING week-4. Validator: validate-boundary-alignment.mjs. Phase 2 (S026+): Type C MCP/API + Type D chat-jump hooks. |
| `boundary-prompt-format` | per-session | blocking | UPGRADED S072 M-CA (PROTO-S072-COUNCIL-ADDRESS — T2 promotion from advisory): T2 enforcer for boundary-prompt.template.md. BLOCKING (scoped): council files (opus-turn.md + sonnet-turn.md) TOP/most-recent entry missing headers → exit 1. ADVISORY (pre-discipline): chat-jump-prompt-*.md corpus stays advisory — do NOT retrofit. Detection uses line-start check (trimStart().startsWith). S074 ADDITION: FORMAL-PROTO-CHANNEL enforcement — detects ═══-fenced PROTO blocks embedded in chat-jump/HANDOFF files (advisory). PROTOs must pre-exist in opus-turn.md BEFORE tab opens; never embedded in startup-blocks. Enforcement trio COMPLETE: T1=pre-tool-use-council-address-required.sh (BLOCKING) · T2=this (BLOCKING scoped + FORMAL-PROTO-CHANNEL advisory) · T3=session-open + AGENTS.md hard-NO. PREVENTION CLASS: FREESTYLE-BOUNDARY-PROMPT-WITHOUT-FORMAL-HEADERS + CONVENTION-DEFINED-BUT-ENFORCEMENT-ADVISORY + PROTO-EMBEDDED-IN-STARTUP-BLOCK. Behavioral test: tools/tests/behavioral/boundary-prompt-format-test.sh (4+1: A=compliant, B=missing-header, C=pre-discipline, D=prose-mention-regression, EXIT=exit-code). |
| `vercel_projects` | per-session | blocking | NEW S075 HARDWIRE-006: BLOCKING if any active/deployed entry in tools/config/deploy-targets.yaml has a root_dir that does not exist on disk. Prevents "The specified Root Directory does not exist" Vercel build failure from triggering on every push. Root cause: apps moved to _trials-vaulted/ without removing Vercel project → build fails silently until email arrives. PREVENTION CLASS: VERCEL-DEPLOY-FAILURE-SILENT-UNTIL-EMAIL. Validator: validate-vercel-projects.mjs. Companion: tools/scripts/vercel-health-check.mjs (proactive status check without email). |
| `external_integration_health` | per-session | blocking | NEW S075 B3-lean P2: BLOCKING if any active/deployed entry in tools/config/external-integration-registry.yaml lacks health_check_command or verify_mechanically. Generalizes HARDWIRE-006 to ALL external services (Vercel/Supabase/Clerk/ZenStack/GitHub). 5 active integrations registered S075. PREVENTION CLASS: EXTERNAL-INTEGRATION-REGISTRATION-STALENESS. Validator: validate-external-integration-health.mjs. Companion: vercel-health-check.mjs. |
| `bypass_settings` | per-session | blocking | NEW S074 HARDWIRE-003: BLOCKING if either settings.local.json has wrong bypass state. Prevents recurring permission prompts (S069, S074). Checks: (1) .claude/settings.local.json is {} or has defaultMode:bypassPermissions; (2) .claude/settings.json has defaultMode:bypassPermissions; (3) ~/.claude/settings.local.json has defaultMode:bypassPermissions. Root cause: settings.local.json with permissions object but no defaultMode → CONFIG HIERARCHY SHADOW → prompts appear. Fix: session-open.sh already repairs at tab start. CRITICAL tier (same session as DONE claim). PREVENTION CLASS: PERMISSION-PROMPTS-FROM-SETTINGS-DRIFT. Validator: validate-bypass-settings.mjs. |
| `capability_registry` | per-session | advisory | NEW S075 PART 3: validates capability slug format in libs/policies/capabilities.ts (lowercase_underscore, no duplicates, no empty). ADVISORY: DB parity check skipped pre-migration. After prisma db push: run with --check-db flag. PREVENTION CLASS: CAPABILITY-SLUG-DRIFT-BETWEEN-CODE-AND-DB. Validator: validate-capability-registry.mjs. |
| `governing_intent_coverage` | per-session | blocking | NEW S075 B2 HARDWIRE-007: BLOCKING if any post-S075 principle lacks governing_intent field. ADVISORY for legacy (15% coverage S075 — backfill by significance on reactivation). governing_intent = L3 intent of the rule; without it, D11 exploits the gap between definition (L2) and intent (L3). Coverage metric: (principles with governing_intent) / total. Promotion: ≥80% for 3 sessions → BLOCKING for legacy. PREVENTION CLASS: NEW-PRINCIPLE-WITHOUT-GOVERNING-INTENT. Validator: validate-governing-intent-coverage.mjs. |
| `default_shape` | per-session | blocking | NEW S075 B1 (P-META-031 Reasoned-Adoption): BLOCKING if any new (post-D14) D* entry in default-correction-registry.yaml lacks reasoning+reframe+adopted_value fields. Counter-instruction alone = T3-only = drifts within 3 sessions. Legacy D1-D14 are advisory (grandfathered; D7/D11/D12/D14 retrofitted S075). PREVENTION CLASS: NEW-DEFAULT-WITHOUT-REASONING-REFRAME-ADOPTED-VALUE. Validator: validate-default-shape.mjs. |
| `advisory_has_promotion_path` | per-session | advisory | NEW S074 BATCH 5 (H5 cut list): ADVISORY (→ blocking after 5 exemplar passes per P-META-028) if any advisory validator in audit-runner.md lacks promotion_trigger or death_date. 141 advisory validators with no path = measuring theater. PREVENTION CLASS: ADVISORY-VALIDATOR-WITHOUT-PROMOTION-PATH. Validator: validate-advisory-has-promotion-path.mjs. Advisory initially; promotes to blocking when 5 exemplar passes confirm the heuristic is reliable. |
| `hardwire_dna_coverage` | per-session | advisory | NEW S074 BATCH 7 (L4 DNA-at-birth): ADVISORY (→ blocking after 5 exemplar passes) if new permanent files (docs/plan/pillar-*/*, tools/validators/validate-*.mjs, behavioral-contracts/B_*.md) lack satisfaction_point entry in SP-registry. Detects NEW-PERMANENT-FILE-WITHOUT-SP-ENTRY at creation time. PREVENTION CLASS: NEW-PERMANENT-FILE-WITHOUT-SP-ENTRY. Validator: validate-hardwire-dna-coverage.mjs. |
| `hardwire_completeness` | per-session | blocking | NEW S074 BATCH 2 (H1 HARDWIRE protocol): BLOCKS if any hardwire-done row in tools/data/hardwire-register.yaml has empty block_test_output. HARDWIRE-DONE = 7 surfaces + pasted BLOCK-TEST output. No claim without evidence. Prevents D7: "content written" ≠ DONE. PREVENTION CLASS: HARDWIRE-DONE-WITHOUT-BLOCK-TEST. Validator: validate-hardwire-completeness.mjs. Register: hardwire-register.yaml. |
| `satisfaction_point_coverage` | per-session | blocking | NEW S074 BATCH 1 (H4 satisfaction-point-registry): BLOCKS if any entry in tools/data/satisfaction-point-registry.yaml has empty/descriptive verify_mechanically field. Every governed permanent thing must have a shell command that exits 0 when SP achieved, exits non-0 when not. Kills D7 (action-bias): "code written" ≠ done. PREVENTION CLASS: DONE-WITHOUT-MECHANICAL-SATISFACTION-POINT. Validator: validate-satisfaction-point-coverage.mjs. Registry: satisfaction-point-registry.yaml. |
| `push_status` | per-session | advisory | NEW S072 (Governor Turn 12 — vlt-S073-push-mandatory-discipline). Surfaces unpushed local commits in every verify run. Advisory only (exits 0 always). Thresholds: >5 commits = advisory warn (sample — tunable), >10 = strong warn (sample — tunable). Closes governance gap where 16 commits were built locally without pushing. PREVENTION CLASS: GOVERNANCE-WORK-NOT-PUSHED-TO-ORIGIN. Validator: validate-push-status.mjs. Behavioral test: advisory-exempt (depends on live git remote state). T1+T3 (session-open injection + session-close gate check) queued vlt-S073-push-mandatory-discipline. |
| `prior-plan-conflict` | per-session | advisory | NEW S072 (PROTO-S072-CIP M1+M1.1). T2 for Change-Impact Pipeline staging layer. Checks staged changes in tools/data/change-impact-staging.yaml against (1) tools/config/unified-plan.yaml open items [status!=done/intake] — ≥3 shared keywords → advisory (CIP M1.1 complete), (2) vault-pending.yaml keyword overlap, (3) audit-runner.md pipeline registration gap. Keywords split on hyphens/dots to match compound-token plan titles against prose staged changes. ADVISORY in S072; promotes to BLOCKING after sample exemplar passes (tunable per P-META-028). PREVENTION CLASS: LOCAL-FIX-HARMS-GLOBAL-WITHOUT-RIPPLE-CHECK. Validator: validate-prior-plan-conflict.mjs. Behavioral test: tools/tests/behavioral/prior-plan-conflict-test.sh (4/4: A=conflict→flag, B=clean→pass, C=SHEDDABLE→exempt, D=open-plan-overlap→flag). |
| `file-size-ratchet` | PR | error | No diff makes file size worse on changed files |
| `cognitive-complexity` | PR | error | Cognitive complexity ≤15 on changed functions (`sonarjs/cognitive-complexity`) |
| `hotspot-analysis` | weekly | warn | Top decile of `churn × complexity` per file; posted to dashboard |
| `orphan-file-detection` | weekly | info | Files with no bundle membership AND no `links.index` reference |
| `module-folder-trigger` | nightly | warn | Slice exceeded thresholds; suggests `nx g platform:split` (per P-ARCH-022) |

### Skills (6)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `skill-vendor-integrity` | PR | error | SHA-pin matches `skills.lock.yaml`; integrity hash valid |
| `skill-capability-drift` | PR | error | Declared permission set hasn't expanded since last review |
| `skill-eval-freshness` | weekly | warn | Every blessed skill re-evaluated within 30-day cadence |
| `skill-banned-tools` | PR | critical | No community-tier skill references banned tools (Bash, Write, Edit, WebFetch, etc.) |
| `skill-collision-check` | PR | error | No naming/feature_key collisions between skills |
| `skill-prompt-injection-scan` | PR | critical | Static scanner (Snyk-style pattern library) for hidden Unicode, HTML comments, "ignore previous" patterns |

### Cost (3)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `tier-feature-key-reconcile` | nightly | error | Every `feature_key` referenced in code exists as a Stripe Feature; vice versa |
| `cost-drift` | weekly | warn | Stripe usage records vs expected per-tier consumption (free-tier tenants generating Pro-tier load) |
| `audit-retention-pruning` | weekly | info | Per-tier audit retention partition pruning (free 30d, paid 365d, enterprise 7y) |

### Integrity (5 — +2 S018 P8 complexity-and-hotspots)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `audit-log-integrity` | nightly | critical | No >5-min gap per tenant in `audit.events` (signals dropped writes) |
| `fk-orphan-scan` | nightly | warn | Orphan-row scan per FK; cascading-delete leakage |
| `orphan-entity-scan` | weekly | info | Skills/agents/personas with zero usage in 30 days flagged for deprecation |
| `file-complexity` | per-session | advisory | LIVE S030 E2 (alias for file-complexity-threshold): validate-file-complexity.mjs measures dual-gate (lines>300 AND H2≥3) for .md files in docs/plan/. See file-complexity-threshold row for full spec. **LIVE: validate-file-complexity.mjs.** |
| `file-complexity-threshold` | PR | warn | Scans all .md files in /docs. Applies dual-gate: lines > 300 AND distinct H2 sections ≥ 3. Files passing both gates without `mini_tree_root: true` in frontmatter = warn. Reports candidate split list. Respects `complexity_exempt: true` exemptions. Drives platform-core-alignment L2 execution. **LIVE S030 E2: validate-file-complexity.mjs wired to verify.mjs. 85 advisory candidates at S030.** **conceptual_sample_of:** ARCH L2 — a file exceeding the dual-gate threshold is an architectural anti-pattern; cognitive load scales with file size. |
| `mini-tree-intro-required` | PR | warn | For every directory containing 2+ sub-domain .md files, checks for a root .md file with `mini_tree_root: true` frontmatter and `sub_files:` array. Missing intro file for multi-file directory = warn. Uses mini-tree-intro.template.md as the required structure. **Registered S018 P8. Build week-4.** **conceptual_sample_of:** ARCH L2 — a split file without an intro+index is an orphan tree; the intro IS the canonical entry point. |

### Meta (19 — the audit-the-audits checks, P-META-001 enforcers; +2 S005 turn 19 for B_PRE_CLOSE_VERIFICATION; +3 S005 turn 24 for B_COGNITIVE_CONTEXT_DISCIPLINE; +2 S005 turn 25 for B_AGENT_ALIGNMENT_PROTOCOL; +3 S005 turn 27 for B_GOVERNOR_PROMPTS + B_HANDOFF_PRE_FLIGHT_AUDIT; +3 S005 turn 28 for B_MUTUAL_UNDERSTANDING_VALIDATION; +1 S005 turn 30 for chat-vs-session distinction; +5 S007 turn 4 for B_TOKEN_BUDGET extension of P-META-009; +1 S007 turn 5 for K=2 closed-enum drift prevention)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `audit-of-audit` | nightly | critical | Every check ran in its expected interval; no silent dead checks |
| `principle-coverage` | PR | critical | Every principle in `principles.yaml` has ≥ N enforcers per its severity (P-META-001 enforcer) |
| `enforcer-orphans` | PR | error | Every `// @enforces:` annotation in source references a real principle |
| `principles-codegen-fresh` | PR | error | Regenerated AGENTS.md + skills + hooks + MCP resources from `principles.yaml` must match committed |
| `principle-count-staleness` | PR + nightly | warn | Greps for hardcoded principle-count text (`\b\d+ meta-principle`, `\b\d+ operating principle`, `P-META-001 through P-META-\d+`, `P-OP-001 through P-OP-\d+`) in active prose; compares against current `^  - id: P-META-` / `^  - id: P-OP-` row counts in principles.yaml; warn on drift; lists offending lines. `audit_exempt_paths` glob: HANDOFF-S*.md / qc-audit-results-S*.md / validation-pass-S*.md / chat-jump-prompt-*.md / principles-snapshot.md / decisions-snapshot.md / pending-work.md / user-intents.md / blockers-S*.md / gaps-and-duplications-S*.md / VAULT/insights.md + ADR self-references where the ADR documents the fix (ADR-0021 amendment line + ADR-0022). **S084 PREV-2 extension**: also asserts session-state.json `platform_state` counts (principles_count / validators_active / hooks_active) match canonical live sources (principles.yaml all-P-* total / verify.mjs STANDARD+CRITICAL count / verify-hooks-functional.sh DECLARED_HOOKS count) — ADVISORY only, does not block. Behavioral test: tools/tests/behavioral/principle-count-staleness-test.sh (3 cases). **Registered S005 turn 8 per ADR-0022 K=2 auto-ADR.** |
| `pre-close-cycle-coverage` | PR + per-session | error | Scans every closing-summary-S<NNN>.md (and HANDOFF §10/§17) for §10.0 "Pre-close verification cycle results" header; fails if missing OR if any cycle marked DEFERRED without explicit reason field; PR-blocking error. Per P-META-008 + B_PRE_CLOSE_VERIFICATION. **Registered S005 turn 19 atomically with B_PRE_CLOSE_VERIFICATION engraving (per FSE amendment atomic-validator-registration mandate). Build deferred week-4.** **conceptual_sample_of:** GVRN L2 session governance — session close without ZF evidence = governance concept not honored. |
| `nominal-rzf-detection` | PR | warn | Scans session log + closing summaries for RZF/CEC/FSE evidence blocks not preceded by paired `pnpm verify` stdout in same session. Detects nominal-not-actual claim pattern (validator cited but never run). Per P-META-008. **Registered S005 turn 19 atomically. Build deferred week-4.** |
| `cognitive-context-discipline-coverage` | per-session | warn | Scans every closing summary for evidence that the 5-layer Cognitive Context Architecture was used: Layer 1 cache loaded? Layer 2 contract referenced? Layer 3 mid-session edits paired with re-reads? Layer 4 MCP queries used vs whole-file reads? Layer 5 subagents used for grep-heavy work? Per B_COGNITIVE_CONTEXT_DISCIPLINE + P-META-009. **Registered S005 turn 24 atomically per FSE amendment. Build deferred week-4.** |
| `model-routing-on-ratification` | PR | error | QG1 enforcer: scans session log for ratification / engraving (B_*, P-META-*) / ZF-synthesis / architectural-decision / honest-self-audit events; verifies model used was Opus 4.7; flags downgrade as QG1 violation (PR-blocking error). Per B_COGNITIVE_CONTEXT_DISCIPLINE QG1 + P-META-009. **Registered S005 turn 24 atomically. Build deferred week-4.** |
| `cache-content-hash-fresh` | nightly | warn | QG4 enforcer: verifies cache breakpoint placements (in claude-code session config) correspond to stable-content boundaries (Layer 1 + Layer 2 surfaces only); flags dynamic content (Layer 3 active work) cached at 1h TTL as anti-pattern. Per B_COGNITIVE_CONTEXT_DISCIPLINE QG4 + P-META-009. **Registered S005 turn 24 atomically. Build deferred week-4.** |
| `token-budget-claude-md-size` | PR | warn | R1 enforcer: AGENTS.md word count <500 / line count <200 per token-optimization.md v0.3 §14.6 mode 1; remediation = move detail to skills (Phase 4 of token-optimization topic-plan). Per B_TOKEN_BUDGET (extension of B_COGNITIVE_CONTEXT_DISCIPLINE) + P-META-009.config.token_budget_operating_rules. **Registered S007 turn 4 atomically per FSE amendment. Build deferred week-4.** |
| `token-budget-skills-completeness` | PR | warn | R1 enforcer: every governance domain has a backing skill at `packages/skills/<name>/SKILL.md`; missing = R1 violation (L1-fits-within-N-tokens collapses without skill on-demand load). Per B_TOKEN_BUDGET + P-META-009.config.token_budget_operating_rules. **Registered S007 turn 4 atomically. Build deferred week-4.** |
| `token-budget-hook-presence` | PR | warn | R5 enforcer: every declared rule (in AGENTS.md cascade or behavioral-contracts) has a backing `.claude/hooks/*.sh` script if the rule is mechanical. Missing = rule-without-enforcement which composes with R5 (tool output structured discipline). Per B_TOKEN_BUDGET + P-META-009.config.token_budget_operating_rules. **Registered S007 turn 4 atomically. Build deferred week-4.** |
| `token-budget-compact-frequency` | per-session | warn | R3 enforcer: ratio of `/compact <focus>` invocations to IMPL_BATCH boundaries (commit-worthy events / L<N>→L<N+1> transitions) <0.8 fires warn. Targets silent-auto-compact-mid-session anti-pattern. Per B_TOKEN_BUDGET + P-META-009.config.token_budget_operating_rules. **Registered S007 turn 4 atomically. Build deferred week-4.** |
| `token-budget-cache-continuity` | per-session | warn | R2 enforcer: detects mid-session model switch (Sonnet→Opus or vice versa) which invalidates cache (cache is model-specific). Targets cache-rebuild-via-tier-switch waste pattern. Per B_TOKEN_BUDGET + P-META-009.config.token_budget_operating_rules. **Registered S007 turn 4 atomically. Build deferred week-4.** |
| `token-budget-clear-discipline` | per-session | warn | R4 enforcer (v2 1M-context variant): detects /clear invocation when context utilization <80%. At 1M context, /clear before 80% destroys the session archive — the conversation IS the live governance record. Advisory when <80%; error when <50% at /clear time. Per B_TOKEN_BUDGET R4 v2 (S018 ratification) + P-META-009. **Registered S018 CEC B_TOKEN_BUDGET v2. Build deferred week-4.** **conceptual_sample_of:** AI L2 inner-defaults — training default is /clear on idle; CSPS override is /clear only at saturation + closed arc. |
| `token-budget-cost-measurement` | per-session | warn | R6 enforcer: checks session log for /cost invocation at session-open (baseline) AND at each IMPL_BATCH boundary (delta). Missing = warn. Tracks cost-per-ZF-0-batch as primary KPI. Without measurement, B_TOKEN_BUDGET is behavioral not mechanical. Per B_TOKEN_BUDGET R6 (S018 ratification) + P-META-009. **Registered S018 CEC B_TOKEN_BUDGET v2. Build deferred week-4.** **conceptual_sample_of:** OPER L2 — cost measurement is the operational feedback loop without which optimization is aspiration. |
| `subagent-delegation-required` | PR + per-session | warn | R7 enforcer: detects ZF cycles / validate-*.mjs suites / file scanning >5 files / log analysis running in main context without subagent delegation. Advisory: surfaces which operations could be delegated to Haiku subagent (CLAUDE_CODE_SUBAGENT_MODEL=haiku). Week-4: promotes to warn-blocking when subagent model not set. Per B_TOKEN_BUDGET R7 (S018 ratification) + B_AGENT_ALIGNMENT_PROTOCOL. **Registered S018 CEC B_TOKEN_BUDGET v2. Build deferred week-4.** |
| `cache-stable-static-context` | per-session | error | R8 enforcer: detects CLAUDE.md modification OR MCP server install/removal OR plugin changes during an active session (between session-open and session-close). Any detection = error. Cache invalidation mid-session forces full cold rebuild on next turn. Per B_TOKEN_BUDGET R8 (S018 ratification) + P-META-009. **Registered S018 CEC B_TOKEN_BUDGET v2. Build deferred week-4 (file-mtime tracking in session hook).** **conceptual_sample_of:** AI L2 — cache warmth is the road that charges the car; modifying it mid-session loses the charge. |
| `template-compliance` | PR + per-week | warn | S018 STUB (core seed TEMPLATE_COMPLIANCE_BLOCKING deprecated S044 OPEN-058): validate-template-compliance.mjs — scans artifacts with template_used: domain-card. STUB mode — exits 0 always. Core seed deprecated — blocking upgrade deferred to S045+. Template compliance now via template-registry.md + validate-template-grade.mjs. **STUB S018, seed closed S044.** |
| `question-coverage` | PR + per-session | warn | NEW S019 Question Protocol: checks that (1) every topic-plan has a non-empty question_register frontmatter field, (2) platform-level questions Q-GVRN-001/Q-VALD-001/Q-AI-001 have been answered with THIS-SESSION evidence. Questions are "encrypted context and intents" — unanswerable questions = explicit gaps. **Registered S019 P11 vocabulary-canon pipeline. Build week-4 (validate-question-coverage.mjs).** **conceptual_sample_of:** GVRN L2 — the question registry is the mechanized form of governance intent; if the platform can't answer its own questions, those are the governance gaps. |
| `concept-load-declared` | per-session | warn | NEW S018 CEC — B_CONCEPT_LOAD (P-META-020 mechanical enforcement): scans session transcript for CONCEPT_LOAD declaration at start of each substantive response. Absence = AI operated from training defaults not CSPS conceptual frame. Per B_CONCEPT_LOAD + P-META-020 + inner-ai-defaults calibration instrument. **Registered S018 atomically. Build deferred week-4.** **conceptual_sample_of:** AI L2 inner-defaults domain — this validator samples whether the CONCEPT_LOAD contract is being honored at input boundaries. |
| `model-routing-profile-consistency` | PR + per-session | warn | NEW S010 amendment: verifies settings.json `"model"` field matches the `main_session_default.value` of the active profile (`is_default: true`) in [model-routing-dashboard.md §10](./model-routing-dashboard.md#10--configuration-profiles-template-groups). Catches drift between "settings.json says Sonnet" and "dashboard profile says Opus" or vice versa. Also checks T4 per-session model-budget plan cites the active profile_id. Per B_TOKEN_BUDGET R2 + B_COGNITIVE_CONTEXT_DISCIPLINE + model-routing-dashboard.md §10. **Registered S010 atomically. Build deferred week-4 S012 (ships alongside Phase 9 bundling orchestrator as part of full auto-tiering enforcement).** |
| `core-seeds-coverage` | PR + per-session | warn | NEW S018 Core Seeds: scans all .mjs and .sh files for @core-seed: markers. Verifies each seed has: plan reference + grows-to description + target session. Reports seeds that are OVERDUE (target session past) + seeds that have GROWN (grows-to artifact now exists). Makes gap between "designed" and "implemented" VISIBLE and AUDITABLE. **Registered S018 atomically. Build STUB (validate-core-seeds.mjs — enforcement_stage: stub, exits 0, week-4 promotes to advisory).** **conceptual_sample_of:** GVRN L2 — Core Seeds are the mechanical bridge between plan and implementation; finding seeds shows what was promised; finding grown seeds shows what was delivered. |
| `cdp-status-consistency` | PR | warn | NEW S018 CDP: checks that `cdp_status:` field in any artifact follows the valid state machine transitions (raw→pipeline-intake→...→sealed) and that `zf-achieved` status is only present when the artifact's last ZF run evidence block shows ZERO BLOCKING FINDINGS (INST-VALD-001). **Registered S018 CDP plan L1. Build week-4 (validate-cdp-status-consistency.mjs).** **conceptual_sample_of:** GVRN L2 — cdp_status is the governance lifecycle state; zf-achieved without evidence is a declaration, not a verified state. |
| `frontmatter-closed-enum-drift-prevention` | PR + per-session | warn | K=2-promoted prevention angle for closed-enum drift (S006 lifecycle_state:draft + S007 maturity:active fired same anti-pattern). Composes with existing `frontmatter_validate` (post-write detection); this slug is pre-write coverage — scans session log for Edit/Write tool calls modifying closed-enum frontmatter fields without consulting [frontmatter-closed-enums.md](./frontmatter-closed-enums.md) within prior N turns. Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2 K=2 promotion + P-META-019. **Registered S007 turn 5 atomically per FSE amendment. Build deferred week-4.** **S018 amendment:** `validate-frontmatter.mjs` extended with `enforcement_stage: stub\|planned\|week-4\|active` closed enum + S018 close amendment adds `human-judgment` (Tier 3). **S071 PHASE 0 amendment:** `validate-frontmatter.mjs` EXEMPT_PATH_GLOBS extended to exclude `_handoff/VAULT/external-input/**` (raw external research docs without CSPS frontmatter, per VAULT_DEFER discipline). **S078 amendment:** EXEMPT_PATH_GLOBS updated to also match `_handoff/VAULT/archive/` prefix for snapshot/historical files moved during vault-root archival (chat-jump-prompt-*, qc-audit-results-*, validation-pass-*, gaps-and-duplications-*, blockers-*). | |
| `agent-alignment-coverage` | PR | error | **conceptual_sample_of:** AI L2 alignment protocol — every agent operating without AAP frontmatter is a wildcard; wildcards violate the concept that intelligence is a managed resource (L1_CORE_AI). Class A enforcer: every SKILL.md across ALL CSPS authoring locations — `packages/skills/*/SKILL.md` (platform skills) + `.claude/skills/*/SKILL.md` (Claude Code auto-load; S007 §24+ amendment) + `libs/agents/*/agent.zmodel` (Mastra runtime; week-6+) — has AAP frontmatter populated: `csps_aligned: true` + `aap_version` + `agent_class: A\|B\|C\|D` + `acknowledged_contracts: [...]` (universal-required: B_AI_PROFESSIONAL_VOICE + B_VALIDATE_BEFORE_ASSUME) + `respects_quality_gates: [QG1, QG2, QG3, QG4]` + `output_contract` + `trust_tier`. Missing fields fail PR. **No-wildcards mandate** per S007 §24+ user directive: non-aligned agents/skills are wildcards that damage platform integrity. Per B_AGENT_ALIGNMENT_PROTOCOL + P-META-010. **Registered S005 turn 25 atomically per FSE amendment + multi-location coverage S007 §24+ atomic. Build active S005 turn 26 (impl complete; coverage expansion same-commit).** |
| `skill-location-coverage-completeness` | PR | error | NEW S007 §24+ amendment: meta-validator confirming `validate-aap-frontmatter.mjs` `SKILL_PATHS` glob covers all SKILL.md locations in CSPS. Walks repo for any `**/SKILL.md` files outside the declared `SKILL_PATHS` glob; flags as wildcard hazard. Prevents future skill-location additions from going unaudited. Per B_AGENT_ALIGNMENT_PROTOCOL + P-META-010 + B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2. **Registered S007 §24+ atomically per FSE amendment. Build deferred week-4.** |
| `vocabulary-canon-completeness` | per-week | warn | Reads vocabulary.md and cross-references against: AGENTS.md hard NOs, behavioral-contracts.md slugs, audit-runner.md slugs, and topic-plan frontmatter fields. Flags any governance meta-term used in artifacts without a vocabulary entry. Target: 0 undefined governance meta-terms. **Registered S018 P11 vocabulary-canon. Build week-4 (validate-vocabulary-canon-completeness.mjs).** **conceptual_sample_of:** GVRN L2 — every governance term must be defined before use; undefined terms = governance on quicksand. |
| `vocabulary-term-schema-valid` | PR | error | Validates every entry in vocabulary.md has: id + term + definition + concept_ref (L2 domain) + canonical_home + at least one example. Missing fields = schema violation. Blocks PR. **Registered S018 P11 vocabulary-canon. Build week-4.** **conceptual_sample_of:** GVRN L2 — a vocabulary entry without schema fields is documentation not governance; only structured entries can be machine-queried. |
| `threshold-completeness` | per-session | warn | Checks that within first 3 turns of a session: session-state.json was read + pnpm verify ran + VLTs confirmed 0 PENDING + CONCEPT_LOAD declared. Missing any = warn. All missing = error. Per B_CONCEPT_LOAD + threshold-gate-v2.md + session-open.sh Q1-Q15. **Registered S018 P13 threshold-gate. Build week-4 (validate-session-open-completeness.mjs).** **conceptual_sample_of:** GVRN L2 — the Threshold IS the governance entry point; unverified means work runs without the conceptual frame activated. |
| `tag-status-deep-audit` | weekly cron + on-demand | warn | NEW S008 turn 8 per user GP-S008-07 directive: weekly recurring scan of ALL governed CSPS artifacts (extraction notes / topic-plans / element-reviews / closing-summaries / SKILL.md / behavioral contracts / ADRs) for tag drift / state-machine illegal transitions / required fields per state / SLA compliance per [`tag-status-contract.md`](../../_intake/tag-status-contract.md). Composes with point-in-time `frontmatter_validate` + `aap_frontmatter_coverage` (this slug = recurring failsafe for what point-in-time misses post-authoring). Hook stub: [`.claude/hooks/cron-weekly-tag-status-deep-audit.sh`](../../../.claude/hooks/cron-weekly-tag-status-deep-audit.sh). Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2 (recurring-detection mechanism extending S007 K=2 closed-enum drift fix to scheduled-cadence audit) + B_INTAKE_DISCIPLINE (extraction-note state-machine coverage). **Registered S008 turn 8 atomically per FSE amendment. Build deferred week-4 (cron mechanism requires hook activation OR Claude Code SessionStart adaptation).** |
| `subagent-spawn-preamble-required` | per-session | warn | Class B enforcer: scans session log for Agent tool invocations (Explore/Plan/general-purpose/claude-code-guide/statusline-setup); verifies spawn prompt contains AAP alignment preamble as first content block before task description; flags missing preambles as wildcard violations. Per B_AGENT_ALIGNMENT_PROTOCOL + P-META-010. **Registered S005 turn 25 atomically. Build deferred week-4.** |
| `ai-behavior-spine-slices-sync` | PR + on-demand | error | NEW S010 Phase 7 Candidate #4: verifies all 10 ai-behavior-spine section slices at `docs/plan/pillar-0-governance/ai-behavior-spine/<slug>.md` are present. Run `node tools/generators/split-ai-behavior-spine.mjs` to regenerate. Per token-optimization §9.8 Candidate #4 + B_TOKEN_BUDGET R1. **Registered S010 Phase 7 atomically. Build ACTIVE S010 (tools/validators/validate-ai-behavior-spine-slices.mjs).** |
| `behavioral-contract-slices-sync` | PR + on-demand | error | NEW S010 Phase 7 Candidate #2: verifies all 39 B_* behavioral contract slice files at `docs/plan/pillar-0-governance/behavioral-contracts/B_NAME.md` are present. Run `node tools/generators/split-behavioral-contracts.mjs` to regenerate after editing behavioral-contracts.md. Per token-optimization §9.8 Candidate #2 + B_TOKEN_BUDGET R1 + B_SAVINGS_AND_SSOT_UNIFIED. **Registered S010 Phase 7 atomically. Build ACTIVE S010 (tools/validators/validate-behavioral-contract-slices.mjs).** |
| `audit-runner-slices-sync` | PR + on-demand | error | NEW S010 Phase 7 Candidate #3: verifies all 28 audit-runner pipeline slice files at `docs/plan/pillar-0-governance/audit-runner/pipeline-<slug>.md` are present. Run `node tools/generators/split-audit-runner.mjs` to regenerate. Per token-optimization §9.8 Candidate #3 + B_TOKEN_BUDGET R1. **Registered S010 Phase 7 atomically. Build ACTIVE S010 (tools/validators/validate-audit-runner-slices.mjs).** |
| `principle-slices-sync` | PR + on-demand | error | NEW S010 Phase 7 Candidate #1: verifies all 53 principle slice files at `packages/principles/principles/P-XXX-NNN.yaml` are present + each parses as valid YAML with correct `id` field + `principles-index.yaml` total_count matches source count. Run `pnpm principles:split` to regenerate after editing principles.yaml. Per token-optimization §9.8 Candidate #1 3b (sub-phase validator) + B_SAVINGS_AND_SSOT_UNIFIED + B_CONSOLIDATION_PASS + P-META-009 B_TOKEN_BUDGET R1. **LIVE. S032 E5 update: validator updated to prefix-match (P-ARCH-001-*.yaml) after E5 principle slice name backfill (61 slices renamed with topic-kebab suffix). 61 slices, 0 missing.** |
| `aap-9-field-coverage` | PR | warn (Phase 1 S010) → error (Phase 2 S012) | NEW S010 amendment: 9-field AAP frontmatter coverage check on every Class A SKILL.md across all CSPS skill-authoring locations. Validates 2 new fields beyond the 7-field baseline: `principle_compliance` (array of P-* IDs; minimum `P-META-010` + `P-META-002` universal-required) + `consolidation_cross_refs` (array of artifact paths whose discipline overlaps; empty `[]` valid for genuinely-novel skills per B_CONSOLIDATION_PASS). Phase 1 (S010): warn-level — logs missing fields but does NOT fail PR (preserves verify continuity for 16 existing SKILL.md while new SKILL.md S010+ get guidance). Phase 2 (S012 target): warn → error after S011 backfill pass retrofits all 16 SKILL.md. Per B_AGENT_ALIGNMENT_PROTOCOL S010 amendment + EXT-20260505-002-B 9-element DNA gate (CSPS adapts: drops spheres-RETIRED CSP-specific). Composes with `agent-alignment-coverage` (the 7-field error-level baseline) + B_CONSOLIDATION_PASS + B_SAVINGS_AND_SSOT_UNIFIED + B_GRADUAL_BUILD_BY_FOUNDATIONS phased trajectory. **Registered S010 turn 6c atomically per FSE amendment. Build active S010 (warn impl shipped same-commit at validate-aap-frontmatter.mjs).** |
| `governor-prompt-coverage` | per-session | error | Per-session enforcer: every substantive user prompt in session log has GP-S<NNN>-<NN> entry in `_handoff/VAULT/governor-prompts/S<NNN>.md`; missing entries fail PR. Per B_GOVERNOR_PROMPTS + P-META-012. **Registered S005 turn 27 atomically per FSE amendment. Build deferred week-4.** |
| `governor-prompt-distribution-complete` | PR | warn | PR-blocking warn: every GP entry has `distribution_targets` populated (principle / contract / leaf / audit / ADR / decision); null targets allowed only when status is `dropped` with explicit drop reason. Per B_GOVERNOR_PROMPTS + P-META-012. **Registered S005 turn 27 atomically. Build deferred week-4.** |
| `hpfa-pre-handoff-coverage` | PR | error | PR-blocking error: every `HANDOFF-S<NNN>-to-S<NNN+1>.md` has Handoff Pre-Flight Audit evidence block in closing-summary §10.0f covering 7 mandatory checks (governor_prompts / engraving / audit_registration / cycle_evidence / schema_dynamic / distribution_targets / carry_forward_explicit); missing or empty fails. Per B_HANDOFF_PRE_FLIGHT_AUDIT + P-META-013. **Registered S005 turn 27 atomically. Build deferred week-4.** |
| `muv-chat-jump-prompt-completeness` | PR | error | PR-blocking error: every `chat-jump-prompt-S<NNN>-to-S<NNN+1>-detailed.md` has 8 mandatory sections (handoff §0 / post-close addenda / governor-prompts pointer / HPFA pointer / carry-forwards-with-reasons / cardinals-verbatim / verify state / EXPLICIT ALIGNMENT-QUESTIONS section); missing or empty section fails. Per B_MUTUAL_UNDERSTANDING_VALIDATION + P-META-014. **Registered S005 turn 28 atomically per FSE amendment. Build deferred week-4.** |
| `muv-subagent-output-contract-verification` | per-session | warn | Per-session enforcer: every Agent tool invocation has paired output verification (returned summary matches declared output_contract — max_tokens within limit / shape matches `returns:` / no-synthesis-claims / no-ratification-claims). Per B_MUTUAL_UNDERSTANDING_VALIDATION boundary type 2 + P-META-014. **Registered S005 turn 28 atomically. Build deferred week-4.** |
| `muv-cross-chat-handshake-completion` | per-session | warn | Per-session enforcer: tracks chat-jump prompts across sessions; flags handshakes that haven't reached alignment-confirmed-explicit within 7 sessions (the iteration loop is mandatory; abandoned handshakes are gap propagation). Per B_MUTUAL_UNDERSTANDING_VALIDATION boundary type 1 + P-META-014. **Registered S005 turn 28 atomically. Build deferred week-4.** |
| `chat-vs-session-id-discipline` | PR | error | PR-blocking error: scans artifacts for chat/session conflation — flags (a) any handoff/governor-prompts entry without `S<NNN>-C<n>` chat-segment ID; (b) any artifact citing "chat" where session is meant or vice versa per §12 definitions; (c) any post-close same-chat addendum without explicit `§24+ post-close addendum` tag; (d) detection of N:1 violation (S<NNN+1> work begun in same chat as closed S<NNN>). Per protocols.md v1.10 §12 + AGENTS.md hard NO. **Registered S005 turn 30 atomically per FSE amendment. Build deferred week-4.** |
| `depth_marker_creation_gate` | PR + on-Write | warn | NEW S009 L1.1 per token-optimization §9.0 synthesized order: pre-tool-use scan on Write of governed artifacts (`docs/plan/**/*.md` + `tools/templates/*.md` + `.claude/skills/*/SKILL.md`); warns if file projected >300 lines AND missing `file_depth_markers` frontmatter field. Composes with [`feedback_depth_discipline.md`](memory) cognitive-layer pre-write reference + [`depth-discipline.md`](./depth-discipline.md) canonical 5-semantics catalog. Hook stub: [`.claude/hooks/depth-marker-creation-gate.sh`](../../../.claude/hooks/depth-marker-creation-gate.sh) (PENDING L1.6 governor-permission batch per popup discipline memory entry 44). Per EXT-20260505-004-A + EXT-20260505-004-B + B_FIVE_SURFACE_ENGRAVING + B_STRUCTURAL_PREVENTION_DISCIPLINE. **Registered S009 L1.1 atomically per FSE amendment. Build deferred week-4 (hook activation gated by L1.6 batch).** |
| `depth-field-semantic-consistency` | PR | warn | NEW S009 L1.1: scans artifacts for L1/L2/L3 nomenclature usage without naming which semantic (per [depth-discipline.md §1](./depth-discipline.md) 5-semantic disambiguation table). Detects CSP file #4 §4 Duplication #3 anti-pattern. Per B_CONSOLIDATION_PASS + EXT-20260505-004-A. **Registered S009 L1.1 atomically. Build deferred week-4.** |
| `placeholder-staleness-detection` | weekly | warn | NEW S009 L1.1: scans frontmatter for `TBD-S<NNN>` placeholders age >5 sessions; forces backfill per EXT-20260505-004-B Step 4. Composes with `depth_marker_creation_gate` (creation-time placeholder allowance) and `tag-status-deep-audit` (recurring scan cadence). Per B_FIVE_SURFACE_ENGRAVING + B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2. **Registered S009 L1.1 atomically. Build deferred week-4.** |
| `depth_chosen-scope-violation` | PR | error | NEW S009 L1.1: rejects `depth_chosen` field appearing in artifacts OUTSIDE `_handoff/VAULT/topic-plans/<topic-id>.md` per [depth-discipline.md §1](./depth-discipline.md) Disambiguation Rule (semantic 5 is topic-plan-specific). Other artifacts must use `depth_levels_invoked` (semantic 2). Per EXT-20260505-004-A naming-drift mitigation. **Registered S009 L1.1 atomically. Build deferred week-4.** |
| `consolidation-pass-coverage` | PR + post-comprehensive-guide-commit | warn | NEW S009 L1.3: grep-based duplicate detection scanning governed artifacts for ≥3 occurrences of multi-line facts/lists/definitions/rules per [B_CONSOLIDATION_PASS](./behavioral-contracts.md#b_consolidation_pass). Detects 6 duplication patterns A-F (List > Rule > Definition > Example > Cross-section > Citation). Flags consolidation candidates with proposed canonical home + cross-reference paths. Composes with [`tag-status-deep-audit`](#tag-status-deep-audit) weekly cron (consolidation pass fires at every cron tick) + [`depth_marker_creation_gate`](#depth_marker_creation_gate) (Phase 6 spawn templates produce no duplications). Hook ACTIVE: [`.claude/hooks/post-stop-consolidation-pass.sh`](../../../.claude/hooks/post-stop-consolidation-pass.sh) (lifecycle: production / v1.0.0 — PROTO-S067-MASTER-THRESHOLD-ROUTER STEP 2 commit 6996d1e). Per EXT-20260505-003-A + B_FIVE_SURFACE_ENGRAVING + B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2. **Registered S009 L1.3. ACTIVE S067 STEP 2 — 6 patterns A-F detector + 3/3 behavioral test.** |
| `consolidation-exempt-justification-required` | PR | warn | NEW S009 L1.3: when artifact frontmatter declares `consolidation_exempt: true`, validator requires paired `consolidation_exempt_reason:` field citing one of the rigid-vs-flex counter-cases per [EXT-20260505-003-D](../_handoff/VAULT/contexts/governance/anti-duplication/EXT-20260505-003-D-when-NOT-to-consolidate-counter-cases.md) (e.g., glossary-restatement / safety-critical-redundancy / batch-boundary-cite). Empty exempt = anti-pattern (counter-case abuse). Per B_CONSOLIDATION_PASS counterweight. **Registered S009 L1.3 atomically. Build deferred week-4.** |
| `savings-ssot-coverage` | PR + post-comprehensive-guide-commit + weekly cron | warn | NEW S009 L1.4: single-pass measurement of BOTH savings axis (token-budget reduction per B_TOKEN_BUDGET R1-R5) AND SSoT axis (canonical-home compliance per B_CONSOLIDATION_PASS) per [B_SAVINGS_AND_SSOT_UNIFIED](./behavioral-contracts.md#b_savings_and_ssot_unified). Composes with `consolidation-pass-coverage` (SSoT axis) + `token-budget-*` 5 audits (savings axis) — this validator is the unified-pass that runs both as ONE Phase 9 measurement (S013) per EXT-20260505-005-A unification insight. Hook stub: [`.claude/hooks/post-stop-savings-ssot-coverage.sh`](../../../.claude/hooks/post-stop-savings-ssot-coverage.sh) (PENDING L1.6 governor-permission batch). Per B_FIVE_SURFACE_ENGRAVING + P-META-009 + Q3=A confirmed S009. **Registered S009 L1.4 atomically per FSE amendment. Build deferred Phase 9 (S013).** |
| `canonical-home-field-declaration-coverage` | PR | warn | NEW S009 L1.4: when artifact body content matches a known fact-pattern (closed-enum value / depth semantic / 5-step protocol / etc.), frontmatter MUST declare `canonical_home_field: <path>` per [B_SAVINGS_AND_SSOT_UNIFIED](./behavioral-contracts.md#b_savings_and_ssot_unified) unified-rule SCHEMA-as-canonical-home semantic. Composes with `nothing-stands-alone-audit` (core_spine + schema_anchor required) + `consolidation-pass-coverage` (duplication detection). Per EXT-20260505-005-A §5 verbatim: *"canonical home is a SCHEMA field where possible"*. **Registered S009 L1.4 atomically. Build deferred week-4.** |
| `token-budget-validate-all-modes` | PR + per-session | warn (advisory window) | NEW S011 Phase 9: 5-mode token budget validator per [token-optimization.md §14.6](./token-optimization.md) + [EXT-20260505-002-A](../_handoff/VAULT/contexts/governance/validators/EXT-20260505-002-A-validator-class-structure-smoke-test-severity-taxonomy.md) 6-commitment class. Mode 1=AGENTS.md line count (<200 lines) / Mode 2=skills completeness (context-loading templates have disk-present artifacts) / Mode 3=hook presence (settings.json declared hooks exist on disk) / Mode 4+5=advisory (session-log dependent). Advisory window; FAIL_CLOSED ratchet at ≥10 sessions with ≥50% savings vs Phase 1 baseline. Per B_TOKEN_BUDGET R1-R5 + P-META-009 CCA. **Registered S011 Phase 9 atomically. Build ACTIVE S011 (impl shipped at tools/validators/validate-token-budget.mjs; wired into pnpm verify as `token_budget_validate`).** |
| `corespine-hub-depth-markers` | PR | warn (advisory → error on week-4) | NEW S011 Phase 9 (EXT-20260505-004-D Improvement #8): verifies all 5 L1_CORE_<SPINE>.md HUB files at `.claude/core-spines/` have `file_depth_markers` frontmatter field (l1_lines/l2_lines/l3_lines). Also checks L2_DOMAIN_*.md count ≥16 + L3_INSTANCES_*.md count = 5 per P-ARCH-028 3-layer doctrine. Prevents "HUB file no depth markers" anti-pattern where CCA Layer 1 bulk-loads sealed doctrine without L1/L2 routing. Per P-ARCH-028 + depth-discipline.md + EXT-20260505-004-D. **Registered S011 Phase 9 atomically. Build ACTIVE S011 (impl shipped at tools/validators/validate-corespine-depth-markers.mjs; wired into pnpm verify as `corespine_depth_markers`).** |
| `schema-index-coverage` | PR | warn | NEW S011 Phase 9 (EXT-20260505-005-C §6): verifies all schema-bearing files listed in [schema-index.md](./schema-index.md) still exist on disk; flags new schema-bearing files NOT listed in schema-index.md (orphan schemas). Recursive SSoT enforcement — index is canonical for schema-discovery. Per B_CONSOLIDATION_PASS + B_SAVINGS_AND_SSOT_UNIFIED + EXT-20260505-005-C. **Registered S011 Phase 9 atomically. Build deferred week-4.** |
| `pe-read-budget-token-estimate` | per-session | info | NEW S011 Phase 9 (EXT-20260505-004-C §7): pe-compute.mjs computes read_budget for current task-class using file_depth_markers from referenced artifacts; result logged as INFO with bundle_opportunities. Advisory at this stage — Phase 10 activates enforcement gate. Per B_TOKEN_BUDGET R1 + depth-discipline.md + EXT-20260505-004-C. **Registered S011 Phase 9 atomically. Build ACTIVE S011 (impl shipped at tools/pe-compute.mjs; cache at tools/pe-context-cache.json). Runtime integration via context-orchestrator hook deferred S012.** |
| `10-scenario-test-verification` | quarterly | warn | NEW S011 Phase 9 (EXT-20260505-002-F discipline 5 + token-optimization §14.5): quarterly 10-scenario over-compression test verifying each trigger scenario loads correct skill/context-template. PASS criterion ≥9/10. Test spec at [tools/test-scenarios/token-optimization-10-scenario.json](../../../tools/test-scenarios/token-optimization-10-scenario.json) — AWAITING_USER_VERIFICATION (Phase 4d un-deferred S011; user-tested verification required). Per B_TOKEN_BUDGET R1 + cruel-critic Critique 3. **Registered S011 Phase 9 atomically. Build ACTIVE S011 (test spec shipped; mechanical runner deferred week-4).** |
| `session-artifact-sync` | PR + per-session | warn | NEW S011 §24++ gap-fix: HANDOFF carry-forward phase claims match token-optimization.md phase COMPLETE markers; HANDOFF verify state (N active validators) matches actual verify.mjs cycle count; post-close addendum work has explicit §24++ section in closing-summary. Root cause: Phase 9 completed post-close but 4 artifacts retained PARTIAL/DEFERRED language until deep audit. Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2 + B_HANDOFF_PRE_FLIGHT_AUDIT. **Registered S011 §24++ atomically. Build ACTIVE S011 (impl at tools/validators/validate-session-artifact-sync.mjs; wired into pnpm verify).** |
| `git-pushed-state` | per-session | warn (advisory) | NEW S011 zero-laptop L1: no uncommitted governed-path changes + no commits ahead of origin/main before session close. Advisory window. Per P-OPER-001 Zero-Laptop-Dependency Q-2=B. **Registered S011 §24++ atomically. Build ACTIVE advisory (tools/validators/validate-git-pushed-state.mjs in pnpm verify).** |
| `intake-event-schema-validation` | PR + per-session | error | NEW S011 unified-intake L3: every row in intake-log/*.jsonl validates against IntakeEvent schema. **Registered S011 §24++ atomically. Build ACTIVE (tools/validators/validate-intake-event.mjs in pnpm verify).** |
| `intake-source-class-coverage` | PR + per-session | error | NEW S011 unified-intake L3: all 4 source classes covered in intake-normalizers.md + schema + router exist. **Registered S011 §24++ atomically. Build ACTIVE (tools/validators/validate-source-class-coverage.mjs in pnpm verify).** |
| `plan-know-how` | PR | error | NEW S011 §24++++ B_KNOW_HOW_DISCIPLINE: every plan session >=S011 has §KH know-how consultation section OR know_how_consulted:true in frontmatter. Grandfathered: plans from pre-S011. Per B_KNOW_HOW_DISCIPLINE + plan-creation-protocol.md Step 6. **Registered S011 §24++++ atomically. Build ACTIVE (validate-plan-know-how.mjs in pnpm verify).** |
| `pe-connectivity` | per-session | blocking→advisory | NEW S022: Priority Engine connectivity audit — mechanically checks which platform elements SHOULD be connected to PE but aren't. BLOCKING when: active topic-plan missing priority_score/priority_band. ADVISORY when: depth-3+ plan missing per-session PE_SCORE, planned validators have no PE completion tracking, bedrock unchecked items have no Band-1 plan. Full PE function defined in validate-pe-connectivity.mjs header. **Registered S022 atomically. Build ACTIVE (validate-pe-connectivity.mjs in pnpm verify).** |
| `simulation-before-implementation` | PR | advisory→blocking | S023 Sandbox Policy: checks implementing/implemented plans have a sandbox spec with simulation_status: pass. ADVISORY now (grace for pre-S023). BLOCKING from S024 onward. Per B_SANDBOX_BEFORE_IMPLEMENTATION + sandbox-ratification-policy.md. **Build ACTIVE (validate-simulation-before-implementation.mjs in pnpm verify).** |
| `sandbox-lifecycle` | per-session | advisory | S023 Sandbox Policy: checks sandbox specs follow correct lifecycle order (sandbox→simulated→ratified→implementing→implemented). ADVISORY — detects lifecycle skipping. **Build ACTIVE (validate-sandbox-lifecycle.mjs in pnpm verify).** |
| `satisfaction-point` | per-session | advisory | NEW S026: SP-001 satisfaction-point detector — catches T1 trigger patterns ("I've run X", narration without output) in sonnet-turn.md + HANDOFF files. Highest-drift AI default. Teaching moment: "Paste the output. The output IS the evidence." Validator: validate-satisfaction-point.mjs. |
| `agreement-without-evidence` | per-session | advisory | NEW S026: SP-002 agreement bias detector — catches T2 trigger vocabulary (good point/exactly/I agree without because) in sonnet-turn.md and HANDOFF files without evidence. Part of "Drive Don't Fight" CHUNK 5. Validator: validate-agreement-without-evidence.mjs. |
| `scope-conflict` | PR + per-session | advisory | S028 K=2 MILESTONE: 3 scope violations in one session (credentials S2→S1, vercel link S2→S1, Root Directory S2→S1). Detects S2 actions proposed for S1 requirements in procedure docs. Advisory Phase 1; BLOCKING Phase 2. Validator: validate-scope-conflict.mjs. |
| `core-contamination` | PR | blocking | SROF-011 D.3: "never in core" structural enforcement. BLOCKING for external API calls (fetch/http/axios/curl) in tools/validators/ or .claude/hooks/. ADVISORY for principles citing external sources as canonical. Ensures S0/S1 governance code is self-contained — validators and hooks cannot depend on external services. Validator: validate-core-contamination.mjs. |
| `request-ledger` | per-session | advisory | S028: Governor Request Accountability — tracks every substantive request with PE score + resolution status. Surfaces OPEN items sorted by PE (highest first). SSoT: tools/config/governor-request-ledger.yaml. BLOCKING at session close (future). Governor directive: "accountability tracking with PE to sort by importance — consolidated into one source of truth." Validator: validate-request-ledger.mjs. |
| `skill-dna-alignment` | PR + per-session | advisory | S028: Validates all SKILL.md files aligned with current CSPS DNA (17 elements). Checks: scope_level, template_grade, backed_by_principle exists in principles.yaml, backed_by_contract exists in behavioral-contracts.md. Governor directive: "agents/skills dynamically aligned to CSPS DNA — bulletproof mechanically enforced." Validator: validate-skill-dna-alignment.mjs. |
| `laptop-patterns` | PR + per-session | blocking | S028 FSE (Governor directive: "mechanically enforce zero-laptop — global context mandatory element"). Scans governance docs for laptop-dependency language: "pnpm dev" / "localhost:PORT" / ".env.local" in procedures. BLOCKING for NEW docs; ADVISORY for pre-existing. S034-A: KNOWN_PRE_EXISTING list expanded (4 legacy topic plans grandfathered). **S056: gate-3-procedure.md added to KNOWN_PRE_EXISTING — description field references ".env.local" as an anti-pattern ("never .env.local"), not an instruction.** B_ZERO_LAPTOP_DEPENDENCY | P-OPER-001. Validator: validate-laptop-patterns.mjs. |
| `no-laptop-secrets` | PR + per-session | advisory | S028 (B_ZERO_LAPTOP_DEPENDENCY — P-OPER-001): Governor directive "zero dependency on local computers — mechanically enforce it now." Checks: .env.local files with real values (laptop dependency), missing vercel.json (can't deploy zero-laptop). Secrets belong in Vercel env vars, NOT .env.local. Validator: validate-no-laptop-secrets.mjs. |
| `deferred-target-session` | per-session | advisory | S028: surfaces governance debt — week-4/deferred audit-runner rows + contracts without target session S<NNN>. RP-001: "week-4 is a commitment, not a metaphor." Advisory Phase 1; surfaces debt visibly. PE=75. Validator: validate-deferred-target-session.mjs. |
| `schema-anchors` | PR | blocking | Session B: validates schema_anchor values against schema-registry.md. NEW anchors not in registry = BLOCKING. Pre-existing 248 anchors all registered. RP-003 enforcement — fields that don't resolve are decoration. Validator: validate-schema-anchors.mjs. |
| `generated-artifact-freshness` | per-session | advisory | Session B: checks generated:true artifacts (L3 instances) are fresh — generator not >24h newer than output, output not >7 days old. RP-004 enforcement. Validator: validate-generated-artifact-freshness.mjs. |
| `spine-hierarchy` | PR | blocking | S028: validates L1/L2/L3 structural rules. BLOCKING for missing sealed fields in L1, missing parent_l1_doctrine in L2/L3, non-canonical spines. ADVISORY for do_not_expand violations. Per spine-graduation-principle.md. PE=67. Validator: validate-spine-hierarchy.mjs. |
| `frontmatter-count-consistency` | PR | advisory | S028: body counts match description counts (RP-002). Checks: csps-platform-dna §1 rows, principles total_count, contracts total_count. PE=70. Validator: validate-frontmatter-count-consistency.mjs. |
| `opus-rzf-gap-tracking` | per-session | advisory | NEW S027+: validates Opus RZF NEGATIVE findings tracked to platform artifacts (SROF/backlog/session-state). ZF-as-production-chain: FINDING→TRACKING→RESOLUTION. Advisory Phase 1. Validator: validate-opus-rzf-gap-tracking.mjs. |
| `opus-cec-artifacts` | per-session | advisory | NEW S027+: validates CEC Applied YES claims cite traceable artifacts (commit sha or file path). Prevents CEC SP-001 — "Applied YES" without proof. Advisory Phase 1. Validator: validate-opus-cec-artifacts.mjs. |
| `dna-evidence` | PR | advisory | NEW S027: DNA application evidence per element — verifies §6b table in csps-platform-dna.md: present + 17 rows + each row has non-trivial observable evidence (specific validator/hook/command). Bridges the gap between "DNA element acknowledged" and "DNA element applied." Advisory Phase 1; BLOCKING Phase 2 after K=2 promotion. PE=67 S027. Validator: validate-dna-evidence.mjs. |
| `bottleneck-patterns` | per-session | advisory | NEW S027: Structural bottleneck detector — Class A (N+1 query patterns in API routes: auth lookup + DB call in same handler), Class B (O(N) validator file-walkers that degrade at 30 apps), Class C (missing @@index([tenantId]) → full table scans). All advisory. Scale projection: 30 apps × 1,000 tenants × 100 req/s. Spec: bottleneck-and-gradual-structures-S019.md §1. Validator: validate-bottleneck-patterns.mjs. |
| `diataxis-type` | PR | blocking | NEW S027: diataxis_type mandatory for pillar-0-governance/ .md artifacts. BLOCKING if any file missing diataxis_type. Valid enum: tutorial | how-to | reference | explanation. Enables documentation spine for AI CONCEPT_LOAD + navigation clarity. PE=67. Validator: validate-diataxis-type.mjs. |
| `multi-topic-decomposition` | per-session | advisory | S028 (P-META-024 SEALED Opus Turn 16): detects INTENT ABSORBED sections with >3 PE items handled without routing table; HANDOFF Zone B with many items and no decomposition note. Teaching moment: "N concerns in one prompt = N intake events. Emit routing table: [concern | spine | disposition]." **ACTIVE (validate-multi-topic-decomposition.mjs in pnpm verify).** |
| `comprehensive-response` | per-session | advisory | NEW S027: SP-003 comprehensive response detector — catches T3 trigger vocabulary (could you also / while you're at it / and also) without PE score annotation; flags Done sections with 5+ items and no PE ordering; checks raw-thoughts-queue populated (vaulting active). Teaching moment: "ONE focal point. PE-score the rest. Vault to raw-thoughts-queue." Part of CHUNK 5 enforcement 33%→36%. Validator: validate-comprehensive-response.mjs. |
| `dead-links` | PR + per-session | blocking | S026 Phase 2: BLOCKING only if broken link count INCREASES above S026 baseline (71, adjusted from S025's 67 as scan scope expanded). Pre-existing links advisory. New broken links = BLOCKING. Ratchet pattern — SSoT integrity maintained going forward. S083 Phase B.1 (imp_DEAD_LINKS_CRLF_FRONTMATTER): CRLF-safe frontmatter detection — normalize \r\n→\n before indexOf; 2000-char truncation fallback removed; hasFrontmatter guard added. Baseline shift 71→70 (one CRLF false-positive eliminated on ai-collaboration-charter.md). Validator: validate-dead-links.mjs. |
| `crystallization-bypass` | PR | advisory | S026 "Drive Don't Fight" architecture: catches SP-005 pattern — AI acts on Layer 1 "just figure it out" expression without running Step 0a crystallization (Q1c/Q2c/Q3c). ADVISORY initially; BLOCKING after K=2. Per P-META-022 (Human Intent Crystallization) + sample-library SP-005. **ACTIVE (validate-crystallization-bypass.mjs in pnpm verify).** |
| `intent-crystallized` | PR | blocking | S023+S025: checks active deep_quality plans have intent_crystallized + threshold_route + failure_signal fields. BLOCKING for deep_quality plans missing intent_crystallized. ADVISORY for missing failure_signal (P-META-023 M3) and threshold_route. S025: failure_signal advisory check added. Per B_INTENT_CRYSTALLIZATION + P-META-023. **ACTIVE (validate-intent-crystallized.mjs in pnpm verify).** |
| `routing-declared` | PR | advisory | S023+S025: checks active plans declare threshold_route (which wizard template was matched). Valid routes include personal.finance (S025 addition for Budget Planner). Also confirms routing.config.ts exists. ADVISORY — encourages Threshold adoption. **ACTIVE (validate-routing-declared.mjs in pnpm verify).** |
| `governing-intent-coverage` | PR + per-session | advisory | S027 P-META-025 C&I: checks that B_* contract bodies in behavioral-contracts.md have `governing_intent:` field (the Layer 3 intent the rule serves). ADVISORY initially — contracts without governing_intent are usable but reduce C&I alignment. K=2 promotion → BLOCKING. Build Session B: validate-governing-intent-coverage.mjs. Per P-META-025 context-intent-principle.md. |
| `ux-principles-declared` | PR | advisory | S023 UX/UI DNA: checks page.tsx files declare ux_principle. Strong customer focus — every screen declares its governing UX principle. ADVISORY → BLOCKING after K=2. **Build ACTIVE (validate-ux-principles-declared.mjs in pnpm verify).** |
| `ux-audit` | PR + per-session | advisory→blocking | S060 PROTO-I Pipeline 6: scans apps/csps-playground/src/app/platform/**\/page.tsx for full UX context declaration — pageDNA (const pageDNA = {...}), purpose field, and PageContext or PageHeader import (score 0-3 per page). Advisory per page with score < 3. BLOCKING if coverage < 50% AND more than 10 pages scanned. Pairs with T1 gate (pre-tool-use-ux-creation-gate.sh) which prevents violations at write time. Part of Audit Pipeline 6 in libs/audits/src/dispatcher.ts. **Build ACTIVE S060 (validate-ux-audit.mjs in pnpm verify).** |
| `text-input-standards` | PR + per-session | advisory | S060 Q1 Governor directive: ALL free-text fields on the platform MUST offer voice input + file upload. Scans apps/csps-playground/src/app/**\/page.tsx for raw textarea or input[type=text] without VoiceFileInput component or @voice-exempt exemption comment. Advisory per violation. Pairs with T1 gate (pre-tool-use-text-input-standard-gate.sh). VoiceFileInput component: libs/ui/src/VoiceFileInput.tsx. **Build ACTIVE S060 (validate-text-input-standards.mjs in pnpm verify).** |
| `voice-profile` | PR + per-session | advisory | S062 PROTO-S062-K GAP-1 Component B: checks that all app page.tsx files containing form elements declare voiceProfile in pageDNA AND expose it via data-voice-profile attribute on the form container. Advisory per violating page; @voice-exempt comment exempts. Phase 1 ratification of debt-collection required this validator for platform-wide voice profile enforcement. **Build ACTIVE S062 (validate-voice-profile.mjs in pnpm verify).** |
| `isolation-layers` | per-session | blocking | Session C S022: checks ZenStack enhance() active + Postgres RLS documented + AuditEvent trigger present. BLOCKING if ZenStack bypassed. ADVISORY if RLS undocumented. Per INS-S022-004 defense-in-depth. **Build ACTIVE (validate-isolation-layers.mjs in pnpm verify).** |
| `webhook-idempotency` | PR | advisory | Session C S022: checks Clerk webhook handlers have existence check before create. Per SYS-1 flow audit. ADVISORY — idempotency gaps. **Build ACTIVE ADVISORY (validate-webhook-idempotency.mjs in pnpm verify).** |
| `solo-user-flow` | per-app | advisory | Session C S022: checks each app with Clerk webhooks declares solo_user_flow policy. Per GAP-A1 flow audit. ADVISORY — solo signup conversion gap. **Build ACTIVE ADVISORY (validate-solo-user-flow.mjs in pnpm verify).** |
| `pe-situation-declared` | per-session | advisory | Session C S022: confirms active_situation field in session-state.json matches pe-situation-registry.md. Per INS-S022-008 situation registry. ADVISORY. **Build ACTIVE ADVISORY (validate-pe-situation-declared.mjs in pnpm verify).** |
| `gdpr-erasure-path` | per-app | advisory | Session C S022: confirms eraseUser() in libs/integrations + DELETE /api/settings/account per app. Per GAP-D1 flow audit. ADVISORY → BLOCKING before EU launch. **Build ACTIVE ADVISORY (validate-gdpr-erasure-path.mjs in pnpm verify).** |
| `subscription-error-handling` | PR | advisory | Session C S022: confirms write routes with subscription gate return structured error {error, message}. Per GAP-E1 flow audit. ADVISORY → BLOCKING before launch. **Build ACTIVE ADVISORY (validate-subscription-error-handling.mjs in pnpm verify).** |
| `consolidation-check` | PR | blocking→advisory | NEW Session A S022: Checks all active topic-plans have §0 CONSOLIDATION CHECK section. BLOCKING for S023+ plans. ADVISORY for S022 and earlier (grace period). Enforces "CHECK WHAT EXISTS!!" consolidation discipline mechanically. Per over-the-system-audit-S022.md §3. **Build ACTIVE (validate-consolidation-check.mjs in pnpm verify).** |
| `plan-zf-requirement` | per-session | advisory | NEW Session A S022: Checks active topic-plans have zf_required_level field (1=commit/2=phase/3=session). Always ADVISORY — encourages gradual adoption. Per csps-consolidation-and-zf-hardening.md §1.4. **Build ACTIVE ADVISORY (validate-plan-zf-requirement.mjs in pnpm verify).** |
| `plan-ai-defaults` | PR | advisory→blocking | NEW S022 Governor directive: when a plan is primarily shaped by AI training defaults rather than CSPS protocols, surface a visible notification. Scans topic-plans for ai_defaults_influence: partial|dominant. Advisory when partial + unratified; BLOCKING when dominant + unratified. Enables Governor + Opus to catch AI-invented values before they become implementation. Per B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS + inner-ai-defaults/reasoning-patterns.md. **Registered S022 atomically. Build ACTIVE advisory (validate-plan-ai-defaults.mjs in pnpm verify).** |
| `mini-tree-integrity` | PR | advisory | S030 mini-tree-split-protocol: every file with mini_tree_root:true must have valid sub_files entries; every sub_file must exist; directories with 2+ .md files need an intro with mini_tree_root:true. Bidirectional integrity check. Per mini-tree-split-protocol.md §6. **LIVE S030 E1. S032 update: parser fixed for CRLF line endings; 4 mini-tree README intro files created (behavioral-contracts, external-integrations, audit-runner, ai-behavior-spine). validate-mini-tree-integrity.mjs.** |
| `permanence-coverage` | per-session | advisory→blocking | v2.0 PROTO-S062-A STEP 2: FRONTMATTER-FIRST dual-metric. Reads enforcement_trio: YAML frontmatter block (canonical ground truth). Body-scan fallback for unmigrated files (advisory only). Reports: has_trio=N (% with frontmatter block) + full_trio=N canonical (all 3 statuses non-none) + partial=N (status:none = genuine gap). BASELINES post-S062 STEP 6 ratchet: has_frontmatter=66, full_trio_canonical=38 (58%). Ratcheted 32→38 on Opus ADVANCE (STEP 6). BLOCKING on regression below baselines. ADVISORY on partial (genuine coverage gap, not migration error). Pre-migration body-scan was 100% (over-counted via cross-references) — FINDING-S062-PERMANENCE-DRIFT. **Updated S062 PROTO-S062-A STEP 6. Build ACTIVE (validate-permanence-coverage.mjs in pnpm verify).** |
| `skill_invocation_rate` | per-session | advisory | S067 STEP 4 — C2 prevention. Checks each skill in council-registry.md has non-empty trigger patterns. Advisory for skills without triggers. 3/3 behavioral tests. **Registered S067 STEP 4. Build ACTIVE (validate-skill-invocation-rate.mjs + council-invocation-dispatcher.mjs).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + C2 |
| `inventory_scan_coverage` | per-session | advisory | S067 STEP 5 — P-META-029. Validates 11 platform registries are accessible for inventory queries. 3/3 behavioral tests. **Registered S067 STEP 5. Build ACTIVE (validate-inventory-scan-coverage.mjs + platform-inventory-scan.mjs + pre-tool-use-inventory-scan-required.sh).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-029 |
| `prevention_class_required` | per-session | advisory | S067 STEP 7 — APPENDIX C. Validates improvement-register + gap-recurrence-register entries have prevention_class field. Advisory for unclassified. 2/2 behavioral tests. **Registered S067 STEP 7. Build ACTIVE (validate-prevention-class-required.mjs + migrate-S067-prevention-class-field.mjs).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-029 |
| `threshold_routing_coverage` | per-session | advisory | S067 STEP 3 — PROTO-S067-MASTER-THRESHOLD-ROUTER. M-42 UNIFIED THRESHOLD-ROUTER: validates threshold-intake-log.yaml entries have 4-axis classification (scope/intent/mandate_relation/route). Advisory for historical entries. 5/5 behavioral tests (A=PROCESS-NOW / B=VAULT / C=ESCALATE / D=INVOKE / E=SHAPE-TIER). **Registered S067 STEP 3. Build ACTIVE (validate-threshold-routing-coverage.mjs + tools/scripts/threshold-router.mjs).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + M-42 |
| `consolidation_pass_active` | per-stop | advisory | S067 STEP 2 — PROTO-S067-MASTER-THRESHOLD-ROUTER. ADVISORY: post-stop-consolidation-pass.sh now ACTIVE (was STUB). Detects 6 duplication patterns A-F (List/Rule/Definition/Example/Citation/Cross-ref) in governed artifacts per EXT-20260505-003-A. Heuristic: ≥3 occurrences = candidate. Exits 0 always; findings to stderr. S075 B4 EXTENDED: Pattern G (structural ID/concern overlap — would auto-catch D15-D17 collision) added to consolidation-pattern-detector.mjs. PROMOTION SPLIT (B5 S075): Pattern G → blocking-eligible (structural integrity); Patterns A-F → death_date=S078 unless blast-radius Governor approval by S077 (doc-content findings, ~56 docs). promotion_trigger (Patterns A-F): Governor approves blast-radius → promotes to BLOCKING for new governed artifacts; legacy = advisory. **Registered S067 STEP 2. Build ACTIVE. Pattern G added S075 B4.** | B_CONSOLIDATION_PASS + P-OP-001 + P-META-019 |
| `claimed_mechanical_presence` | per-session | advisory | S067 STEP 6.3a — C1 EXISTS_NOT_ACTIVE prevention. Scans moat-registry.md + audit-runner.md LIVE rows for claimed validator/hook paths; verifies files exist on disk. ADVISORY for CSEP-pending planned validators; BLOCKING for LIVE rows with phantom paths. 3/3 behavioral tests. **Registered S067 STEP 6.3a. Build ACTIVE (validate-claimed-mechanical-presence.mjs + claimed-mechanical-presence-test.sh).** | C1_EXISTS_NOT_ACTIVE + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `bstar_trio_coverage_strict` | per-session | advisory | S067 STEP 6.3a — C3 TEXT_WITHOUT_GATE prevention. Stricter trio path verification: for every B_*/P_* with enforcement_trio, verifies T1/T2/T3 declared paths exist on disk. ADVISORY for pre-S055 path debt (expected); BLOCKING if new S055+ contract has missing paths. 3/3 behavioral tests. **Registered S067 STEP 6.3a. Build ACTIVE (validate-bstar-trio-coverage-strict.mjs + bstar-trio-coverage-strict-test.sh).** | C3_TEXT_WITHOUT_GATE + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `external_integration_gate` | pre-Edit/Write | advisory | S067 STEP 6.3b — C5 RE_DERIVATION_KNOWN prevention. The M-19 phantom hook MADE REAL. ADVISORY S067: emits warning when AI edits integration-related files (vercel/supabase/clerk/.env etc.) without having read the canonical external-integrations doc. Prevents re-deriving integration decisions already documented. 3/3 behavioral tests. **Registered S067 STEP 6.3b. Build ACTIVE (pre-tool-use-external-integration-gate.sh + external-integration-gate-test.sh).** | C5_RE_DERIVATION_KNOWN + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `knowledge_writeback_required` | pre-commit | advisory | S067 STEP 6.3b — C9 PATCH_WITHOUT_KNOWLEDGE_UPDATE prevention. ADVISORY S067: warns when staging integration fix code without a corresponding canonical knowledge doc update. BLOCKING S068. Closes C9 gap that produced the vercel.md R9 / da23887 → 80777fbc window. 3/3 behavioral tests. **Registered S067 STEP 6.3b. Build ACTIVE (pre-commit-knowledge-writeback-required.sh + knowledge-writeback-required-test.sh).** | C9_PATCH_WITHOUT_KNOWLEDGE_UPDATE + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `cross_finding_cluster` | per-session | advisory | S067 STEP 6.3c — C6 FRAGMENT_FINDINGS prevention. Scans improvement-register + gap-recurrence-register for same-session entries sharing keyword clusters (shared root cause). ADVISORY only — suggests clustering. Always exits 0. 3/3 behavioral tests. **Registered S067 STEP 6.3c. Build ACTIVE (validate-cross-finding-cluster.mjs + cross-finding-cluster-test.sh).** | C6_FRAGMENT_FINDINGS + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `sonnet_checkpoint_relay` | per-session | advisory | S067 STEP 6.3c — C7 ASYMMETRIC_RELAY prevention. Scans sonnet-turn.md CHECKPOINTs for commit_sha + behavioral test results + ZF cycles with file citations. ADVISORY when fields missing. 3/3 behavioral tests (5 CHECKPOINTs scanned). **Registered S067 STEP 6.3c. Build ACTIVE (validate-sonnet-checkpoint-relay.mjs + sonnet-checkpoint-relay-test.sh).** | C7_ASYMMETRIC_RELAY + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `reactive_opus_prevention` | per-session | advisory | S067 STEP 6.3c — C8 REACTIVE_OPUS prevention (session-open T3 extension). session-open.sh now injects proactive Opus review trigger + META-QUESTION + D9 recency-bias override at every tab start. 3/3 behavioral tests. **Registered S067 STEP 6.3c. Build ACTIVE (session-open.sh C8 extension + reactive-opus-prevention-test.sh).** | C8_REACTIVE_OPUS + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `per_step_gate_tier` | per-session | advisory | S067 STEP 6.3d — C13 AUTO_EXECUTE_SCOPE_INHERITANCE prevention. Scans PROTO-*.md files: warns if PROTO has multiple STEP sections + gate_tier:full-advance but no per_step_gate_tier:check-in declared. ADVISORY S067 — PROTO-S067 itself has per_step_gate_tier:check-in (engraved by F-NEW-16 lesson). 3/3 behavioral tests (8 PROTOs scanned). **Registered S067 STEP 6.3d. Build ACTIVE (validate-per-step-gate-tier.mjs + per-step-gate-tier-test.sh).** | C13_AUTO_EXECUTE_SCOPE + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `tab_transfer_completeness` | pre-Write/Edit + per-session | advisory | S067 STEP 6.4 — B_META_QUESTION_DISCIPLINE. Scans tab-transfer artifacts (HANDOFF-*.md + tab-transfer-template.md) for false-assumption checklist section + ≥10 items. Template self-validates (recursive consistency). ADVISORY S067 → BLOCKING S068. 3/3 behavioral tests. **Registered S067 STEP 6.4. Build ACTIVE (validate-tab-transfer-completeness.mjs + pre-tool-use-false-assumption-gate.sh + B_META_QUESTION_DISCIPLINE.md + tab-transfer-template.md).** | B_META_QUESTION_DISCIPLINE + P-META-029 + M-38 |
| `opia_audit_completeness` | per-Opus-ACK | advisory | S067 STEP 6.5 — OPIA framework. Scans opus-turn.md ACK blocks for 15-point OPIA table (≥10 numbered rows + ✓/✗ markers). ADVISORY S067 → BLOCKING S068. Inaugural: S067 turns 37-38 + commit 8fa3cc00. Includes §2 PVA (Process-Value-Audit) cadence with HIGH/MEDIUM/LOW/DEAD yield tiers. **Registered S067 STEP 6.5. Build ACTIVE (validate-opia-audit-completeness.mjs + tools/council/opia-checklist.md).** | OPIA + P-META-029 + B_HUMBLE_CONSOLIDATION_DISCIPLINE |
| `apps_typecheck` | per-verify | advisory | S067 STEP 6.5 — C11 HIDDEN_REGRESSION closure. apps/ TypeScript check added to verify.mjs cycle (was excluded from typecheck_recursive which only covers packages/**). Closes gap where apps/ TS errors could slip through. **Registered S067 STEP 6.5. Build ACTIVE (tools/verify.mjs apps_typecheck cycle extension).** | C11_HIDDEN_REGRESSION + P-META-029 |
| `session_source_usage` | per-session | **blocking** | S067 STEP 1 — PROTO-S067-MASTER-THRESHOLD-ROUTER. BLOCKING: any hook computing session locally (bypassing tools/lib/session-source.mjs) → exit 1. Fixes F-NEW-17 (358/358 garbage session=unknown entries). 3/3 behavioral tests. **Registered S067 STEP 1. Build ACTIVE (validate-session-source-usage.mjs in pnpm verify).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `finding_scheduling` | per-session | advisory→blocking-by-K | S066 WAVE-2-STEP-2 — auto-scheduling validator for improvement-register + gap-recurrence-register. K=1 overdue → ADVISORY (exit 0 + stderr). K>=2 overdue → BLOCKING (exit 1). K=1 unpromoted 3+ sessions → auto-promote to K=2. Reads must_address_by_session field (added by STEP-1 migration). **S076 EXTENDED: also reads must_address_by_date (calendar deadline) — today > date + no fix_sha + no defer = ADVISORY at k=1 / BLOCKING at k≥2; today > date+14d → auto-promote k=1→k=2. Tests A/B/C/D/E/F all pass. Governor directive: UUID upgrade 2026-06-16.** **Registered S066, extended S076. Build ACTIVE.** | B_STRUCTURAL_PREVENTION_DISCIPLINE + M-30 + P-META-019 |
| `proto_core_seed` | per-session | advisory→blocking | S066 WAVE-1-STEP-2 — validates all PROTO-*.md for required frontmatter (plan_item_id, core_seed_present: true, gate_tier) and body sections (## Core Seed, ## DONE WHEN, ## ZF gate). Grandfathered PROTOs advisory only; new PROTOs blocking. PROTO-S066-WAVE-1.md self-passes. **Registered S066 WAVE-1-STEP-2. Build ACTIVE (validate-proto-core-seed.mjs in pnpm verify + pre-commit-proto-core-seed-mandatory.sh).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-007 |
| `shape_check` | per-write | advisory | S066 WAVE-1-STEP-1 — ADVISORY S066 (exit 0 + stderr warning). Fires on Write|Edit to tools/council/opus-turn.md >200 chars; warns if SHAPE block missing from first 500 chars. BLOCKING from S067 after 5 clean Opus turns. 5/5 behavioral tests. **Registered S066 WAVE-1-STEP-1. Build ACTIVE (.claude/hooks/pre-tool-use-shape-check.sh + tools/scripts/shape-check-gate.mjs, PreToolUse settings.json).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `pre-commit-describe-without-implement` | per-commit | **blocking** | S063 ITEM 2 — R4 reasoning fix (describe-feels-like-solve). PreToolUse hook blocks planning-language commits (Proposed fix / Structural fix candidate / Will fix / Planned fix) without staged implementation files UNLESS defer-to-session: S<NNN> in body. 3/3 behavioral tests. **Registered S063 ITEM 2. Build ACTIVE (.claude/hooks/pre-commit-describe-without-implement.sh + tools/scripts/describe-without-implement-gate.mjs).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `corespine-check` | per-write | advisory | S063 ITEM 3.1 — B_CORE_SPINE_DISCIPLINE T1 (partial). Advisory PreToolUse hook on Write to .md files; warns if core_spine or schema_anchor missing from frontmatter. Never blocks. **Registered S063 ITEM 3.1. Build ACTIVE (.claude/hooks/pre-tool-use-corespine-check.sh + tools/scripts/corespine-check-gate.mjs, PreToolUse settings.json).** | B_CORE_SPINE_DISCIPLINE + P-ARCH-028 |
| `governor_prompts` | per-session | advisory | S063 ITEM 3.2 — B_GOVERNOR_PROMPTS T2. Validates VAULT/governor-prompts/ directory existence and GP-S<NNN>-<NN> entry format (timestamp + verbatim + tags). Advisory exit 0. **Registered S063 ITEM 3.2. Build ACTIVE (validate-governor-prompts.mjs in pnpm verify).** | B_GOVERNOR_PROMPTS + P-META-012 |
| `template_citation` | per-session | advisory | S063 ITEM 3.3 — B_TEMPLATE_FIRST_CREATION T2. Scans HANDOFF and PROTO files for template_used/parent_template frontmatter fields. Advisory. **Registered S063 ITEM 3.3. Build ACTIVE (validate-template-citation.mjs in pnpm verify).** | B_TEMPLATE_FIRST_CREATION + P-META-015 |
| `structural_fix` | per-session | advisory→blocking | S063 ITEM 3.4 — B_STRUCTURAL_PREVENTION_DISCIPLINE T2. Walks gap-recurrence-register K>=2 entries; BLOCKS if K>=3 status=open with no structural_fix_committed. Advisory on K=2 open. **Registered S063 ITEM 3.4. Build ACTIVE (validate-structural-fix.mjs in pnpm verify).** | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `five_surface` | per-session | advisory | S063 ITEM 3.5 — B_FIVE_SURFACE_ENGRAVING T2. Checks each B_*.md contract for 5-surface completeness: contract file + T2 validator path + T1 hook path + T3 memory + schema_anchor. Advisory during initial deployment. **Registered S063 ITEM 3.5. Build ACTIVE (validate-five-surface.mjs in pnpm verify).** | B_FIVE_SURFACE_ENGRAVING + P-META-013 |
| `gradual_build` | per-session | advisory | S063 ITEM 3.6 — B_GRADUAL_BUILD_BY_FOUNDATIONS T2. Checks PROTO files for defined STEP structure and ZF gates per step. Advisory for S060+ PROTOs. **Registered S063 ITEM 3.6. Build ACTIVE (validate-gradual-build.mjs in pnpm verify).** | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `proto_receipt` | per-session | advisory | S063 PHASE B.2 — FINDING-OPUS10-5. Scans council files for PROTO citations and confirms each cited PROTO file exists in docs/plan/protos/. Advisory — planned PROTOs may not yet exist. **Registered S063 PHASE B. Build ACTIVE (validate-proto-receipt.mjs in pnpm verify).** | B_PRE_CLOSE_VERIFICATION + P-META-006 |
| `pre-commit-claim-validator-gate` | per-commit | **blocking** | S063 ITEM 1 — Structural fix for gap_DONE_CLAIM_BEFORE_VALIDATOR_CONFIRMS K=4 S062. PreToolUse hook that intercepts Bash tool calls to git commit; BLOCKS if commit message contains DONE/SEALED/COMPLETE/CLOSED/RATIFIED but verify-last-run.md is older than HEAD commit. Ensures THIS-HEAD verify required for all claim keywords. 11/11 behavioral tests passing. **Registered S063 ITEM 1. Build ACTIVE (.claude/hooks/pre-commit-claim-validator-gate.sh + tools/scripts/claim-validator-gate.mjs, PreToolUse settings.json).** | P-META-019 + B_PRE_CLOSE_VERIFICATION + gap_DONE_CLAIM_BEFORE_VALIDATOR_CONFIRMS |
| `pre-commit-bstar-engraving-gate` | per-write | **blocking** | S064 Item 2.1 — Check-In gate for new B_*.md contracts. Write tool creating new B_*.md behavioral contract BLOCKS unless frontmatter contains `opus_reviewed_seed: <SHA>`. EXEMPT: Edit tool (modified existing), placeholder file stems. Proves Opus reviewed core-seed text. 5/5 behavioral tests passing. **Registered S064 Item 2.1. Build ACTIVE (.claude/hooks/pre-commit-bstar-engraving-gate.sh + tools/scripts/bstar-engraving-gate.mjs, PreToolUse settings.json).** | B_FIVE_SURFACE_ENGRAVING + P-META-007 |
| `bstar-trio-gate` | per-edit | blocking | PROTO-S062-A STEP 4 (META-T1): PreToolUse hook — fires on Edit/Write to B_*.md behavioral contracts. Validates enforcement_trio frontmatter structure: (1) frontmatter has enforcement_trio block (2) t1+t2+t3 all present (3) each tier has {tier,path,status} (4) status=none requires non-null exempt_reason (5) path existence check [advisory]. Exit 1 BLOCKING if rules 1-4 violated. "Bleed stopped": future contracts cannot bypass enforcement_trio. Honest framing: STEP 4 is META-T1 (format guard) not contract-specific T1 — the 34 partials still have t1.status=none; STEP 5b triage resolves those. **Registered S062 PROTO-S062-A STEP 4. Build ACTIVE (.claude/hooks/pre-tool-use-bstar-trio-gate.sh + tools/scripts/bstar-trio-gate.mjs, Write|Edit matcher in settings.json).** |
| `north-star-gate` | per-session | advisory→blocking | NEW S060 PROTO-NORTHSTAR-1: North Star Presence Protocol (NSPP) gate compliance. CHECK 1 — Gate 1 (T1): northStar block present in session-open-context.mjs JSON output (Opus sees NS question at every session start). CHECK 2 — Gate 2 (T2): ADVANCE/HOLD/DRIFT session classification present in sonnet-turn.md (Gate 2 applied at session close). CHECK 3 — Structural: gap_NSPP_MISSING K≥2 status=open → BLOCKING. Advisory during adoption period; blocking once K=2 recurrence registered. **Registered S060 atomically. Build ACTIVE (validate-north-star-gate.mjs in pnpm verify). Ref: docs/plan/pillar-0-governance/CSPS-NORTH-STAR.md §3 NSPP Gates.** |
| `partial-processes` | weekly | advisory | NEW S029 Governor directive: weekly "What do we do if..." audit. Identifies partial processes across the platform: validators registered but not built, backlog items without session targets, mini-tree directories without intro files, capacity elements at risk, deferred audit slugs past threshold. For each finding: what exists, what's missing, what to do. Advisory (blocking when partial-process count > 20). Run: `pnpm audit:weekly`. See: validate-partial-processes.mjs. |
| `audit-health` | per-session | warn | NEW S011 §24+++++++++++ THE AUDITOR OF AUDITORS: validators newer than audit-runner.md (stale descriptions) + constitutional changes requiring full re-run + validator negative test coverage. Meta-audit of the audit mechanism. Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2. **Registered S011 §24+++++++++++ atomically. Build ACTIVE (validate-audit-health.mjs in pnpm verify).** |
| `model-tier-currency` | per-session | warn | NEW S011 §24+++++++++++++++: model tier vocabulary registry (tools/model-tier-registry.yaml) is present + DEEP_REASONING/STANDARD_BUILD/MECHANICAL_SCAN all have current_mapping.model_id + last_updated within 180 days. The model-agnostic vocabulary that decouples intent from implementation. **Registered S011 §24+++++++++++++++ atomically. Build ACTIVE (validate-model-tier-currency.mjs in pnpm verify).** |
| `session-receipt` | per-session | info | NEW S011: §17 receipt chain continuity — latest closing-summary has attestation + session numbering is continuous. Advisory. **Registered S011 atomically. Build ACTIVE advisory (validate-session-receipt.mjs).** |
| `ai-defaults-freshness` | per-session | warn | NEW S011 §24++++++++++++: inner-ai-defaults registry has csps_model_version field + calibrated within 90 days + all category files present. Fires if model version changes and registry not refreshed. Per EP-013 + B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS. **Registered S011 atomically. Build ACTIVE (validate-inner-ai-defaults-freshness.mjs in pnpm verify).** |
| `inner-ai-defaults-enforcement-rate` | per-session | measurement→advisory→blocking | NEW S019 L11 opus-lessons: measures % of inner-AI-defaults entries that have live mechanical validators vs. "impl deferred." Current rate: 6% (2/31 entries). enforcement_stage: measurement (exits 0 always) — tracks gap; advisory stage activates S020; blocking (<25%) activates S025. Purpose: make behavioral governance enforcement rate visible as a metric so the Governor can track improvement session-over-session. Captures the most important governance quality gap: declared contracts without mechanical enforcement are aspirational, not governance. **Registered S019 atomically. Build ACTIVE measurement (validate-inner-ai-defaults-enforcement-rate.mjs in pnpm verify).** | S019 + L11-opus-lessons + P-META-019 + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `opus-audit-due` | per-session | advisory→blocking | NEW S019 Part3 opus-lessons: Mechanical enforcement of Opus audit trigger criteria. Reads session-state.json opus_audit section. Level 1: Session Interval Gate (SIG) — blocks when sessions_since_opus_review >= 10. Level 2: manual opus_audit_due=true flag — blocks immediately. Levels 3-6 (BPG, PEG, PIA, KCP) deferred → VLT-S019-BPG/PEG/PIA/KCP. Baseline: S019 review = session 0, next due S029. Purpose: prevent 19-session accumulation of architectural debt without external review. **Registered S019 atomically. Build ACTIVE (validate-opus-audit-due.mjs in pnpm verify).** | S019 + Part3-opus-lessons + sessions_since_opus_review |
| `hook-lifecycle-state` | per-session | advisory | NEW S021 CEC N3: reports all declared hooks with lifecycle-state (stub vs active). Coverage Levels: ✓ Level 1 list hooks ✓ Level 2 enforcement-claim mismatch (entry cites STUB hook as live) ✗ Level 3 stub promotion timelines → VLT-S021-HOOK-STUB-TARGETS. stub_rate baseline S021: 46% (11/24). Enables accurate Track A enforcement planning. **Registered S021 CEC atomically. Build ACTIVE advisory (validate-hook-lifecycle-state.mjs in pnpm verify).** |
| `session-harvest-readiness` | per-session | **BLOCKING** | NEW S021. **S022 PROMOTED TO BLOCKING** per Governor directive "ZF within system not over it." Exits 1 when validators_checked ≥ 40 AND extraction missing — session cannot be declared DONE without harvest. Previously advisory. Now: validate-session-harvest-readiness.mjs exits 1 on HARVEST_READY (mature session). Exits 0 on HARVEST_DONE or NOT_YET_MATURE. **Build ACTIVE BLOCKING (validate-session-harvest-readiness.mjs in pnpm verify).** |
| `prose-no-confirmation-seeking` | per-session | advisory | NEW S021 enforcement-rate-uplift Track B: scans verify-last-run.md + session artifacts for banned confirmation-seeking tail phrases per B_NO_CONFIRMATION_SEEKING. Coverage Levels: ✓ Level 1 artifact scan ✓ Level 2 session artifacts ✗ Level 3 live transcript → VLT-S021-TRANSCRIPT-SCAN. Always exits 0 (ADVISORY). Covers inner-AI-defaults: prose-confirmation-seeking. **Registered S021 atomically. Build ACTIVE advisory (validate-prose-no-confirmation-seeking.mjs in pnpm verify).** |
| `decision-frame-citation` | per-session | advisory | NEW S021 enforcement-rate-uplift Track B: scans recent closing-summaries + handoffs for multi-option discussions without PCR structure (Pros/Cons/Recommendation). Coverage Levels: ✓ Level 1/2 artifact scan ✗ Level 3 trivial-skip violation → VLT-S021-PCR-TRIVIAL ✗ Level 4 quality → VLT-S021-PCR-QUALITY. Covers inner-AI-defaults: prose-naked-question + reasoning-implicit-decision-no-PCR (one validator, two entries). **Registered S021 atomically. Build ACTIVE advisory (validate-decision-frame-citation.mjs in pnpm verify).** |
| `concept-load-declared` | per-session | advisory | NEW S021 enforcement-rate-uplift Track B: checks recent closing-summaries for P-META-020 spine declarations (CONCEPT_LOAD / GVRN L2 / ARCH L2 / etc). Coverage Levels: ✓ Level 1/2 closing-summary scan ✗ Level 3 turn-boundary verification → VLT-S021-CONCEPT-BOUNDARIES. Covers inner-AI-defaults: reasoning-context-depth-degradation. **Registered S021 atomically. Build ACTIVE advisory (validate-concept-load-declared.mjs in pnpm verify).** |
| `subagent-spawn-preamble` | per-session | advisory | NEW S021 enforcement-rate-uplift Track B: verifies haiku-spawn-template.md + class-b-agent-spawn-preamble.template.md exist with required fields + AAP pre-tool-use hook registered in settings.json. Coverage Levels: ✓ Level 1/2 infrastructure check ✗ Level 3 live spawn scan → VLT-S021-SPAWN-TRANSCRIPT. Covers inner-AI-defaults: tooling-subagent-no-preamble. **Registered S021 atomically. Build ACTIVE advisory (validate-subagent-spawn-preamble.mjs in pnpm verify).** |
| `inner-ai-defaults-enforcement-rate` | per-session | measurement→advisory→blocking | NEW S019 L11 opus-lessons: measures % of inner-AI-defaults entries that have live mechanical validators vs. "impl deferred." Current rate: 29% (9/31 entries). enforcement_stage: measurement (exits 0 always) — tracks gap; advisory stage activates S025; blocking (<25%) activates S030 (revised from S025 — target now MET at S021). **S021 amendment: rate advanced from 6% (S019) → 10% (S020) → 29% (S021). Target hit.** **Registered S019 atomically. Build ACTIVE measurement (validate-inner-ai-defaults-enforcement-rate.mjs in pnpm verify).** | S019 + L11-opus-lessons + P-META-019 + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `layer-boundary` | PR + per-session | blocking | NEW S020 LAYER-1: Enforces Platform Layer Boundaries (L0 Core libs/ must NEVER import from L1/L2 apps/). Scans all libs/ .ts files for static import + require() patterns pointing to apps/ paths. Coverage Levels: ✓ Level 1 static imports ✓ Level 2 require() calls ✗ Level 3 dynamic import() → VLT-S020-LAYER-DYNAMIC ✗ Level 4 indirect re-exports → VLT-S020-LAYER-INDIRECT. Exit: BLOCKING (1) when any L0→L1/L2 import found; CLEAN (0) otherwise. Baseline S020: files_scanned=5, violations=0, status=CLEAN. Resolves VLT-S019-LAYER-BOUNDARY. **Registered S020 LAYER-1 atomically. Build ACTIVE BLOCKING (validate-layer-boundary.mjs in pnpm verify).** | S020 + platform-layer-boundaries.yaml + VLT-S019-LAYER-BOUNDARY |
| `gradual-bundling` | per-session | advisory | NEW S021 Governor directive: Comprehensive check of ALL 7 gradual bundling elements: (1) depth discipline (L1/L2/L3 — L<N+1> blocked until L<N> ZF), (2) humble batching (≤3 P1 per session, related items only), (3) core spiral cycle position (design→ZF→extract→engrave→advance), (4) Gradual Execution Protocol (enforce_stage coherence, Stage 1 before Stage 3), (5) ZF levels, (6) B_COMPLETION_OVER_SHINY (≥50% done = 1.5× PE), (7) Simulation-ZF gate (no implementing without simulation). Coverage Levels: ✓ L1 depth_chosen declared ✓ L2 near-complete plans surfaced ✓ L3 enforce_stage coherence ✗ L4 batch composition validation → VLT-S021-BATCH-COMPOSE ✗ L5 PE score integration → VLT-S021-PE-LIVE. **Registered S021 atomically. Build ACTIVE advisory (validate-gradual-bundling.mjs in pnpm verify).** Connected to: pe-dashboard.md, priority-engine.md, validate-open-plan-levels.mjs. | S021 + B_HUMBLE_EXECUTOR + B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 + PE |
| `naming-convention` | per-session | advisory | IMPLEMENTED S021 from S006 turn 24 B_NAMING_POLICY (week-4 deferred). Checks new/modified files for: kebab-case filenames, forbidden suffixes (-v1/-final/-copy), external input files missing EXT- prefix, duplicate filename detection (--duplicates flag). Coverage Levels: ✓ L1 kebab-case + forbidden suffix ✓ L2 EXT- prefix advisory ✓ L3 duplicate detection (--duplicates) ✗ L4 cross-reference integrity after rename → VLT-S021-NAMING-XREF. Stage: advisory (always exits 0). S021 amendment: council context brief files (opus-1-context, sonnet-1-context, haiku-1-context) exempt as working documents. **Registered S006 turn 24, built S021. Build ACTIVE advisory (validate-naming-convention.mjs in pnpm verify).** | B_NAMING_POLICY + P-ARCH-029 + naming-policy.md |
| `research-reuse` | per-session | advisory | NEW S021 Governor directive: Research Registry gate — prevents re-researching topics already studied. Reads tools/config/research-registry.yaml (7 entries, all active, 6 high-confidence). Surfaces stale entries approaching refresh window. Mandate: check registry BEFORE commissioning new research; add findings AFTER. Stage: advisory (always exits 0 — can't block if registry missing). Coverage Levels: ✓ L1 count+staleness ✓ L2 confidence distribution ✗ L3 cross-ref plans against registry → VLT-S021-RESEARCH-PLAN-XREF ✗ L4 auto-suggest → VLT-S021-RESEARCH-SUGGEST. **Registered S021 atomically. Build ACTIVE advisory (validate-research-reuse.mjs in pnpm verify).** | S021 + research-registry.yaml + Governor-directive |
| `completion-circle` | per-session | measurement | NEW S021 Governor directive: Surfaces implemented platform elements missing the full closed-circle: (1) developer_surface — no developer-facing API/doc/export, (2) user_value — no external user benefit statement. Defines COMPLETE = schema + logic + developer_surface + user_value + wisdom_harvested. Coverage Levels: ✓ L1 scan for developer_surface/user_value fields ✓ L2 flag cdp_status=implemented with missing circles ✗ L3 verify dev surface resolves → VLT-S021-DEV-SURFACE. Baseline S021: 19 assessed, 19 missing both fields — all partially implemented. Stage: measurement. **Registered S021 atomically. Build ACTIVE measurement (validate-completion-circle.mjs in pnpm verify).** | S021 + Governor-directive + platform-excellence-consolidated-S021 |
| `update-backlog` | per-session | measurement→advisory→blocking | NEW S021 Governor directive: Tracks all pending platform improvements across 4 categories (Governor decisions, Opus tasks, Sonnet P1/P2/P3). Reads tools/config/platform-update-backlog.yaml; surfaces pending/blocked/done counts. Stage: measurement (exits 0 always). Coverage Levels: ✓ Level 1 count by status/priority/who ✓ Level 2 surface blocked items + blockers ✗ Level 3 verify done items actually done → VLT-S021-BACKLOG-DONE-VERIFY. Baseline S021: 36 items (24 pending, 12 blocked, 5 governor decisions). Mechanically enforced as part of ALL planning/implementing/validation per Governor directive. **Registered S021 atomically. Build ACTIVE measurement (validate-update-backlog.mjs in pnpm verify).** | S021 + platform-update-backlog.yaml + Governor-directive |
| `drift-registry` | per-session | advisory→blocking | NEW S020 DRIFT-1: Drift registry coverage gate. Reads tools/config/drift-registry.yaml; reports coverage % (active validators / total drift types). Coverage Levels: ✓ Level 1 count by status ✓ Level 2 flag CRITICAL drift types with no VLT ✗ Level 3 verify cited validators exist → VLT-S020-DRIFT-VALIDATOR-EXIST ✗ Level 4 verify validators detect what they claim → VLT-S020-DRIFT-VALIDATOR-QUALITY. Exit: ADVISORY (0) when coverage < 50%; BLOCKING (1) when < 25% AND CRITICAL drift type has no VLT. Baseline S020: 43% (3/7 active), status=ADVISORY, critical_unprotected=0. Target: 71% (5/7) by S025. Positive ZF claim: drift-registry.yaml present + parseable + all CRITICAL types have VLTs or active validators. **Registered S020 DRIFT-1 atomically. Build ACTIVE (validate-drift-registry.mjs in pnpm verify).** | S020 + drift-registry.yaml + sonnet-task-list-S020 |
| `core-seeds-advisory` | per-session | advisory | S044 OPEN-057 fixed: validate-core-seeds.mjs now uses grows-to artifact existence check + DEPRECATED marker detection + reads CURRENT_SESSION from session-state.json. **S045 OPEN-059: planted_by + pmi_gate checks.** **S046 PROTO-041 Step 1: walkForSeeds scans .yaml in addition to .mjs/.sh. Step 4 (OPEN-057): pmi_gate status check — if pmi_gate item is done in unified-plan.yaml, advisory fires: seed should be DEPRECATED. Pre-loads planItemMap at main() start for efficient cross-ref. Seeds: 8 total.** **LIVE S046 PROTO-041.** | S019 + validate-core-seeds.mjs |
| `catch-completeness` | per-session | warn (advisory) | NEW S011: every §10.13b catch in closing-summary has matching EP-NNN file. Advisory — surfaces orphan catches. Per B_CATCH_TO_ENGRAVING + EP-015 (satisfaction point prevention). **Registered S011 atomically. Build ACTIVE advisory (validate-catch-completeness.mjs in pnpm verify).** |
| `council-coverage` | PR + per-session | error | NEW S011 §24+++++++++: all 24 SKILL.md files registered in council-registry.md with domain + PE band + audit pipeline. Catches new skills added without registry update. **Registered S011 §24+++++++++ atomically. Build ACTIVE (validate-council-coverage.mjs in pnpm verify).** |
| `universal-alignment` | pre-commit + per-session | warn | NEW S011 §24+++++++++: universal alignment gate for ANY new artifact — core_spine + DNA + connectivity + impl_status. The pre-alignment pipeline. Per threshold-gate-v2.md STEP 0. **Registered S011 §24+++++++++ atomically. Build ACTIVE (validate-universal-alignment.mjs in pnpm verify).** S022 close: exempt list extended to cover HANDOFF + closing-summary + chat-jump-prompt session lifecycle artifacts (governed by protocols.md §10-§17, not by universal alignment). |
| `import-quarantine` | PR + per-session | warn | NEW S011 §24++++++++++: all non-CSPS-originated packages + .claude/skills/ have CSPS DNA (frontmatter + csps_aligned). The Threshold external import discipline. Per threshold-gate.md + B_IMPORT_QUARANTINE. **Registered S011 §24++++++++++ atomically. Build ACTIVE (validate-import-quarantine.mjs in pnpm verify).** |
| `nothing-stands-alone` | PR | warn (advisory) | NEW S011 §24++++++++++: P-ARCH-001 mechanical implementation — every governed artifact has core_spine + schema_anchor. 43 pre-S006 artifacts need connectivity backfill (S012). **Registered S011 §24++++++++++ atomically. Build ACTIVE advisory (validate-nothing-stands-alone.mjs in pnpm verify).** |
| `moat-coverage` | per-session | error | NEW S011 §24+++++++: all CSPS moat elements have active recurring audit coverage (now 18 elements — M-16 GEP, M-17 CDP, M-18 Question Protocol added S019). **S019 amendment:** 3 new moat elements added: M-16 Gradual Execution Protocol (B_HUMBLE_EXECUTION_PIPELINE), M-17 Core Dynamic Plan (cdp_status lifecycle), M-18 Question Protocol (F+C+G+Q full context). Run: node tools/validators/validate-moat-coverage.mjs. **Registered S011 §24+++++++ atomically. Build ACTIVE (validate-moat-coverage.mjs in pnpm verify).** (validator OR hook). 0 critical gaps = moat is mechanically defended. Per moat-registry.md. **Registered S011 §24+++++++ atomically. Build ACTIVE (validate-moat-coverage.mjs in pnpm verify).** |
| `impl-status` | PR + per-session | warn | NEW S011 §24++++++: impl_status closed-enum state machine for implementation quality. States: swift-implemented→audit-1-complete→sealed-zf↔recurring-audit-pending / architecture-pending. Batch processing: architecture-pending→monthly vault; recurring→weekly hook. Per frontmatter-closed-enums.md §impl_status. **Registered S011 §24++++++ atomically. Build ACTIVE (validate-impl-status.mjs in pnpm verify).** |
| `no-implementation-without-plan` | PR + per-session | warn (→ error at Ring 3 build start) | NEW S011 §24+++++ EP-011: implementation files in libs/apps/packages/ without matching active topic-plan. Exempt: platform infra (principles/ principles-mcp/ schemas/ glossary/). Per AGENTS.md hard NO 77 + B_GRADUAL_BUILD_BY_FOUNDATIONS + P-OP-002 FWWS. **Registered S011 §24+++++ atomically. Build ACTIVE advisory (validate-no-implementation-without-plan.mjs in pnpm verify).** **conceptual_sample_of:** GVRN L2 decision rights — implementation without a ratified plan = Governor authority bypassed. |
| `rzf-evidence` | per-session | warn | NEW S011 §24+++++ EP-008: validates that closing-summary §10.0 cites THIS-SESSION pnpm verify output (cycle names + exit_code 0 + ran_at). Primary: checks verify-last-run.md. Per P-META-006 RZF + B_PRE_CLOSE_VERIFICATION. **Registered S011 §24+++++ atomically. Build ACTIVE (validate-rzf-evidence.mjs in pnpm verify).** **conceptual_sample_of:** VALD L2 evidence specificity — a DONE claim without THIS-SESSION evidence is context-depth-degradation in output form. |
| `slice-freshness` | PR + per-session | error | NEW S011 §24+++++ EP-009: behavioral-contracts.md / audit-runner.md / principles.yaml / ai-behavior-spine.md monolith files must not be newer than their slice dirs (mtime comparison). Prevents edit-monolith-forget-split pattern. Per B_CONSOLIDATION_PASS. **Registered S011 §24+++++ atomically. Build ACTIVE (validate-slice-freshness.mjs in pnpm verify).** |
| `config-inheritance-gaps` | per-session | warn | NEW S014 spine-audit: checks that critical config files in a hierarchy (user→project settings, parent→child tsconfig, base→extended ZModel) have critical fields EXPLICITLY declared at child level, not relying on silent inheritance from parent. Root cause fix for S014 canonical instance where project settings.json lacked defaultMode field causing bypassPermissions to be silently overridden by system default. Per code-pattern config-silent-override. **Registered S014 atomically. Build deferred week-4 (requires config hierarchy mapping).** **conceptual_sample_of:** ARCH L2 config domain — hierarchical config requires explicit declarations; inheritance is additive not override. | P-META-021 + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `session-extraction-completeness` | per-session | warn | NEW S014: checks that session-S<NNN>-extraction.md exists at vault path for the current session; warns if missing with WHY context. Per B_POSITIVE_VALUE_EXTRACTION — insights not extracted are lost at session boundary; extracted patterns compound into platform improvements. **Registered S014 atomically. Build deferred week-4 (requires transcript session-ID parsing).** **conceptual_sample_of:** AI L2 inner-defaults domain — positive ZF is as important as negative; failing to harvest means future sessions re-discover the same patterns. | B_POSITIVE_VALUE_EXTRACTION + P-META-021 |
| `vlt-blocking` | per-session | warn | NEW S014 session-extraction: reads session-state.json blocking_decisions; warns when any have status=PENDING. Enforces the distinction: REGISTRATION (acknowledged) ≠ RESOLUTION (Governor ratified). PENDING VLTs block phase advance per B_CONSENSUS_BEFORE_PROCEEDING. Context: every artifact built while a VLT is PENDING inherits the VLT's uncertainty and compounds downstream. **Registered S014 session-extraction atomically. Build ACTIVE advisory (validate-vlt-blocking.mjs in pnpm verify).** **conceptual_sample_of:** GVRN L2 decision rights — phase advance on PENDING VLTs = Governor authority bypassed. | P-META-021 + B_CONSENSUS_BEFORE_PROCEEDING |
| `triad-coverage` | per-session | warn | NEW S014 CEC P-META-021: for each CONSEQUENTIAL decision in the session (phase advance / DONE claim / VLT resolution / new principle ratification), checks all 3 triad layers were present — (1) L2 spine domain named, (2) applicable P-* principle cited, (3) mechanical enforcer exists. Single-layer or two-layer consequential decisions = governance gap. Per P-META-021 + B_TRIAD_GOVERNANCE. **Registered S014 CEC atomically. Build deferred week-4 (requires session transcript parsing).** **conceptual_sample_of:** GVRN L2 decision rights — consequential decisions without the full triad bypass governance structure. | P-META-021 + B_TRIAD_GOVERNANCE |
| `instruction-context` | per-session | warn | NEW S014 ZF audit: scans B_* contracts, principles.yaml, and production hooks for WHY reasoning (not just WHAT). Missing WHY = instruction that produces instance-patching, not concept navigation. 3 surfaces: behavioral contracts (Canonical/Source blocks), principle industry_lineage, hook @csps-description. Per P-META-020 (context as compass). **Registered S014 ZF audit atomically. Build ACTIVE advisory (validate-instruction-context.mjs in pnpm verify).** **conceptual_sample_of:** AI L2 inner-defaults domain — instructions without WHY produce rule-followers that drift exactly like training defaults. | P-META-020 + B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS |
| `plan-age-alignment` | per-session | warn | NEW S015 STALE PLAN ALIGNMENT GATE: any active topic plan written >1 session ago with no `alignment_verified_session` field = STALE. Protocol: before executing items from a stale plan, review each [ ] item (DONE → mark [x] / STALE → update approach / VALID → keep). Add `alignment_verified_session: S<NNN>` to frontmatter to clear the gate. Also detects `likely_done_items` (unchecked items whose referenced files now exist on disk — candidates for [x]). Surfaced at Threshold (session-open.sh) as `stale plans: N requiring alignment`. Root cause: s006/s007/s008/s011 plans had items completed by later sessions but never marked done — 111 reported open items was ~40-70 genuinely open. Phase 1: WARN advisory. Phase 2 (next session): integrate with plan-coverage-gate to block execution from STALE plans without alignment_verified_session. **Registered S015 atomically. Build ACTIVE (validate-plan-age-alignment.mjs in pnpm verify + session-open Threshold).** | S015 + B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-020 |
| `core_reminder` | per-session | advisory | S047 LIVE: validate-core-reminder.mjs — checks csps_core_reminder field coverage in docs/plan/pillar-0-governance/ + tools/council/. Reports with_reminder vs without_reminder. Stale ref check against principles.yaml + behavioral-contracts/. Advisory — backfill is gradual. **LIVE S047.** | validate-core-reminder.mjs + governed artifacts |
| `activation_coverage` | per-session | advisory | S047 LIVE: validate-activation-coverage.mjs — checks all 63 B_* contracts for at least one activation mechanism (T1 hook / T2 validator / session-open / DNA always_include). Per AP-001 + B_EXISTS_NOT_EQUALS_ACTIVE. contracts_checked=63 activated=40 no_activation=23 at launch. Graduates to BLOCKING after 3 sessions. **LIVE S047.** | validate-activation-coverage.mjs + behavioral-contracts/ |
| `exists-not-equals-active-check` | per-session | advisory | S046 REGISTERED (now fulfilled by activation_coverage): validates B_* contracts have activation mechanisms. Per AP-001. **SUPERSEDED by activation_coverage S047.** |
| `rule_has_enforcement` | per-session | advisory | S041 ADVISORY: validate-rule-has-enforcement.mjs — scans PI items that create behavioral rules for enforcement_trio field + recent HANDOFFs for enforcement delta. Embeds AI deep instruction (DEFAULT-ME-1 override) in output. Week-4 BLOCKING for PI items. **LIVE.** | inner-ai-defaults/mechanical-enforcement-defaults.md |
| `enforcement_trio_assigned` | per-session | advisory | S041 EXTENDED: validate-enforcement-trio-assigned.mjs v2.0 — now covers (1) PI files enforcement_trio field + (2) ALL 62 behavioral contracts T1/T2/T3 declaration scan. Reports advisory_gaps=N. 60 contracts currently unenforced. **LIVE.** | AGENTS.md + behavioral-contracts.md |
| `governor-instructions` | per-session | advisory | S040 ADVISORY: validate-governor-instructions.mjs — scans AI communication files for navigation directives directed at the Governor ("see above", "paste from earlier", etc.). B_ZERO_NAVIGATION_FOR_GOVERNOR constitutional. Week-4 promotes to blocking. **LIVE.** | AGENTS.md §B_ZERO_NAVIGATION_FOR_GOVERNOR |
| `error-registry-coverage` | per-session | advisory | S036 LIVE: validate-error-registry-coverage.mjs — for each inner-ai-defaults override, checks if EP-ERR file exists. 6 patterns registered at S036. **LIVE.** | docs/plan/_handoff/VAULT/error-registry/ |
| `nodefile-compliance` | per-session | advisory | S069 STEP 4 LIVE: validate-nodefile-compliance.mjs — NODEFILE-CONTRACT compliance scan for ~36 in-scope files (CORE L1/L2 + pillar headers + docs/plan top-level). Derives pillar from path; flags mismatch; ranks missing-field-count (most debt first). Hash-cache for performance. ADVISORY S068 → BLOCKING after PVA (§105). wiring_state: active. Files ABSENT (described-only): validate-foreign-element-coverage, validate-refinement-before-ratification, validate-gap-harmonization. **LIVE S069 STEP 4.** | NODEFILE-CONTRACT + SPINE-PILLAR-MAP + CSPS-PLANNING-DISCIPLINE-§14 |
| `wiring-completeness` | per-session | advisory | S036 LIVE (updated S038): validate-wiring-completeness.mjs — WIRED/DEFERRED/ORPHAN classification. wiring_state: active (exists + registered in verify.mjs). S038: 19 WIRED, 34 DEFERRED (all service-account-gated), 0 ORPHAN. Also checks root-level integration files (errors.ts, webhook-idempotency.ts). **LIVE.** | libs/components + libs/integrations |
| `communication-protocol` | per-session | advisory | S036 LIVE (S084 COMM-CORE S5 extended: --extended mode adds WARRANT+ACTION checks, born EXTENDED, 0 new STANDARD cycles) (updated S046 Rule 13): validate-communication-protocol.mjs — Rules 1+2+13: Sonnet→Opus messages start "Opus, this is Sonnet." + Opus→Sonnet directives use Rule 2 + ALL cross-boundary sections in sonnet-turn.md checked (not just Sonnet Report). Catches SROF entries and step reports missing handshake. **LIVE S046.** | communication-protocol-shared.md |
| `zca-compliance` | per-session | advisory | S036 registered: P-UX-002 Zero-Context Assumption — checks cross-boundary communications (chat-jump files, SROFs, HANDOFF Zone A) for WHO/WHAT/HOW/NOW structure. Advisory: validate-boundary-alignment.mjs covers Type A/B/C/D/E boundaries. ZCA-specific full validator deferred (build when chat-jump generation is automated). **Registered S036 atomically. Covered by validate-boundary-alignment.mjs Level 1.** | P-UX-002 + B_ZCA + boundary-assumptions.md |
| `implementation-gate` | per-session | advisory | S037 LIVE (PI-003): validate-implementation-gate.mjs — reads git log -1 --name-only; if last commit touches libs/ or apps/ AND has no PI-NNN reference AND does not start with fix:/chore:/docs: → ADVISORY "No PI reference". Transition period: advisory only. Future BLOCKING after PI backfill. **LIVE S037.** | PI-003 + P-ARCH-031 + plan-items/ |
| `agent-calls` | per-session | advisory | S044 PROTO-035 Step 2 LIVE: validate-agent-calls.mjs — T2 for INV-004. Scans sonnet-turn.md for Agent() invocations, checks for UNDERSTANDING BLOCK nearby. ADVISORY (exits 0). Completes INV-004 (T1=BLOCKING + T2=ADVISORY). **LIVE S044.** | validate-agent-calls.mjs + sonnet-turn.md |
| `invariant-coverage` | per-session | advisory | S044 PROTO-034 Step 3 LIVE: validate-invariant-coverage.mjs — reads invariant-registry.yaml; for each invariant checks T1 hook file + T2 validator file exist. Reports complete/partial/minimal per invariant. ADVISORY. **LIVE S044.** | validate-invariant-coverage.mjs + invariant-registry.yaml |
| `plan-readiness` | per-session | blocking-implementing/advisory-ratified | PI-037 LIVE: validate-plan-readiness.mjs — reads unified-plan.yaml, scores PMI per item. BLOCKING exit 1 if any implementing item has PMI < 4/5 HIGH. ADVISORY for ratified items below threshold. Gate: items move to implementing only when PMI >= 4/5. **S045 OPEN-059: planted_by + pmi_gate comment fields added to seed annotation (logic unchanged).** **LIVE S043/S045.** | validate-plan-readiness.mjs + unified-plan.yaml |
| `page-dna` | per-session | advisory | S043-E LIVE: validate-page-dna.mjs — scans csps-playground platform/ HTML pages for window.CURRENT_PAGE.dna field. **S054 UPDATE: also checks apps/csps-playground/src/app/platform/**/*.tsx for const pageDNA (guard: Next.js App Router prohibits export const in page files). tsx_dna_missing=0 on 16 pages confirmed.** ADVISORY. **LIVE S043, Updated S054.** | validate-page-dna.mjs + csps-playground/ |
| `unified-plan-sync` | per-session | advisory | S043-F LIVE: validate-unified-plan-sync.mjs — checks tools/data/plan-api.json is fresher than unified-plan.yaml. ADVISORY if stale — run pnpm plan:export. Prevents Planning Hub serving outdated data. **LIVE S043.** | validate-unified-plan-sync.mjs + unified-plan.yaml |
| `page_schema_consistency` | per-session | advisory | S047 LIVE: validate-page-schema-consistency.mjs — checks platform page dirs are registered in BOTH HTML index AND page-data.js PAGES. Root cause of menu/breadcrumb misalignment: AP-002 class (creation without schema registration). Advisory — missing_from_pages=N. **LIVE S047.** | validate-page-schema-consistency.mjs + page-data.js + platform/ |
| `playground_links` | per-session | blocking | S047 VALIDATE-PLAYGROUND-LINKS LIVE: validate-playground-links.mjs — scans apps/csps-playground/platform/ for subdirectories with index.html. BLOCKING if any page not linked in parent index.html. T1 advisory hook: pre-tool-use-playground-link-reminder.sh. **LIVE S047.** | validate-playground-links.mjs + apps/csps-playground/platform/index.html |
| `pi-questions-answered` | per-session | advisory | S037-B LIVE (OPEN-007): validate-pi-questions-answered.mjs — for each PI file with status: implementing, checks questions: array for unanswered entries → ADVISORY. Prevents starting implementation with unresolved pre-conditions. P-OPER-002 + P-ARCH-031. **LIVE S037-B.** | validate-pi-questions-answered.mjs + plan-items/ |
| `persona-chain-complete` | per-session | advisory | S037-C LIVE (OPEN-008): validate-persona-chain-complete.mjs — for each PI file with status: implementing, checks persona_chain_log: field for all 6 reviews (consolidation/balance/domain/ux/critic/synergy) with status: complete → ADVISORY if missing. **LIVE S037-C.** | validate-persona-chain-complete.mjs + plan-items/ |
| `ui-completeness` | per-commit | advisory | S039 LIVE v1.1: validate-ui-completeness.mjs — scans new app page .tsx files from git diff + apps/csps-playground/src/app/platform/ directly (submodule bypass). **S054 UPDATE: fixed to scan csps-playground/src/app/platform/ directly since submodule files don't appear in parent repo git diff. 16 files now checked.** ADVISORY. **LIVE S039, Updated S054.** | validate-ui-completeness.mjs |
| `sync-state-fresh` | per-session | advisory | S037 LIVE: validate-sync-state-fresh.mjs — universal-sync-state.json must be within 24h AND principle/moat/contract counts must match. T2 of 3-direction DNA sync. ADVISORY if stale/drift. **LIVE S037.** | validate-sync-state-fresh.mjs + universal-sync-state.json |
| `new-file-dna` | per-commit | blocking-libs/advisory-apps | S037 LIVE v1.0: validate-new-file-dna.mjs — new TS/JS files in last commit must carry CSPS DNA (@csps-id/@csps-enforces/graceful-passthrough/PI-coverage). BLOCKING for libs/ > 50 lines. Closes DNA inheritance gap. M-26 moat element. **LIVE S037.** | validate-new-file-dna.mjs |
| `creation-completeness` | per-session | advisory | S037-H LIVE (Turn 85 §2 + OPEN-021): validate-creation-completeness.mjs — PI files from last 30 days checked for wiring_checklist ≥3 entries, enforcement_trio, done_criterion, ep_err_pre_check (EP-ERR→Planning loop). ADVISORY transition period. **LIVE S037-H.** | validate-creation-completeness.mjs + plan-items/ |
| `directive-has-rzf` | per-session | blocking-latest/advisory-historical | S042 v2.1 BLOCKING exit 1 (OPEN-050 promotion): validate-directive-has-rzf.mjs — SONNET DIRECTIVE sections: checks RZF present, RZF ordering BEFORE directive, Rule 2 format header, verification tail. BLOCKING exit 1 for most recent Turn AND turns >= Turn 96 (S040+). ADVISORY for historical. **Promoted ADVISORY→BLOCKING S042.** | validate-directive-has-rzf.mjs + opus-turn.md |
| `quality-alignment` | per-session | advisory | S037-H LIVE (OPEN-022): validate-quality-alignment.mjs v1.1 — OPUS-2 RZF rate + Sonnet INTENT ABSORBED rate ≥80%, AND directive_rzf_quality_rate (Cycle 2 ≥10 words per EP-ERR-008). Shared quality discipline. **LIVE S037-H.** | validate-quality-alignment.mjs + opus-turn.md + sonnet-turn.md |
| `handoff-completeness` | per-session | error | S040 BLOCKING. S052: added SONNET STARTUP BLOCK as mandatory 4th section — paste-ready YOU ARE/I AM/SITUATION/FIRST-ACTION prompt for incoming Sonnet tab. B_ZERO_NAVIGATION_FOR_GOVERNOR: new tab starts from ZERO; startup block must be template-produced not AI-composed prose. Grandfather: S037-S052 advisory for startup block (added S052 close). BLOCKING from S053+ if Zone A, Zone B, ALIGNMENT QUESTIONS (≥3), or SONNET STARTUP BLOCK missing. **S086 SEED-D ADVISORY**: 3 new minSession:86 advisory checks — team-routing/Haiku mention, open parks surfaced, WHAT NOT TO DO section. Applied only to HANDOFF-S086+ files. **LIVE S040, UPDATED S052, UPDATED S086.** | validate-handoff-completeness.mjs + HANDOFF-*.md |
| `turn-counter-refresh` | per-turn | advisory | S040 NEW (B_INHERITANCE_POLICY): user-prompt-submit-turn-counter.sh — increments turn counter per UserPromptSubmit. At turn 25, 50, 75: re-injects governance constitution (role + mandate + active B_* contracts + ZF discipline). Prevents in-conversation salience decay without human action. **LIVE S040.** | .claude/hooks/user-prompt-submit-turn-counter.sh + tools/zf-session-tracker.json |
| `enforcement-trio-assigned` | per-session | advisory | S037-F LIVE (Turn 84): validate-enforcement-trio-assigned.mjs — PI files with status: ratified or implementing must have enforcement_trio: field. Constitutional: rules without tier assignment = suggestions. ADVISORY transition period. **LIVE S037-F.** | validate-enforcement-trio-assigned.mjs + Turn 84 |
| `active-protocol` | per-session | advisory/blocking | S036-PROTO LIVE: validate-active-protocol.mjs — ADVISORY if no active_directive, BLOCKING if two protocols active. No Parallel Pipelines rule. **LIVE.** | tools/session-state.json |
| `snapshot-continuity` | on-demand | advisory | S036-PROTO LIVE: validate-snapshot-continuity.mjs — checks module snapshots are still valid (exports exist + still imported). **LIVE.** | docs/plan/_handoff/VAULT/snapshots/ |
| `active-protocol-compliance` | per-session | advisory/blocking | S036-PROTO LIVE: reads tools/session-state.json active_directive — ADVISORY if no protocol registered, BLOCKING if two protocols simultaneously active. Enforces No Parallel Pipelines rule (Opus Turn 66 §3). **LIVE: validate-active-protocol.mjs.** | tools/session-state.json |
| `security-headers` | PR + per-session | error | S032-D BLOCKING alias: validate-security-headers.mjs scans all apps/*/next.config.js. See security-headers-compliance row. **LIVE S032-D.** | M-24 |
| `security-headers-compliance` | PR + per-session | error | S032-D BLOCKING: every app/*/next.config.js must have CSPS security headers configured — either (a) import securityHeaders from @csps/integrations/security/headers and spread in headers(), or (b) inline Content-Security-Policy + X-Frame-Options headers directly (when CJS/ESM incompatibility prevents import). BLOCKING if any deployment app missing CSP/HSTS/X-Frame-Options. **LIVE S032-D: validate-security-headers.mjs. Updated S040-PROTO-018: inline pattern also accepted.** | libs/integrations/security/headers.ts + M-24 |
| `scope-level-declared` | per-session | advisory | ADR-0027 Phase 1 S032: checks governed .md files in docs/plan/pillar-0-governance/ and .claude/core-spines/ for scope_level: S[0-5] in frontmatter. 206 missing at S032 — advisory until Phase 2 backfill script runs. Phase 2 (S033): build tools/scripts/backfill-scope-level.mjs. Phase 3 (S034): BLOCKING. **LIVE: validate-scope-level-declared.mjs.** | ADR-0027 + Opus Turn 21 |
| `file-naming` | per-session | advisory | E3 S031: 5 naming rules (R1 validators=validate-noun-verb.mjs, R2 VAULT opus-*.md, R3 topic-plans, R4 core-spines, R5 principle slices). Advisory only — 88 existing violations grandfathered in tools/config/naming-exempt.yaml (file-name keyed, not pattern). New files MUST comply. **LIVE: validate-file-naming.mjs.** |
| `opus-chat-jump-freshness` | per-session | advisory | E4 S031: counts # Opus Turn headers in opus-turn.md. If ≥20 turns AND no current-session chat-jump → ADVISORY. **LIVE: validate-opus-chat-jump-freshness.mjs.** |
| `platform-capacity` | per-session | advisory | E0 S030 + **S073 B0.5 fix**: Reads tools/config/platform-capacity-registry.yaml, measures each element. **pnpm-verify-cycles counter updated S073**: now counts only CRITICAL+STANDARD validators (excludes skip:true AND run_tier:DEEP). Result: 191 active cycles (well below hard_limit 200). Validator tiering (CRITICAL/STANDARD/DEEP) implemented in verify.mjs — DEEP validators run via --deep flag or zf-orchestrator; no hard_limit raise. **S076 EXTENDED tier added**: run_tier:'EXTENDED' = weekly-cron only (not in default or --deep verify). 5 stable dim-3/dim-4 structural invariants moved to EXTENDED; Phase 2+S5 promoted from DEFERRED to EXTENDED. Counter updated: excludes EXTENDED same as DEEP. Active cycles=199/200. 4 advisories current: agents-md-lines, deferred-audit-slugs, vault-root-files, verify-cycles(soft). **LIVE S030, Updated S073 B0.5, S076 EXTENDED.** | tools/config/platform-capacity-registry.yaml + P-ARCH-030 |
| `platform-first-coverage` | per-session | warn | NEW S015 B_PLATFORM_FIRST_OPTIMIZATION: checks if session produced non-trivial governed artifacts (new contracts / validators / templates / API routes) WITHOUT a documented CEC walk or generalization note. Every artifact should answer: "was platform generalizability evaluated?" Root cause fix: local implementations that could generalize don't, because no surface forces the evaluation. Phase 1 advisory: warns when CEC evidence is absent from a session that produced implementation artifacts. **Registered S015 atomically (impl deferred week-4: validate-platform-first-coverage.mjs).** | S015 + B_PLATFORM_FIRST_OPTIMIZATION + P-META-006 |
| `verbatim-compliance` | per-session | warn | NEW S016 B_VERBATIM_HUMAN_TEXT: when user provides exact text and AI response differs significantly → WARN. impl deferred week-4: validate-verbatim-compliance.mjs. **Registered S016 atomically.** | S016 + B_VERBATIM_HUMAN_TEXT |
| `context-sensitive-rule-coverage` | PR | warn | NEW S016 rigid-rule anti-pattern: every AGENTS.md hard NO must have three parts: WHY concept (not just WHAT to prevent), precise SCOPE (initiation pattern, not activity class), and ESCAPE HATCH (Governor-override condition). Absence of any part = the rule is a blanket ban that breaks at edge cases. Root cause: D10 over-generalization — AI writes "never X" when it means "never proactively initiate X without Governor direction." Phase 1: WARN. Phase 2 (week-4): ERROR when hard NO lacks escape_hatch field. **Registered S016 atomically (impl deferred week-4: validate-context-sensitive-rules.mjs).** | S016 + P-META-020 + rigid-rule-anti-pattern |
| `milestone-assessment-coverage` | per-session | warn | NEW S016 B_HUMBLE_EXECUTOR: checks that each completed phase in an active topic plan has milestone extraction evidence — at minimum one vault entry (raw-thoughts-queue or extraction note) created after phase completion. Absence = phase transitioned silently without extract→validate→PE cycle. Root cause fix: 1M context window allows multiple phases per session; without milestone enforcement, discoveries are lost between phases. **Registered S016 atomically (impl deferred week-4: validate-milestone-assessment.mjs).** | S016 + B_HUMBLE_EXECUTOR + P-META-006 |
| `preflight-coverage` | per-session | warn | NEW S016 B_AUTONOMOUS_BATCH_WITH_PREFLIGHT: for each implementation batch in the session (≥4 files written in a single batch), checks that a pre-flight block was present (scope declaration + Q-GATE + Q-COMPLETE + Q-GLOBAL). Absence = batch ran without decision extraction. Phase 1: WARN advisory. Phase 2 (week-4): BLOCKING when pre-flight absent for batches ≥10 files. **Registered S016 atomically (impl deferred week-4: validate-preflight-coverage.mjs).** | S016 + B_AUTONOMOUS_BATCH_WITH_PREFLIGHT + P-OP-003 |
| `bedrock` | per-session | warn→blocking | Alias slug for `bedrock-completion` — validate-bedrock.mjs. Reads csps-bedrock.md §3 checklist; reports 2 root decisions missing (ZenStack + plan-methodology-v2 L2); exits 0 when all items done or explicitly deferred with VLT. See full entry under `bedrock-completion`. **Registered S016. Build ACTIVE.** | S016 + csps-bedrock.md |
| `bedrock-completion` | per-session | warn→blocking | NEW S016 Governor directive: reads csps-bedrock.md §3 completion checklist. Reports completion% (currently 67% done + 100% tracked). Exits 0 when all items done or explicitly deferred with VLT tracking. Exits 1 (BLOCKING) when mandatory items remain without tracking. 2 root decisions missing: ZenStack + Plan Methodology v2 L2 — each blocking 3-4 downstream items. Purpose: surface bedrock gap before app #2 is started; prevent building on incomplete foundation. **Registered S016 atomically. Build ACTIVE (validate-bedrock.mjs in pnpm verify).** | S016 + B_PLATFORM_FIRST_OPTIMIZATION + csps-bedrock.md |
| `foundation-slices-schema-drift` | per-session | **blocking** | S017 ZenStack mandate + S019 field-level extension (L1 opus-lessons): runs `zenstack generate` on `libs/policies/schema.zmodel` + compares generated schema against `apps/task-mgmt/prisma/schema.prisma` at TWO levels: (1) model-level — models missing from app schema are BLOCKING; (2) field-level — fields in generated ZModel but missing from app schema are BLOCKING; fields in app schema but missing from ZModel are ADVISORY. Coverage levels: ✓ L1 model-names ✓ L2 field-names ✗ L3 live-DB→VLT-S019-LIVEDB ✗ L4 type-annotations→VLT-S019-ANNOTATIONS. Root cause: silent field drift (confirmed: Tenant.stripeSubscriptionId was in app schema but not ZModel — now fixed). **Registered S017; field-level extended S019. Build ACTIVE (validate-foundation-schema-drift.mjs in pnpm verify, cycle 41).** **conceptual_sample_of:** ARCH L2 data domain — ZModel→Prisma consistency IS the platform's schema security promise; drift = the promise is broken. | S017 + S019 + B_PLATFORM_FIRST_OPTIMIZATION + VLT-S016-ZENSTACK + opus-lessons-S019/part1 |
| `plan-harvest-coverage` | PR | warn | NEW S015 plan-methodology-v2 L3: every active gradual-build-plan has `§HARVEST` section with `harvest_triggers` and `harvest_questions` populated. Plans without §HARVEST = incomplete methodology adoption. Root cause fix: harvesting was a session-close ceremony; this validator makes it structural per-plan. Advisory window: plans written before S015 are grandfathered. **Registered S015 atomically. Build ACTIVE S016 (validate-plan-harvest-coverage.mjs wired in pnpm verify; 7 plans checked, 0 warnings after §HARVEST + execution_mode retrofit).** | S015 + B_GRADUAL_BUILD_BY_FOUNDATIONS + plan-methodology-v2 |
| `execution-mode-declared` | PR | warn | NEW S015 plan-methodology-v2 L3: every gradual-build-plan has `execution_mode: velocity \| balanced \| deep_quality` declared in frontmatter. Missing = plan will be executed in AI default mode (potentially wrong for schema-lock or schema-changing work). Advisory window: pre-S015 plans grandfathered via alignment_verified_session. **Registered S015 atomically. Build ACTIVE S016 (validate-execution-mode-declared.mjs wired in pnpm verify; 6 plans retrofitted with execution_mode this session).** | S015 + plan-methodology-v2 |
| `b-prace-enforcement` | per-session | advisory | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_PRACE T2 — validates B_PRACE mechanical enforcement surfaces present (no T3-only contracts). Reports T3-only count + advisory for each. **BUILT S061 (validate-prace-tiers.mjs in pnpm verify). 37 T3-only contracts surfaced as advisory backlog for S062+.** | B_PRACE |
| `b-no-ai-impersonation-enforcement` | per-session | advisory→blocking | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_NO_AI_IMPERSONATION T2 — scans council/*.md (top 100 lines) for impersonation phrases ("I am Opus", "as Opus 4", etc.). BLOCKING: recent content. ADVISORY: historical. File-level @ai-honesty-exempt + table row skipping prevents false positives. **BUILT S061 (validate-ai-honesty.mjs in pnpm verify; blocking=0 advisory=0 after false-positive fix).** | B_NO_AI_IMPERSONATION |
| `b-development-vs-production-enforcement` | per-session | advisory→blocking | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_DEVELOPMENT_VS_PRODUCTION T2 — scans apps/*/src/app/api/ for hardcoded Opus model strings in production routes. BLOCKING: real prod route with opus. ADVISORY: playground (dev-tier) with opus (expected). **BUILT S061 (validate-dev-vs-prod.mjs in pnpm verify; blocking=0 advisory=3 in csps-playground/consult route — correct).** | B_DEVELOPMENT_VS_PRODUCTION |
| `b-apps-are-trials-enforcement` | per-session | advisory | NEW S052 orphan-contract T2: B_APPS_ARE_TRIALS has no audit slug. Validates apps/ vs libs/ discipline. **BUILT S054 (validate-apps-are-trials.mjs in pnpm verify).** | B_APPS_ARE_TRIALS |
| `b-done-right-from-start-enforcement` | per-session | advisory | NEW S052 orphan-contract T2: B_DONE_RIGHT_FROM_THE_START. **BUILT S055 (validate-done-right.mjs in pnpm verify).** | B_DONE_RIGHT_FROM_THE_START |
| `b-definition-before-enforcement-check` | per-session | advisory | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_DEFINITION_BEFORE_ENFORCEMENT T2 — scans packages/principles/principles.yaml. ADVISORY: principle has enforcement_tier but no governing_intent/satisfaction_point field (62 as of S061 — S062 backlog). Signals enforcement without definition. **BUILT S061 (validate-definition-before-enforcement.mjs in pnpm verify).** | B_DEFINITION_BEFORE_ENFORCEMENT |
| `b-autonomy-4-conditions-enforcement` | per-session | advisory | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_AUTONOMY_4_CONDITIONS T2 — scans council/*.md (top 100 lines) for autonomous execution triggers without all 4 conditions (ratified/reversible/mechanical/no-cross-actor) cited nearby. ADVISORY only. **BUILT S061 (validate-autonomy-conditions.mjs in pnpm verify).** | B_AUTONOMY_4_CONDITIONS |
| `b-checkpoint-8-categories-enforcement` | per-session | advisory | NEW S052 gap_T2_ORPHAN_CONTRACTS: B_CHECKPOINT_8_CATEGORIES T2 — scans council/*.md (top 100 lines) for checkpoint category language (scope expansion, constitutional change, etc.) without PCR acknowledgment nearby. ADVISORY only. **BUILT S061 (validate-checkpoint-categories.mjs in pnpm verify).** | B_CHECKPOINT_8_CATEGORIES |
| `b-contextual-locality-enforcement` | per-session | advisory→blocking | NEW S052 orphan-contract T2: B_CONTEXTUAL_LOCALITY. Validates content-at-point-of-use discipline. **BUILT S055 (validate-contextual-locality.mjs in pnpm verify).** | B_CONTEXTUAL_LOCALITY |
| `context-question-coverage` | per-session | advisory | NEW S052 documentation-in-schema T2: scans all governed .md files in docs/ and tools/ for context_question frontmatter field. Reports coverage (currently 12%: 52/428 files). **S053 UPDATE: also checks context_quote field (verbatim Governor words). Both advisory.** Gradual adoption — advisory only. Every governed artifact should answer: "Before using this, what must be verified about [key assumption]?" **Registered S052 atomically. Updated S053. Build ACTIVE (validate-context-question-coverage.mjs in pnpm verify; advisory exit 0).** | documentation-in-schema + B_VALIDATE_BEFORE_ASSUME |
| `threshold-intake` | per-session | advisory | NEW S053 THRESHOLD R1.4.1 T2: reads tools/data/threshold-intake-log.yaml (written by user-prompt-submit-intake.sh T1 hook classification). Reports total entries, type distribution (governor_directive/correction/error/solution/question/core_seed/session_harvest/external_research), and unique session count. Advisory always — grows as governor prompts accumulate. **Registered S053 atomically. Build ACTIVE (validate-threshold-intake.mjs in pnpm verify; advisory exit 0).** | THRESHOLD-CODE + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `platform-genome-freshness` | quarterly | advisory | NEW S054: validates PLATFORM-GENOME.md exists + has 10 sections + each section has at least one link. **S054 FIX: §8 (Settings Invariants) and §9 (Creation Requirements) are text-only prose — correctly exempt from link requirement. advisory=0 when all 10 sections present with §8+§9 exempted.** BLOCKING if file missing. ADVISORY if section empty or missing. Ensures the permanent index stays current as new platform nodes are added. **Registered S054. Build ACTIVE (validate-platform-genome.mjs in pnpm verify).** | PLATFORM-GENOME + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `improvement-register-cec` | weekly | advisory | NEW S054: reads improvement-register.yaml. ADVISORY: K>=2 AND not_yet_propagated non-empty (CEC required). BLOCKING: K>=3 AND status=open AND not_yet_propagated non-empty. Parallel to gap-recurrence validator but for POSITIVE findings. Ensures improvements compound across sessions. **Registered S054. Build ACTIVE (validate-improvement-register.mjs in pnpm verify).** | improvement-register + P-META-006 CEC |
| `ai-conception-enforcement` | per-session | advisory | NEW S055 structural fix for gap_T1_AI_CONCEPTION_VAULT (K=5): scans tools/vault/ai-conception/ for enforcement_tier field. Advisory baseline: 13/13 missing (0% rate). Tracks improvement toward 100% coverage. Behavioral test: tools/tests/behavioral/ai-conception-enforcement-test.sh (2/2 pass). **Registered S055 atomically. Build ACTIVE (validate-ai-conception-enforcement.mjs in pnpm verify).** | gap_T1_AI_CONCEPTION_VAULT + AP-001 |
| `contextual-locality` | per-session | advisory→blocking | NEW S055 T2 for B_CONTEXTUAL_LOCALITY (gap_T2_ORPHAN_CONTRACTS): checks council files top 50 lines for navigation phrases ("see above", "refer to", "from earlier"). BLOCKING for recent content, ADVISORY for historical. Behavioral test 2/2. **Registered S055 atomically. Build ACTIVE (validate-contextual-locality.mjs in pnpm verify).** | B_CONTEXTUAL_LOCALITY + P-UX-001 |
| `done-right` | per-session | advisory→blocking | NEW S055 T2 for B_DONE_RIGHT_FROM_THE_START (gap_T2_ORPHAN_CONTRACTS): checks B_*.md in behavioral-contracts/ for enforcement_tier declaration. BLOCKING for new S055+ contracts, ADVISORY for pre-S055 backfill debt. Behavioral test 2/2. **Registered S055 atomically. Build ACTIVE (validate-done-right.mjs in pnpm verify).** | B_DONE_RIGHT_FROM_THE_START + B_ENFORCEMENT_TRIO |
| `positive-reflexivity` | per-session | **blocking** | NEW S055 POSITIVE-REFLEXIVITY T2: reads improvement-register.yaml + pending-plan-items.yaml + unified-plan.yaml. BLOCKING: K>=2 entry has no draft in pending-plan-items.yaml AND no matching plan item. ADVISORY: draft exists but not promoted. Generator: tools/generators/generate-plan-item-draft.mjs (writes draft to pending-plan-items.yaml). Opus reviews drafts → promotes to unified-plan.yaml. **Registered S055 atomically. Build ACTIVE (validate-positive-reflexivity.mjs in pnpm verify).** | improvement-register + pending-plan-items + POSITIVE-REFLEXIVITY plan item |
| `validate-validators` | per-session | advisory | NEW S055 VALIDATE-VALIDATORS meta-validator (Methodology 5): reads tools/verify-last-run.md. ADVISORY: (a) DEFERRED validators never run last session, (b) validators with empty parse_output (no metrics captured), (c) validators with all-zero numeric output. "Existence ≠ active" (AP-001) applied to validators themselves. **Registered S055 atomically. Build ACTIVE (validate-validators.mjs in pnpm verify).** | AP-001 + B_STRUCTURAL_PREVENTION_DISCIPLINE + verify-last-run.md |
| `flow-activity-monitor` | per-session | **blocking** | NEW S063 FLOW-ACTIVITY-MONITOR: three-layer check (REGISTERED + EXECUTED + PRODUCED). Reads tools/data/flow-activity-monitor.yaml. For each flow element with status=active, verifies expected_output signature present at expected output_location. BLOCKING: active flow with no output AND no exempt_reason. ADVISORY: not-yet-built flows surface for implementation queue. Extends validate-validators with OUTPUT-existence check (not just runtime). RZF Cycle 1 surfaced 4 BLOCKINGs (signature format mismatches — corrected) and 1 not-yet-built (ai_output_exceptional_capture confirming FINDING-S063). RZF Cycle 2: 0 BLOCKING, 6 active flows verified. **Registered S063 atomically. Build ACTIVE (validate-flow-activity-monitor.mjs in pnpm verify).** | FINDING-S063-AI-OUTPUT-CAPTURE-ASYMMETRY + AP-001 + audit-scheduling |
| `session-authority` | per-session | advisory | NEW S055 SESSION-AUTHORITY: compares current_session in session-state.json against latest session marker in sonnet-turn.md. ADVISORY: gap > 2 sessions (stale authority signal). ADVISORY: session_spread ≥ 6 (possible three-drivers problem). Prevents multiple sessions producing conflicting outputs without authority signal. **Registered S055 atomically. Build ACTIVE (validate-session-authority.mjs in pnpm verify).** | session-state.json + AP-001 + three-drivers-prevention |
| `settings-shadow` | per-session | **blocking** | NEW S057 SETTINGS-SHADOW: detects .claude/settings.local.json shadowing project settings.json permissions. BLOCKING if permissions key present WITHOUT defaultMode:bypassPermissions. PASS if explicit bypassPermissions set (canonical form). **S058 UPDATE: validator updated to allow explicit bypassPermissions in settings.local.json (session-open.sh writes canonical form on every session start).** session-open.sh T1 writes explicit canonical form. **Registered S057 atomically. Build ACTIVE (validate-settings-shadow.mjs in pnpm verify).** | CONFIG-HIERARCHY-WARNING + B_ZERO_LAPTOP_DEPENDENCY + permission-bypass-permanent |
| `pie-readiness-gate` | per-session | advisory→blocking | NEW S056 B_PIE_READINESS_GATE: validates no item has status=implementing when its architectural layer prerequisites are incomplete (per CORE-COMPLETE-EXIT-CRITERIA.md). R2 requires Layer 1 4/4, R3 requires Layer 2, R4 requires Layer 3, App items require all 4. ADVISORY: R2 with Layer 1 incomplete. BLOCKING: R3+ with prerequisite layer incomplete. Behavioral test: pie-readiness-gate-test.sh (2/2 pass). **Registered S056. Build ACTIVE (validate-pie-readiness-gate.mjs in pnpm verify, S056 PROTO-D).** | B_PIE + CORE-COMPLETE-EXIT-CRITERIA.md + B_PIE_READINESS_GATE |
| `learning-loop-extraction` | per-session | advisory | NEW S056 LEARNING-LOOP-EXTRACTION: validates that libs/intelligence/ Learning Loop ran at session close and produced output (session-{N}.yaml in .csps/learning-loop/). ADVISORY if missing. Closes the gap where session insights evaporate without extraction. **Registered S056. Build ACTIVE when libs/intelligence/ Phase 1 complete (this session).** | libs/intelligence + Learning Loop sub-engine + B_PIE |
| `ideal-build-freshness` | every-3-sessions | advisory | NEW S054: THE-IDEAL-BUILD.md is the target state reference. Every 3 sessions, check if it's still current: does it reflect what was built? This is the online correction loop for the target state itself. ADVISORY only. **Registered S054. Build PENDING (validate-ideal-build-freshness.mjs — file exists, validator deferred).** | THE-IDEAL-BUILD + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `self-validation-coverage` | quarterly | advisory | NEW S054: SELF-VALIDATION-METHODOLOGIES.md maps 5 research patterns to CSPS. Quarterly check: have the Implementation Roadmap S054/S055 targets been addressed? Advisory — surfaces stale roadmap items. **Registered S054. Build PENDING (validate-self-validation.mjs — reference doc review).** | SELF-VALIDATION-METHODOLOGIES + PLATFORM-GENOME |
| `consolidation_pass` | per-session | advisory | PROTO-S064 Item 1.2 — B_CONSOLIDATION_PASS T2 (new: filename duplication detection). Scans staged/new files for fuzzy-name similarity (60% Jaccard threshold) against existing files in key directories. Advisory — prompts /consolidation-expert review before creating similar-named artifacts. Different from validate-consolidation-check.mjs (which checks topic-plan §0 sections). **Registered S064 PROTO-S064. Build ACTIVE (validate-consolidation-pass.mjs in pnpm verify).** | B_CONSOLIDATION_PASS + P-OP-001 |
| `apps-are-trials` | per-session | **blocking** | NEW S054 B_APPS_ARE_TRIALS T2 (constitutional backfitting, gap_T2_ORPHAN_CONTRACTS). BLOCKING: apps/* contains nested package.json naming a @csps/* libs package (reimplementation). ADVISORY: platform-procedure files in apps/, relative imports bypassing @csps/ API. Deletion test: apps/{app} deleted = zero platform value lost. **Registered S054 atomically. Build ACTIVE (validate-apps-are-trials.mjs in pnpm verify).** | B_APPS_ARE_TRIALS + P-ARCH-030 |
| `app-deploy-readiness` | per-session | advisory | PROTO-S062-DEPLOY STEP 1 + **S073 FLAWLESS-DEPLOY M1 update**: Component B deploy-readiness gate. Checks each app in apps/ (skipping csps-playground + _trials-vaulted) for: CHECK 1 .env.example (devs know required vars), CHECK 2 .csps/deploy-checklist.md (Vercel UI steps), CHECK 3 package.json build script, CHECK 4 no .env.local committed (secret hygiene — BLOCKING), **CHECK 5 root_dir-exists from tools/config/deploy-targets.yaml (BLOCKING — kills the budget-planner failure class: registered app with missing root_dir = Vercel build from wrong dir)**. S073 added: SKIP _trials-vaulted + CHECK 5 + deploy-targets.yaml registry + deploy-check.mjs pre-deploy gate + generate-deploy-config.mjs generator + vercel.md FLAWLESS-DEPLOY section. behavioral test: deploy-readiness-check5-test.sh 3/3. **Registered S062-C5 PROTO-S062-DEPLOY, Updated S073. Build ACTIVE.** | P-ARCH-030 + B_APPS_ARE_TRIALS + vercel.md |
| `communication-quality` | per-session | advisory | NEW S053: checks communication templates for Governor impersonation (BLOCKING per SAMPLE 001) + FROM/TO format adoption (ADVISORY). **S054 UPDATE v1.1: now extracts bad-version patterns from communication-samples.md and pattern-matches against templates. Actual sample usage, not just loaded=true flag.** Currently exit 0 advisory phase. **Registered S053, Updated S054. Build ACTIVE.** | communication-protocol-shared + B_ZERO_NAVIGATION_FOR_GOVERNOR |
| `context-wrapped-numbers` | per-session | advisory | NEW S071 M1 Facet A: P-META-028 cornerstone enforcement. Scans docs/plan/ md+yaml for bare integers lacking context markers (sample/tunable/expandable) within ±3 lines, excluding patterns in `context-wrapped-numbers-allowlist.yaml` (version strings, SHAs, principle IDs, session refs, exit codes, etc.). **ADVISORY always — rigidity-validator cannot itself be rigid (per P-META-028).** files_checked=637 advisory=8422 (high initial count — allowlist will grow). Test 3/3: A bare int no context → flag · B int with (sample) → pass · C allowlisted pattern → pass. **Registered S071 M1. Build ACTIVE.** | P-META-028 + context-wrapped-numbers-allowlist.yaml |
| `threshold-exhaustive` | per-session | **blocking** | NEW S071 M7 PART 2 STEP 2: Exhaustive 10-class classification. Tests all 10 input classes from PROTO-S068-PART-2 §CLASSIFICATION DESIGN against routeInput() + validates zero default-to-unhandled paths. **BLOCKING** if any class silently routes to PLACE-NOT-FOUND when it shouldn't (SILENT-DEFAULT-TO-UNHANDLED). Test 3/3: A matched-class → correct route · B unmatched → PLACE-NOT-FOUND explicit + notify (not silent) · C foreign-element → FOREIGN-ELEMENT:quarantine. 11/11 test cases pass. Classes: governor_directive/implementation/ai_behavior/validation/proposal/external_research/maintenance/conversational/foreign_element/place_not_found (current enumeration — expandable per P-META-028). **Registered S071 M7. Build ACTIVE.** | PROTO-S068-PART-2-THRESHOLD-COMPLETE.md + threshold-router.mjs + notify-place-not-found.mjs |
| `cie-pe-audit` | per-session | advisory | NEW S071 M5 Facet F: CIE+PE trigger audit. Runs `tools/scripts/cie-pe-trigger-audit.mjs`. Checks (sample — expandable): (1) OBSERVE: audience_tier present on ai-behavior-signals.jsonl events (mandate M5); (2) CIE: milestone reports contain verify_top_exit+PREVENTION+§15 evidence; (3) PE: improvement-register entries have pe_score at plan-forks. OBSERVE+AGGREGATE stages only; ADJUST/INJECT/MEASURE deferred S072 Q2. **ADVISORY always.** observe=2 cie_milestones=0 pe_advisories=1 (25 improvement entries lack pe_score — genuine finding). **Registered S071 M5. Build ACTIVE.** | AI-PROFILING-TO-COMMUNICATION-FEEDBACK.md + ai-behavior-signals.jsonl + improvement-register.yaml |
| `vocabulary-coverage` | per-session | advisory | NEW S071 M2 Facet C: Dev↔User vocabulary coverage. Parses `vocabulary.md §Dev↔User Glossary` (≥8 sample-marked entries) and scans user-facing docs (end-user/account-owner-admin/team-leader tier mentions) for dev_terms lacking paired user_term translations. **ADVISORY in S071.** glossary_entries=8 (sample — expandable per P-META-028). findings=29 pre-existing (all advisory). Test 3/3: A dev_term in user-facing content without user_term → flag · B user_term present → pass · C core-developer tier file → pass (not user-facing). **Registered S071 M2. Build ACTIVE.** | vocabulary.md §Dev↔User Glossary + P-META-028 + communication-schema.yaml §audience_hierarchy |
| `nominal-rzf-detector` | per-session | advisory | NEW S071 M1 Facet A sibling: RZF-LATEST §6.I5 anti-nominal-rzf. Scans sonnet-turn.md + opus-turn.md for Cycle-2+ "0 new" claims lacking ≥1 filename-with-extension in ±1 line. S075 B2 EXTENDED: ZF ACHIEVED without tool output → advisory (SP floor). S075 HARDWIRE-008 EXTENDED: ACCEPT/SEAL/GO/OPIA verdict blocks without this-turn tool re-run citation → advisory D14/D15. **S076 Phase B DE-ROLED: "director verdict" → "any executor state-claim" — executor-agnostic floor.** Exempt: Milestone Report headers, ZF ACHIEVED terminal, CROSS-REVIEW ATTESTATION, ASK-OPUS-STOP. ADVISORY + promotion-path. findings=23 pre-existing corpus. **Registered S071 M1, extended S075, de-roled S076. Build ACTIVE.** | RZF-LATEST §6.I5 + P-META-006 + P-META-028 + HARDWIRE-008 |
| `layer-split` | per-session | **blocking** | NEW S076 Phase A: Agent-Decoupling layer classification. Checks layer:system\|scaffold on all D1-D19 defaults in default-correction-registry.yaml + all hardwire-register.yaml rows + relay files (opus-turn.md, sonnet-turn.md) have scaffold classification comment. BLOCKING if any artifact missing layer: field. ADVISORY if scaffold artifact referenced load-bearingly from system validator. Foundation dim 3. **Registered S076 Phase A. Build ACTIVE.** | SANDBOX-agent-decoupling-spec-S076.md + P-ARCH-030 |
| `agent-deletion-test` | per-session | **blocking** | NEW S076 Phase C: Agent-Deletion-Test keystone validator. 6-step simulation proving durable system holds without scaffold layer. **S076 RIPPLE: canonical enforcement for simulation spine B4 (deletion-decoupling-sim) per core-spine-registry.yaml#simulation.** PASS = PROPERLY DECOUPLED (exit 0). FAIL = lists coupled surfaces (exit 1). No artifacts modified — simulation mode. Foundation dim 3 keystone. **Registered S076 Phase C, cross-ref S076 RIPPLE. Build ACTIVE.** | SANDBOX-agent-decoupling-spec-S076.md + validate-layer-split.mjs + P-ARCH-030 + core-spine-registry.yaml#simulation-B4 |
| `executor-contract` | per-session | **blocking** | NEW S076 Phase D: Executor Contract enforcement. Validates each of 4 model-agnostic clauses (C1 cite-evidence / C2 verify-before-seal / C3 threshold-routing / C4 sp-registry-compliance) has system-layer T1+T2. Missing clause T1 → BLOCKING. ADVISORY if T1 exists but lacks exit-1/exit-2 gate. See docs/architecture/EXECUTOR-CONTRACT.md. **Registered S076 Phase D. Build ACTIVE.** | EXECUTOR-CONTRACT.md + SANDBOX-agent-decoupling-spec-S076.md + P-ARCH-030 |
| `register-connectivity` | per-session | **blocking** | NEW S076 Phase 1 ACCOUNTABILITY-PE-CIA-WIRING: all 6 accountability registers (gap-recurrence / improvement / ux-violation / floating-artifacts / exceptional-moments / hardwire) MUST declare cie_connection + pe_connection per NODEFILE-CONTRACT §Delta Fields. BLOCKING if any register missing either field or contains an invalid value. ADVISORY if value=FLAGGED-TO-THRESHOLD (legitimate pending per contract). Behavioral tests A/B/C/D PASS. Closes EXISTS≠ACTIVE on cie-pe-adapter wiring (6 registers had 0/0 connections despite NODEFILE-CONTRACT requiring them). **Registered S076. Build ACTIVE.** | NODEFILE-CONTRACT.md + cie-pe-adapter.mjs + PROTO-S076-ACCOUNTABILITY-PE-CIA-WIRING |
| `cqs-coverage` | per-session | **blocking** | S077 PLANNED: CQS Alignment Layer coverage validator. Stub moved to tools/wip/validate-cqs-coverage-S077.mjs (Governor "close now" — S077 build). Spec: SANDBOX-cqs-alignment-layer-S076.md. Sets: cqs-sets.yaml. **Deferred to S077. Build NOT ACTIVE.** | cqs-sets.yaml + SANDBOX-cqs-alignment-layer-S076.md |
| `boundary-crossing-protocol` | per-session | **blocking** | NEW S076 BOUNDARY-CROSSING-PROTOCOL Phase C T2 validator. Checks tools/data/boundaries-register.yaml: every crossing (k_count>0) has 4 required artifacts — governor_approval / assessment_ref / scheduled_resolution.must_address_by_date / crossings array entry. BLOCKING: any artifact missing; k_count≥2 without re_derivation_artifact. ADVISORY: scheduled date past. BT: crossing with null governor_approval → exit 1 ✓. **EXTENDED: self-referential — promoting to STANDARD IS the first worked example of the protocol. Registered S076. Build ACTIVE.** | SANDBOX-boundary-crossing-protocol-S076.md + T1:pre-tool-use-boundary-crossing-gate.sh |
| `load-test-harness` | per-session | **blocking** | NEW S077 dim-4 Surface 4: k6 N×M LOAD-TEST HARNESS. Checks: tools/load-tests/k6/ + 4 scenarios (A burst, B pool, C rls-deferred, D noisy-neighbor) + DNA + thresholds + DEFERRED notation on C + boundary-003 ref + config.js/supabase-free.ts constant sync. N=30 gate deferred (boundary-003, Free 60-conn limit). BT-A: --block-test-a (missing scenario-a) → exit 1 ✓. **EXTENDED: prevents boundary-001 violation. Seal bar: harness + scenario-a representative run green → Opus OPIA → dim-4 SEAL. Registered S077 dim-4 Surface 4. Build ACTIVE.** | SANDBOX-multi-tenant-scale-readiness-spec-S076.md Surface 4 + boundary-003 |
| `tenant-quota-policy` | per-session | **blocking** | NEW S077 dim-4 Surface 2: TENANT QUOTA POLICY + NOISY-NEIGHBOR ISOLATION. Q6=A (shared libs/platform-quota/ SSoT, not per-app). Q1=FREE (Supabase Free conservative: 60 max_connections, 1 per app, 30 apps = 50% headroom). Checks: quota lib present + DNA + FREE_MAX_DB_CONNECTIONS≤60 + pool math (appCount×connPerApp≤maxConn) + no per-app quota drift + tier-upgrade obligation registered in boundaries-register.yaml (boundary-003). BLOCKING: quota lib missing | pool math exceeds Free limit | per-app quota definitions. ADVISORY: approaching 80% headroom. BT-A: --block-test-a flag (PLATFORM_APP_COUNT_TARGET=100) → pool math exceeds → exit 1 ✓. **EXTENDED: promoted to prevent boundary-001 violation (verify cycles hit 200/200 hard limit). Run weekly via --extended cron. Registered S077 dim-4 Surface 2. Build ACTIVE.** | SANDBOX-multi-tenant-scale-readiness-spec-S076.md Surface 2 + libs/platform-quota/ + boundaries-register.yaml boundary-003 |
| `connection-pool-contract` | per-session | **blocking** | NEW S076 dim-4 Phase 1: CONNECTION-POOL CONTRACT. Every app .env.example DATABASE_URL must use port 6543 (pgBouncer pooled) + pgbouncer=true + connection_limit=1. BLOCKING: port 5432 in DATABASE_URL | missing pgbouncer=true | missing connection_limit. ADVISORY: connection_limit > 1 without CONNECTION_LIMIT_OVERRIDE_REASON | DIRECT_URL missing. Q2 applied: connection_limit=1 standard; override requires documented reason. BT: port 5432 in DATABASE_URL → exit 1. apps_checked=7, all compliant at registration. **Registered S076 dim-4 Phase 1. Build ACTIVE.** | SANDBOX-multi-tenant-scale-readiness-spec-S076.md Surface 1 + feedback_supabase_pgbouncer_url |
| `rls-perf-budget` | per-session | **blocking** | NEW S076 dim-4 Phase 2: RLS PERF BUDGET. ZModel index discipline for tenant-scoped RLS. Q4: 10ms provisional budget (ratchet → 5ms post-UUID migration per Q7/Surface 5). BLOCKING: entity with auth().tenantId == tenantId RLS and no @@index([tenantId, ...]) — O(N) scan risk. Compound index @@index([tenantId, x]) accepted (covers tenantId-only lookups in PostgreSQL). ADVISORY: subquery/join-like RLS pattern. BT: model with RLS + no tenantId index → exit 1. 10/17 models have tenant RLS, all compliant at registration. **EXTENDED: run_tier:EXTENDED (weekly cron --extended) — promoted from DEFERRED via EXTENDED-tier S076. Registered S076 dim-4 Phase 2.** **S084 PREV-4: promote trigger corrected** — promote when Governor authorizes AND cycles drop below target 134 (hard_limit=200, soft_limit=140). Phantom "cap raised to 220" removed (hard_limit is 200 per boundary-001). | SANDBOX-multi-tenant-scale-readiness-spec-S076.md Surface 3 + validate-platform-capacity.mjs |
| `uuid-column-types` | per-session | advisory→blocking | NEW S076 dim-4 Surface 5: NATIVE-UUID MIGRATION. Checks Base.id + AppendOnlyBase.id have @db.Uuid after migration. Advisory pre-2026-06-16 (migration not yet due — gap_DIM2_CORE_ID_UUID_UPGRADE calendar-enforced by validate-finding-scheduling.mjs). Blocking after 2026-06-30 (auto-promoted K=2). **S077 update:** @db.Uuid restored to Base + AppendOnlyBase + 25 FK fields in schema.zmodel (migration SQL 20260602_uuid_native_types authored). External-ID exclusion list added (clerkId / clerkOrgId / stripeCustomerId / stripeSubscriptionId / stripePriceId / resourceId — NOT CSPS UUIDs). Validator now 0 blocking 0 advisory (pre-migration schema ready). **EXTENDED: run_tier:EXTENDED (weekly cron --extended) — promoted from DEFERRED via EXTENDED-tier S076. Registered S076 dim-4 Surface 5.** **S084 PREV-4: promote trigger corrected** — promote when (1) Governor authorization + cycles below target 134 (hard_limit=200 per boundary-001; phantom "220" removed) AND (2) migration applied (Governor DB run). | SANDBOX-multi-tenant-scale-readiness-spec-S076.md Surface 5 + gap_DIM2_CORE_ID_UUID_UPGRADE |
| `core-seeds-currency` | per-session | advisory | NEW S071 Turn 26 (OPUS-14): Core-Seeds index currency. Checks `docs/plan/_handoff/CORE-SEEDS-PLAN-PARTS.md` (canonical plan-part index per its own description) has NODE entries for each well-known doctrine in `docs/plan/pillar-0-governance/` (sample — expandable enumeration: 6 doctrines covered initially — RZF-LATEST · LONG-RUN-BUILDER-DOCTRINE · ONE-SOURCE-OF-DOCTRINE · AI-PROFILING-TO-COMMUNICATION-FEEDBACK · PLATFORM-OBSERVATION-DOCTRINE · JOURNEY-DOCTRINE). **PREVENTION CLASS:** `CORE-SEEDS-INDEX-NOT-UPDATED-AT-MILESTONE-BOUNDARY`. **Inaugural instance** Turn 24: 6 S071-authored doctrines had no CORE-SEEDS nodes until Governor surfaced gap; OPUS-14 had treated as instance-fix not pattern-catch (B_STRUCTURAL_PREVENTION_DISCIPLINE violation); Turn 26 challenge produced this validator. **ADVISORY in S071 (promotes to BLOCKING after 5 sample exemplar passes — tunable per P-META-028).** Self-PASS at Turn 26 (6 checked / 0 missing). Full T1+T2+T3 (pre-commit hook + status-divergence check + HPFA 8-check) queued as vlt-S072-core-seeds-currency-full-engraving. **Registered S071 Turn 26. Build ACTIVE.** | CORE-SEEDS-PLAN-PARTS.md + B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 + P-META-028 |
| `no-nominal-stops` | per-session | advisory | NEW S071 M0.7: Long-Run Builder Discipline T2 validator. Scans recent `tools/council/sonnet-turn.md` entries for N1-N8 nominal-stop patterns (confirmation-seeking on ratified work · over-cautious pauses · mid-execution ack requests · scope creep). Exempt: Milestone Report headers + ASK-OPUS-STOP blocks + ZF evidence blocks. **ADVISORY in S071 (promotes to BLOCKING after 5 sample exemplar passes — tunable per P-META-028).** Test 3/3: A nominal-stop mid-milestone → flag · B real-stop R3 ASK-OPUS-STOP → pass · C Milestone Report header → pass. Closes EXISTS≠ACTIVE on B_AUTONOMOUS_BATCH_WITH_PREFLIGHT (CONSTITUTIONAL S016 with T1/T2=none). **Registered S071 M0.7. Build ACTIVE.** | LONG-RUN-BUILDER-DOCTRINE.md + B_AUTONOMOUS_BATCH_WITH_PREFLIGHT + B_NO_CONFIRMATION_SEEKING |
| `proto-completeness` | per-session | advisory | NEW S071 M0.5: validates docs/plan/protos/*.md against proto.template.md mandatory sections. Checks: all 9 required section headers (CORE SEED, INHERITS/ALIGNS-WITH, ASK-OPUS-STOP, N-PERSONA REVIEW, STEP 0, ZF GATE, PREVENTION CLASSES, §15 3-SCOPE, AUTHOR SIGNATURE) + N-PERSONA header integer == actual persona block count (kills nominal persona claims at source) + ratification fields + author signature. **ADVISORY in S071 — promotes to blocking after existing protos pass clean (exemplar gate).** Test 3/3 PASS (behavioral test: proto-completeness-test.sh): A passing-proto → pass · B persona-count-mismatch (5 claimed / 3 present) → flag · C missing-required-section → flag. **S071 M0.5 persona-regex fix:** matches `**name:**` (colon inside) + `**name**:` (colon outside); count check fires even when actualCount=0. **Registered S071 M0.5. Build ACTIVE.** | PROTO-AND-TAB-TRANSFER-PROTOCOL.md + proto.template.md + P-META-028 |
| `communication-schema-coverage` | per-session | advisory | NEW S070 M1+M2 + **S073 P2-D extension (CHECK I)**: validates communication-schema.yaml + platform TOOL question text. M1: schema file exists, 8 situation IDs, 6 tier IDs, 9 B_* contract files, required fields. M2 extension: wired_situations=8/8 + wired_tiers=6/6. **S073 CHECK I (tool-text jargon scan):** scans `apps/**/src/**/*.tsx` for question-like strings containing platform-internal terms inappropriate for user-facing UI (PROTO-, B_*, P-META-, lifecycle_state, ZF Cycle, RZF, behavioral contract, threshold router, GVRN/VALD/OPER as jargon). ADVISORY — baseline S073: tool_jargon_findings=10 (10 question strings in apps/ with platform-internal terms); promotes to blocking once baseline clears. Prevention class: COMMS-SCHEMA-JARGON-IN-TOOL-QUESTIONS. **Registered S070 M1, Extended M2, CHECK I added S073. Build ACTIVE.** | PLAN-S069-COMMS-AND-JOURNEY + communication-spine/ + UX-PAGE-TYPES.md + P-META-028 |
| `register-reference-integrity` | EXTENDED | advisory | NEW S085 P1.1/SEED-A: every PARK-/PROTO-/M-/VLT-/imp_/gap_/SEED- register ID cited in tracked .md/.yaml resolves to its canonical register. Closes the PARK-040 ghost-ref class. Scans 763 files; 189 advisory unresolved refs at baseline (old HANDOFF PROTOs that rolled off opus-turn.md). **Phase 2 (Opus #24-D S085)**: BLOCKING for current-session HANDOFF ghost-refs (files modified within 24h); historical stay ADVISORY. Uses statSync mtime proxy. EXTENDED (corpus scan). Build ACTIVE (validate-register-reference-integrity.mjs). | P-META-033 + SEED-A + HANDOFF-INTEGRITY-SEEDS-S084.md |
| `dual-coverage` | EXTENDED | advisory | NEW S085 SEED-C (updated S085 cadence ladder per Opus #23): 8/8 obligations dual-covered. Context-independence: SOURCE=persistent files + CADENCE=session-open (moats/register-refs/dual-coverage now on session-open background trigger) + SINK=cadence-last-run.json. load_mode passes via T1 creation-gate (pre-tool-use-token-guardian.sh = context-independent by definition). BLOCKING at K=2 per gap-recurrence-register. EXTENDED. Build ACTIVE (validate-dual-coverage.mjs). | SEED-C + P-META-033 + M-46 |
| `file-complexity-threshold` | per-session | advisory | EXTENDED S085 P1.1: extended to code files (.mjs/.ts); was .md-only. New code gate: >500 LOC without @complexity_exempt. verify.mjs at 2461 LOC now surfaced. 7 code files found; 122 .md files pre-existing. Never blocks. |
| `journey-gate` | EXTENDED | blocking | NEW S085 B3.1 (PROTO-S084-B3): PEG enforcement validator for the journey orchestrator SEED-2 gate_mode matrix. Reads tools/data/seed2-gate-mode-matrix.json (SEED-2 ANCHOR, authored OPUS-22). Implements evaluate() — the single admission controller at every PEG transition. Runs 10 block-tests across PEG-1..5 x mechanism (THRESHOLD/ZF/PE/CIE) x risk_class (low/standard/elevated/critical) to prove blocking gates refuse missing-evidence advances. BLOCKING if any block-test fails. EXTENDED: matrix only changes on SEED-2 boundary-crossing event. **Registered S085 B3.1. Build ACTIVE.** | SEED-2 + PROTO-S084-B3 + JOURNEY-SEEDS-S084.md |
| `trunk-matches-seed` | per-session | blocking | NEW S085 B3.2 (PROTO-S084-B3): core-spine-registry journeys trunk must contain all C1-C5 invariants + P1-P5 phases verbatim from SEED-1 (OPUS-22 authored). Prevents C5 transcription drift Opus caught at B1 review. BLOCKING if any C-invariant or phase ID is missing from the registry. ADVISORY if verbatim-source marker missing. input_files: core-spine-registry.yaml + JOURNEY-SEEDS-S084.md (cached when both unchanged). **Registered S085 B3.2. Build ACTIVE.** | SEED-1 + PROTO-S084-B3 + JOURNEY-SEEDS-S084.md |
| `journey-event-store` | per-session | blocking | NEW S085 B3.3 (PROTO-S084-B3 SEED-8): wires SEED-8 event store decision per Opus R3 immutability audit. audit.events HAS active no_direct_write RLS (append-only by policy); public."AuditEvent" trigger commented out (convention only). Decision: REUSE audit.events (entity_type=journey_event) for all journey events. BLOCKING if no_direct_write RLS not active on audit.events (no immutability guarantee). ADVISORY: AuditEvent trigger commented out (harden in B4/B5). **Registered S085 B3.3. Build ACTIVE.** | SEED-8 + P-ARCH-008 + audit-triggers.sql |
| `hash-cache` | per-session | blocking | NEW S085 (PROTO-S084-HASH-CACHE): block-test validator for the Validator Input Manifest cache system. BLOCKING if any always_rerun validator (push_status, git_pushed_state) appears in cache (would mean the anti-nominal guard is broken). Checks: (1) cache file has valid v1 structure; (2) always_rerun markers present in verify.mjs (≥2); (3) input_files declarations present (≥2 pilot validators); (4) --no-cache wired in post-stop-pnpm-verify.sh. DONE-path proof: post-stop calls verify --no-cache → ALL validators run live → a cached PASS cannot satisfy a DONE/SEALED claim. Advisory if cache not yet populated (first run). **Registered S085 hash-cache. Build ACTIVE (tools/validators/validate-hash-cache.mjs).** | PROTO-S084-HASH-CACHE + B_TOKEN_BUDGET + B_PRE_CLOSE_VERIFICATION |
| `token-efficiency` | EXTENDED | advisory→blocking | NEW S084 TEG (PROTO-S084-TEG): T2 of B_TOKEN_BUDGET enforcement trio. Scans .claude/hooks/*.sh + settings.json for load_mode declarations. BLOCKING: `load_mode: eager` without `# justification:` comment (T1 hook should catch this at creation; T2 is the regression gate). ADVISORY: missing load_mode on pre-existing hooks (grandfathered). Reports 2-audience cost: INNER-PLATFORM (per-turn hook execution + session overhead) + EXTERNAL-USER (per-action platform overhead). Regression-flags new eager surfaces vs last-green run. The Guardian itself must be lazy: T2 is EXTENDED (verify-time); T1 fires only on writes to always-on surfaces. R9 promotes the enforcement_trio from T3-only (advisory) to T1+T2+T3 (mechanical). **run_tier:EXTENDED**. Block-test: tools/tests/behavioral/token-guardian-block-test.sh. **Registered S084 TEG. Build ACTIVE (tools/validators/validate-token-efficiency.mjs).** | B_TOKEN_BUDGET R9 + PROTO-S084-TEG |
| `gate-mode-matrix` | EXTENDED | advisory | NEW S084 B2 (PROTO-S084-B2): validates GateDef.gateModeMatrix JSON objects against SEED-2 hard floors (tighten-only; never relax below floor). Checks: (1) valid structure — 4 mechanisms × 4 risk_classes; (2) valid values — GateModeValue {blocking|advisory|silent}; (3) SEED-2 hard floors per mechanism×risk_class×phase. Self-validates the 5 SEED-2 reference matrices on every run. Advisory on floor violations (BLOCKING promoted if gates diverge from SEED-2 reference). **run_tier:EXTENDED** (no STANDARD cycle cost). Registered S084 B2. Build ACTIVE (tools/validators/validate-gate-mode-matrix.mjs). | SEED-2 gate_mode_matrix + JOURNEY-SEEDS-S084.md + journey-closed-enums.yaml |
| `core-spine-template` | per-session | **blocking** | NEW S073 B0 (PROTO-S073-CORESPINE-B0): validates tools/config/core-spine-registry.yaml. BLOCKING: every spine entry has all 8 required sections (trunk · branches · alignment_map · wiring_map · tier_permission · cie_pe · escalation · realtime_save) + alignment_map.classification_dimension ∈ {GVRN,VALD,ARCH,AI,OPER} + every wiring_map[] entry with a `file:` key resolves to a real file on disk (EXISTS≠ACTIVE check) + root (if non-null) resolves. Behavioral test 3/3: A=valid-8-sections-empty-wiring-map→PASS · B=missing-section→BLOCKING · C=wiring-map-bad-path→BLOCKING. ADVISORY mode while registry has only stub entries (auto-detects). DEFERRED in pnpm verify (pnpm-verify-cycles at capacity — run manually: node tools/validators/validate-core-spine-template.mjs). **B3 extension:** wiring_map entries support `status:PLANNED` field — declared-not-required-to-resolve (ACTIVE entries must existsSync, PLANNED entries skipped). Line-by-line block parse (lookahead 10 lines per entry). Behavioral test: 4/4 A=valid-pass · B=missing-section-block · C=bad-ACTIVE-block · D=PLANNED-no-block. Registered S073 B0, extended B3. | P-ARCH-028 + SPINE-PILLAR-MAP.md + core-spine-registry.yaml + PROTO-S073-CORESPINE-B0 |
| `completion-before-new` | per-session | advisory | NEW S073 E1 (COMPLETION-DISCIPLINE-PLAN-S073.md): scans docs/plan/protos/ + docs/plan/ for evidence that new plan items were created while active PROTOs have open milestones (unchecked `- [ ]` items). Flags open PROTO count + unchecked milestone count. ADVISORY (never blocks) — surfaces completion state. **run_tier:STANDARD** (fast scan, governance discipline cornerstone). Prevention class: COMPLETION-BEFORE-NEW (P-OP-008 / P-OP-002 FWWS mechanical arm). Behavioral test: 3/3 (open-proto→advisory · all-sealed→pass · no-protos→pass). **Registered S073 E1 BEFORE wiring. Build ACTIVE (validate-completion-before-new.mjs).** | COMPLETION-DISCIPLINE-PLAN-S073.md + P-OP-008 + P-OP-002 + Long-Run M0.7 |
| `no-floating-artifacts` | per-session | advisory | NEW S073 B4 (PROTO-S073-B4 / ANTI-FLOAT T2): scans docs/plan + docs/SIA for non-terminal status files (draft/proposed/pending-review/awaiting-*). Flags: (a) missing closure obligation (closure_owner+closure_decision+closure_by), (b) registered with escalation_state:overdue. Prevention class: FLOATING-ARTIFACT-NEVER-REACHES-TERMINAL. Accountability spine T2+T4+T7 (path-to-terminal+aging+escalation). **run_tier:DEEP** (exhaustive corpus scan of 860+ files, non-cornerstone — run via --deep or zf-orchestrator). **ADVISORY S073 baseline: floaters_found=28 missing_obligation=27 overdue=26**; promotes to BLOCKING after backfill clears. Behavioral test: 3/3 (floater-no-obligation→flag · obligation-not-overdue→pass · overdue→escalate). DEFERRED in pnpm verify (pnpm-verify-cycles constraint). **Registered S073 B4. Build ACTIVE (validate-no-floating-artifacts.mjs).** | PROTO-S072-ANTI-FLOAT + PROTO-S073-B4 + floating-artifacts-register.yaml + accountability spine T2+T4+T7 |
| `no-implementation-without-plan-gov` | per-session | advisory | S053-B Step 8: validate-no-implementation-without-plan.mjs extended to also count governance files (validators/hooks) without matching plan items. ADVISORY count only — most predate the EXPLORE-RATIFY-EXECUTE protocol. New additions (S053+) should cite plan item ID. **Updated S053-B. Build ACTIVE (validate-no-implementation-without-plan.mjs in pnpm verify; advisory exit 0).** | EXPLORE-RATIFY-EXECUTE + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `gap-recurrence` | per-session | **blocking** | NEW S053 gap_CONCEPTUAL_CLOSURE_NO_TEST fix: reads tools/data/gap-recurrence-register.yaml. BLOCKING: K>=3 AND status:open AND structural_fix_triggered:false (P-META-019 fires). ADVISORY: K>=2 AND status:open AND behavioral_test_exists:false. Surfaces all open gaps with K count. **Registered S053 atomically. Build ACTIVE (validate-gap-recurrence.mjs in pnpm verify).** | P-META-019 + B_STRUCTURAL_PREVENTION_DISCIPLINE + gap-recurrence-register |
| `zf-cycle-format` | per-session | **blocking** | NEW S053 gap_ZF_NOMINAL_CYCLES T2 (K=6): scans tools/council/sonnet-turn.md + opus-turn.md for ZF blocks. BLOCKING: ZF ACHIEVED claimed but Cycle 2+ cites no file names. ADVISORY: vague words (areas/topics/things) without file names. **S062 update (FINDING-OPUS10-4):** FILE_PATTERN extended to recognize `.txt/.env/.cjs/.mts`; cycle-marker regex anchored to start-of-line to prevent mid-text "Cycle N" self-references from being parsed as new cycle starts. Behavioral test at tools/tests/behavioral/zf-cycle-format-test.sh: INPUT A (nominal) → exit=1 ✓, INPUT B (file-citing) → exit=0 ✓. **Registered S053. Parser hardened S062. Build ACTIVE.** | B_ZF_TERMINATION_DISCIPLINE + gap_ZF_NOMINAL_CYCLES |
| `state-claims` | per-session | advisory | NEW S059 PROTO-G B_VALIDATE_BEFORE_ASSUME T2: scans tools/council/sonnet-turn.md for council entries (FROM SONNET / OPUS-*) containing numeric state claims (validators=N, exit_code=N, blocking=N, commit sha) without associated verification evidence text (Confirmed / per Sonnet report / this-session run). ADVISORY only — state_claims_without_evidence=[N]. **Registered S059 PROTO-G. Build ACTIVE (validate-state-claims.mjs in pnpm verify).** | B_VALIDATE_BEFORE_ASSUME + P-META-006 |
| `humble-first-step` | per-session | advisory | NEW S059 PROTO-G B_HUMBLE_FIRST_STEP T2: scans tools/council/sonnet-turn.md for PROTO-* sections where STEP 1 has >10 sub-items. Over-scoped first steps indicate scope creep (B_HUMBLE_FIRST_STEP). ADVISORY — protos_checked=[N] overscoped=[N]. **Registered S059 PROTO-G. Build ACTIVE (validate-humble-first-step.mjs in pnpm verify).** | B_HUMBLE_FIRST_STEP + P-META-019 |
| `declared-never-finished` | daily | advisory | NEW S051 AP-001 subsection: finds 'Build ACTIVE' slugs with no matching validator file + week-4 stubs past target. Three classes: (1) active-claim-no-file, (2) week-4-stale, (3) done-plan-item-no-SHA. Routes per tools/config/scope-questions.yaml gap_classification. **Registered S051 atomically. Build ACTIVE (validate-declared-never-finished.mjs in pnpm verify; advisory exit 0).** | AP-001 + P-OP-007 + scope-questions |
| `gap-routing` | daily | advisory | NEW S051: reads validate-declared-never-finished output, classifies per scope-questions.yaml (CRITICAL/SIGNIFICANT/ADVISORY/COSMETIC), appends to tools/data/gap-vault.yaml. Routing engine: CRITICAL=fix now | SIGNIFICANT=1-2 sessions | ADVISORY=weekly | COSMETIC=daily. No overload — advisory only. **Registered S051 atomically. Build ACTIVE (validate-gap-routing.mjs in pnpm verify; advisory exit 0).** | AP-001 + P-OP-007 + scope-questions |
| `session-posture-check` | per-session | advisory | NEW S051 P-OP-007: session-open injection confirms "optimal path over fastest path" posture. Validator checks that per-session reports cite depth-first choice rationale when depth-1 vs depth-3 options existed. Advisory: surfaces responses where speed-first language ("quick fix", "fast approach", "simple solution") appeared without explicit DPR/PE rationale. **Registered S051 atomically. Build deferred (validate-session-posture.mjs).** | P-OP-007 + B_HUMBLE_EXECUTION_PIPELINE + B_GRADUAL_BUILD_BY_FOUNDATIONS |
| `completion-bias-enforcement` | per-session | warn | NEW S015 B_COMPLETION_OVER_SHINY: detects when active phases were abandoned mid-completion for new items (checks plans where completion_pct > 50 but lifecycle_state reverted to earlier stage, or where exit criteria were left partially checked when a new plan was opened). Also surfaces active phases that have queued-but-unprocessed items in raw-thoughts-queue — reminding AI to run milestone gate before starting new work. Phase 1: WARN advisory. Phase 2: BLOCKING when AI attempts to open a new plan while an active plan is >50% complete and no milestone gate was run. **Registered S015 atomically 5/5 FSE. Build deferred week-4 (validate-completion-bias-enforcement.mjs).** | S015 + B_COMPLETION_OVER_SHINY + P-META-018 |
| `phase-exit-criteria` | per-session | **blocking** | NEW S015 FOUNDATION_EXIT_GATE (major discovery): any active topic plan with mixed-state "Exit Criteria" section ([x] items AND [ ] items in same section) = BLOCKING exit code 1. Metric: `blocking=N sections` (sections with mixed state, not item count). PE score for next phase = 0 until gate is CLEAN. Root cause: Phase 4 declared COMPLETE in session-state while Phase 3B had 2 unchecked exit criteria + Phase 4 roadmap had 0 validated exit criteria — freestyling to Phase 5 was possible because no validator caught the gap. Wired into: pnpm verify + ZF orchestrator Level 1 + session-open.sh. Resolution: (a) complete criterion, (b) defer with explicit WHY. Silent deferral = item stays blocking. **Registered S015 atomically 5/5 FSE. Build ACTIVE (validate-phase-exit-criteria.mjs in pnpm verify + orchestrator).** **conceptual_sample_of:** VALD L2 + GVRN L2 — exit criteria are phase promises. Unchecked promises = foundation debt that compounds into every app built on top. | S015 + B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-016 |
| `open-plan-levels` | per-session | warn | NEW S014 Phase 3A: surfaces ALL unchecked exit criteria (not just file-path ones) across active topic-plans. Per P-META-020 context-depth-degradation pattern — open plan levels are obligations, not options. Root cause fix for foundation-slices L3 orphaning (plan-promise-abandonment). **Registered S014 Phase 3A atomically. Build ACTIVE (validate-open-plan-levels.mjs in pnpm verify).** **conceptual_sample_of:** VALD L2 coverage discipline — open items are the measurement of whether the concept (platform builds only what it commits to) is being honored. | P-META-020 + B_STRUCTURAL_PREVENTION_DISCIPLINE |
| `topic-plan-progress` | PR + per-session | warn | NEW S011 §24++ gap-fix: active topic-plans whose arc ended in a past session without §11 closure = ORPHAN. Also checks unchecked L-level exit criteria with file paths — verifies files exist. Root cause: zero-laptop-dependency (S006) + unified-intake (S008) sat OPEN for 3-5 sessions with no deliverables. Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2 + P-META-016 gradual-build + B_GRADUAL_BUILD_BY_FOUNDATIONS. **Registered S011 §24++ atomically. Build ACTIVE S011 (tools/validators/validate-topic-plan-progress.mjs; wired into pnpm verify).** |
| `audit-slug-coverage` | PR | warn | NEW S011 §24++ gap-fix: every tools/validators/validate-*.mjs has matching audit slug in audit-runner.md. **S054 UPDATE: KNOWN_MAPPINGS extended with 11 new entries (platform-genome-freshness, improvement-register-cec, apps-are-trials, communication-quality, zf-cycle-format, gap-recurrence, threshold-intake, context-question-coverage, guard-questions + more).** Per B_STRUCTURAL_PREVENTION_DISCIPLINE Q-2 + P-META-019. **Registered S011, Updated S054. Build ACTIVE.** |

### Stewardship (4 — P-META-004 Stored Content Lifecycle enforcers)

Per [stewardship-protocol.md](./stewardship-protocol.md). Every saved artifact declares `lifecycle_state` and has a recurring trigger that advances it. These four checks ensure no artifact becomes an orphan-in-waiting.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `orphan-without-lifecycle-state` | PR | error | Every artifact has `lifecycle_state` declared (or inherited via `tools/catalog/variants.ts`); blocks PR if missing |
| `stale-pending-review` | PR + nightly | warn | Items in `pending-review` past `sla_pending_review_days` (14) flagged; PR blocks merge if a referenced item is breached |
| `stale-pending-protocol` | nightly | error | Items in `pending-protocol` past `sla_pending_protocol_days` (30) flagged for protocol creation (signals a real input-class gap) |
| `legacy-archive-review-due` | weekly | warn | `_legacy/` items past their `next_review_at` flagged for review or retirement (matches Backstage 90-day quarterly cadence) |

### Zero-Findings Discipline (6 — P-META-006 enforcers, added S002 turn 10; +2 S018 P1/P5 promotion)

| `extraction-check-blocking` | per-session | **blocking** | PROMOTION S018 (was advisory): session-S<NNN>-extraction.md must exist and be non-empty before ZF Level 3 exits ACHIEVED. Sender's obligation is not complete until positive harvest is documented (B_RESULT_NOT_OUTPUT applied to session-close). **Registered S018 P5 pipeline. Build immediate (edit zf-orchestrator.mjs exitCheckAdvisory flag).** **conceptual_sample_of:** VALD L2 — extraction is the evidence that the session produced value; ZF without extraction is evidence of mechanics without substance. |
| `session-open-sequence-verified` | per-session | warn | Checks that within first 3 turns of a session: (1) session-state.json was read, (2) pnpm verify ran, (3) VLTs confirmed 0 PENDING, (4) CONCEPT_LOAD declared. Missing any = warn. All missing = error. Per B_CONCEPT_LOAD + threshold-gate-v2.md. **Registered S018 P13 pipeline. Build week-4 (validate-session-open-completeness.mjs).** **conceptual_sample_of:** GVRN L2 — the Threshold is the governance entry point; unverified session-open means work runs without the conceptual frame. |

Per [zero-findings-discipline.md](./zero-findings-discipline.md). Every artifact reaching DONE/COMPLETE/RATIFIED emits an RZF evidence block; every ratified principle/leaf/ADR/contract emits a CEC walk-trail. Cycle count is MEASUREMENT not TARGET.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `rzf-coverage` | PR | error | Every artifact at `lifecycle_state ∈ {validated, closed}` has an RZF evidence block (cycles_run + findings_per_cycle + final_status + coverage tokens + validators_run + signature). Missing = fail. |
| `cec-walk-trail-completeness` | PR | warn | Every newly-ratified principle/leaf/ADR/behavioral-contract has a CEC walk-trail (extracted_essence + cycles_walked + walk_scope + applications_made + not_applicable + needs_human_judgment + signature). Missing = warn. |
| `cycle-count-as-target-detection` | PR | warn | Detect language pattern "run N cycles" or "predetermined cycles" in commit messages / docs / closing summaries. Cycle count must be findings-driven, not pre-determined. |
| `progress-not-completion-detection` | per-session | warn | Detects the AI satisfaction point: session log contains "ZF improving" / "only advisory" / "findings reduced" used as ZF ACHIEVED evidence without a final zero-findings run. ZF ACHIEVED = LAST run producing "STATUS: ZF ACHIEVED ✅ — 0 blocking." Progress is not completion. Improvement is not proof. Requires session-log semantic analysis. enforcement_stage: human-judgment (cannot be deterministic — AI self-assessment required). **Registered S018 CEC B_PRE_CLOSE_VERIFICATION enhancement. Build: human-judgment tier — no automated validator possible.** **Self-assessment Q:** "Did my last ZF run output exactly 'STATUS: ZF ACHIEVED ✅ — 0 blocking findings remain'? If not, work is not done." **conceptual_sample_of:** VALD L2 — the satisfaction point fires at improvement; zero findings is the actual coverage target. |
| `audit-of-audits` | nightly | critical | Per CSP precedent: audit registry itself is healthy. Result files exist for every registered audit. No audits in ABANDONED state. Audit-frequency matches declared cadence. The RZF discipline applied recursively to the RZF infrastructure. |

### Learning Loop (6 — P-META-005 enforcers)

Per [learning-loop.md](./learning-loop.md). Every input stream (chat, errors, audits, feedback, AI insights) is routed through a single observed → triaged → routed → fixing → validated → closed pipeline. These six checks ensure inputs don't escape the loop and the loop itself doesn't degrade.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `learning-loop-coverage` | per-session | warn | Every session produced ≥1 extracted item OR explicitly marked "no insights" with justification |
| `repeat-issue-detection` | weekly | error | Same gap appearing in ≥2 sessions within a 90-day window auto-creates an ADR for permanent fix (the killer enforcer; K=2/90d is industry default) |
| `unresolved-observation-stale` | nightly | warn | Items in `observed` state >7 days escalate; >24h SLA for P0/P1 |
| `fix-without-validation` | nightly | error | Items in `fixing` state >14 days without `validated` transition flagged (prevents closure-theater) |
| `validation-without-recurrence-check` | weekly | warn | `closed` items get a 30-day recurrence check (90 for non-critical); reopen if recurrence detected |
| `meta-loop-audit` | monthly | warn | Resolution-cycle-time trend over 90-day rolling window; if degrading >20%, file an ADR. Meta-meta-loop signal. |

### AI-Runtime (8 — Pillar 5 Mastra + persona-composition enforcers, added S003 extended)

Per [mastra-setup.md](../pillar-5-ai-systems/mastra-setup.md) + [persona-composition.md](../pillar-5-ai-systems/persona-composition.md). Defense-in-depth enforcement of the runtime substrate that serves all personas via one Mastra agent.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `middleware-order` | PR | error | crisis-interceptor FIRST, dispatcher-middleware SECOND in BaseAgent middleware array (per ADR-0006 + ADR-0008 ordering invariant) |
| `direct-llm-call` | PR | error | Static-analysis catches any `Anthropic` / `OpenAI` / `mastra.llm()` call bypassing BaseAgent dispatch |
| `composition-skip` | PR | error | Runtime gate + static analysis: every Mastra dispatch must invoke composePersona; CompositionSkipViolation thrown otherwise |
| `extension-registration-static` | PR | error | App-level middleware extensions registered at bootstrap only; no per-request mutations |
| `persona-composition-skip` | PR | error | Every persona dispatch hits the composition function (sibling of `composition-skip`; persona-side check) |
| `persona-eval-baseline` | PR | error | PUBLISHED personas have eval baseline established (drift / style / domain-accuracy) |
| `persona-drift-detection` | nightly | warn | Topic-drift detected over rolling 7-day window per PUBLISHED persona; warn above threshold |
| `persona-overlay-completeness` | PR | warn | Personas with `domain ∈ {clinical, legal, medical, financial, spiritual}` carry the matching non-counsel disclaimer overlay |

### Persona-Crisis (5 — Pillar 5 crisis-escalation enforcers, added S003 extended)

Per [crisis-escalation.md](../pillar-5-ai-systems/crisis-escalation.md). Load-bearing for v1; ADR-0006.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `persona-crisis-slice-attachment` | PR | error | Every persona has the crisis-escalation slice attached (cannot opt out) |
| `crisis-detector-recall` | eval-time | critical | Detector + validator combined recall = 100% on test corpus (must = not ≥); eval blocks PUBLISHED transition if violated |
| `crisis-event-retention` | weekly | warn | CrisisEvent rows retained ≥7 years (regulatory floor); warn if any pruned earlier |
| `crisis-escalation-removal` | PR | critical | No persona overrides the slice to REMOVE a default escalation path; extensions are additive-only |
| `crisis-pattern-review` | quarterly | warn | Regex pattern list reviewed by clinician + safety team within 90-day cadence; warn if overdue |

### Operations + Delivery (6 — Pillar 6 enforcers, added S003 extended)

Per [build-order.md](../pillar-6-operations-and-delivery/build-order.md) + [graduation-pipeline.md](../pillar-6-operations-and-delivery/graduation-pipeline.md) + [open-frontiers.md](../pillar-6-operations-and-delivery/open-frontiers.md) + [dashboards.md](../pillar-6-operations-and-delivery/dashboards.md).

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `dependency-graph-violation` | PR | error | No cross-week dependency violation (e.g., persona slice referencing not-yet-shipped Mastra) per `pillar-6/build-order.md` critical-path graph |
| `graduation-eligibility` | weekly | warn | Apps eligible per `graduation-pipeline.md` gate (slice maturity + persona maturity + audit cleanliness + customer migration plan); advisory only |
| `vendoring-provenance` | PR | error | `provenance.yaml` present + valid on every graduate; bidirectional traceability with CSPS |
| `crisis-slice-vendored` | PR | error | Every graduate's vendored governance includes the crisis-escalation slice (load-bearing inheritance) |
| `frontier-trigger-defined` | PR | error | Every entry in `pillar-6/open-frontiers.md` declares a discovery-trigger |
| `frontier-interim-posture-defined` | PR | error | Every frontier declares interim posture (how we behave until trigger fires) |
| `frontier-stale-2-cycles` | weekly | warn | Frontier living past 2 missed review-dates escalates to critical-stale |

### Bootstrap + Dashboard (4 — added S003 extended)

Per [bootstrap-script.md](../pillar-6-operations-and-delivery/bootstrap-script.md) + [dashboards.md](../pillar-6-operations-and-delivery/dashboards.md).

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `bootstrap-idempotency` | nightly | warn | Re-run of `tools/bootstrap.ps1` produces all SKIP states (proves idempotency contract) |
| `bootstrap-readiness-report-completeness` | PR | warn | `tools/bootstrap-readiness.md` has all required sections per `bootstrap-script.md` |
| `admin-app-singleton` | PR | error | No separate `apps/admin-*` apps exist; `/admin/*` is the singleton surface gated by staffRole |
| `impersonation-banner-presence` | PR | error | Banner component present on every impersonated route (high-contrast, non-dismissible, auto-end at 30min) |
| `dashboard-direct-table-read` | PR | error | Static analysis catches dashboard reads of application tables (must read from `audit.events` + `*_facts` materialized views only) |

### Generator + Skill (5 — Pillar 4 enforcers, added S003 extended)

Per [generators.md](../pillar-4-developer-experience/generators.md) + [skill-ingestion-contract.md](../pillar-4-developer-experience/skill-ingestion-contract.md) + [skills-package.md](../pillar-4-developer-experience/skills-package.md).

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `dual-registration-drift` | PR | error | Files exist in tree without catalog entry (orphan files) OR catalog rows without files (orphan catalog entries); per B_ATOMIC_DUAL_REGISTRATION |
| `orphan-file` | PR | error | Alias for `dual-registration-drift` from the file-side; cited by `generators.md` |
| `generator-test-coverage` | PR | warn | Every generator has a test fixture |
| `skill-codegen-drift` | PR | error | Generated skill content (in `packages/skills/<id>/SKILL.md`) hash matches the source row in `principles.yaml` (codegen freshness check) |
| `skill-frontmatter-completeness` | PR | error | Every `SKILL.md` declares full capability set (allowed_tools / allowed_subagents / allowed_outbound_hosts / allowed_db_operations / sensitive_data_access) per pillar-3 sandboxed-skill-governance |
| `skill-lock-drift` | PR | error | `packages/skills.lock.yaml` mutated outside generator invocation; CI fails |
| `catalog-coverage` | PR | warn | Every leaf-level artifact has a catalog entry (not just files Glob-walks find but explicitly registered) |

### AI Behavior (9 — added S003 extended; +1 S005 turn 8; +5 S018 P12 behavioral-alignment)

Per [ai-behavior-instructions.md](../pillar-4-developer-experience/ai-behavior-instructions.md) + the meta-engraving-of-itself principle.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `agents-md-cascade-completeness` | PR | error | Every `apps/<name>/` directory has an `AGENTS.md` extending the root contract |
| `session-open-reading-order` | per-session | warn | Session log shows the AI loaded the reading-order files in the spec'd sequence (per `pillar-4/ai-behavior-instructions.md`) |
| `audit-of-audits-fse` | nightly | warn | Meta-RZF on the discipline-engraving system itself: every B_* contract has its 5 surfaces accounted for (active / declared / deferred / n/a-with-reason) per FSE block; surfaces_count_active < 2 = error |
| `pcr-completeness-on-decisions` | PR | warn | Stop-hook scan over AI chat output: detects decision-presenting language (trigger patterns per `principles.yaml#P-OP-003.triggers.fire_patterns`) without paired Pros/Cons/Recommendation 3-block. Counterweight skip patterns: trivial-reversibles per `principles.yaml#P-OP-003.triggers.skip_patterns`. Skip without explicit one-line note = anti-pattern (silent-skip). Recommendation must NAME load-bearing factor + include "what would flip" clause. Registered S005 turn 8 per B_PCR_FOR_DECISIONS strengthening (S005 turn 5 user directive). |
| `behavioral-contract-body-present` | PR | error | For every ❌ hard-NO bullet in AGENTS.md referencing a B_* slug, confirms matching `## B_<SLUG>` section in behavioral-contracts.md with: Canonical clause + Hard NO + conceptual_sample_of. A slug referenced in AGENTS.md without a contract body = a wildcard declaration. **Registered S018 P12 behavioral-alignment. Build week-4.** **conceptual_sample_of:** AI L2 inner-defaults — a B_* reference without a body is declaration without definition. |
| `contract-cec-completeness` | per-week | warn | For each B_* contract in behavioral-contracts.md, checks that at least 3 of 8 CEC surfaces were touched: principles cross_ref / audit slug / inner-ai-defaults / closing-summary section / memory file / AGENTS.md hard NO / hook / L2 domain file. Below 3/8 = WARN; below 2/8 = error. **Registered S018 P12. Build week-4.** **conceptual_sample_of:** GVRN L2 — a ratified contract without CEC propagation is governance that stays in one file. |
| `inner-ai-defaults-concept-ref-populated` | per-week | warn | Scans all .md files in docs/plan/_handoff/VAULT/inner-ai-defaults/. For each entry block, confirms `concept_ref:` field is present and populated with an L2 domain string. Missing = warn. **Registered S018 P12. Build week-4.** **conceptual_sample_of:** AI L2 — concept_ref is the calibration instrument's link to the conceptual frame; missing it severs the connection. |
| `b-star-conceptual-sample-populated` | PR | warn | For each `## B_*` section in behavioral-contracts.md, checks that `**conceptual_sample_of:**` is present and non-empty. Promotes backfill of the 43 contracts missing this field. **Registered S018 P12. Build week-4.** **conceptual_sample_of:** AI L2 — every B_* contract IS a reference sample of conceptual alignment; stating which L2 domain it samples makes the contract machine-queryable. |
| `push-back-on-conflict` | per-session | warn | Session-log scan: when AI response follows a Governor assertion that contradicts a registered P-* principle or B_* contract without pushback, emit advisory. Targets the sycophancy failure mode — silence when evidence contradicts the Governor = tacit endorsement of error. **Registered S018 P12. Build future (requires semantic session-log analysis).** **conceptual_sample_of:** AI L2 — push-back duty is the calibration that keeps AI from becoming a yes-machine. |

### Intake-Plane Extensions (10 — added S003 §3.5.c, sourced from `_intake/tag-status-contract.md`)

Per [tag-status-contract.md](../_intake/tag-status-contract.md) §explicit-transition-validators-section.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `transition-validator-pipeline-routed-without-routed-to` | PR | error | `pipeline_state: routed` row WITHOUT `routed_to` field rejected |
| `transition-validator-pipeline-validated-without-recurrence-check-at` | PR | error | `pipeline_state: validated` row WITHOUT `recurrence_check_at` rejected |
| `transition-validator-pipeline-closed-without-closed-reason` | PR | error | `pipeline_state: closed` row WITHOUT `closed_reason` rejected |
| `transition-validator-lifecycle-pending-protocol-without-protocol-spec` | PR | warn | `pending-protocol` row without referenced protocol-design doc surfaces for review |
| `transition-validator-lifecycle-promoted-without-target-leaf` | PR | error | `promoted` row WITHOUT `promoted_to_leaf` field rejected |
| `transition-validator-lifecycle-deprecated-without-superseded-by` | PR | warn | `deprecated` row should declare `superseded_by` (warn-only — pure-retirement permitted) |
| `transition-validator-rzf-on-closed` | PR | error | `pipeline_state: closed` WITHOUT `evidence_block_ref` rejected (per P-META-006) |
| `transition-validator-cec-on-promoted` | PR | warn | `promoted` to principle/leaf/ADR/contract WITHOUT `cec_walk_trail_ref` rejected |
| `transition-validator-backward-without-reason` | PR | error | Any backward transition WITHOUT `backward_transition_reason` rejected |
| `transition-validator-illegal-jump` | PR | error | Skip transition not in allowed-transitions table rejected |

### Tag (5 — sourced from `_intake/tag-status-contract.md`)

Per [tag-status-contract.md](../_intake/tag-status-contract.md) §tag-related-audits.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `tag-closed-enum-violation` | PR | error | Every tag value resolves to the dimension's closed enum |
| `tag-propagation-coverage` | PR | warn | Sub-extraction's inheritable tags match parent input's |
| `tag-context-mismatch` | PR | warn | Extraction routed to `governance/X/` carries `domain:governance` (or has explicit override with reason) |
| `tag-removed-without-reason` | PR | error | Inherited tag dropped requires `removed_inherited_tag` field with reason |
| `tag-canonical-phrasing-drift` | PR | error | The 7 operating principles byte-match across the 4 documented places |

### Status (6 — sourced from `_intake/tag-status-contract.md`)

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `silent-state-bypass` | PR | error | Any state field changed without `state_transitioned_at` updated → drift signal |
| `state-transition-illegal` | PR | error | Transition not in allowed-transitions table fails |
| `state-without-required-field` | PR | error | `closed` without `closed_reason`; `routed` without `routed_to`; `validated` without `recurrence_check_at` |
| `pending-too-long` | PR + nightly | warn → error | Items past their state's SLA escalate severity (warn at 1× SLA, error at 2×, critical at 3×) |
| `recurrence-check-due` | weekly | warn | `validated` items whose `recurrence_check_at` arrived without action |
| `meta-state-machine-drift` | PR | error | State machine declared in `principles.yaml` matches implementation |

### Source-Type + Modality (3 — sourced from `_intake/source-types.md`)

Per [source-types.md](../_intake/source-types.md) §risk-profile + §content_modality.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `missing-timestamp-or-origin` | PR | error | External input missing any of 4 mandatory stamping fields (received_at_iso / origin / origin_detail / received_via) |
| `content-modality-required-on-extraction` | PR | error | Every extraction note declares `content_modality:` (closed enum from §content_modality); "unknown" permitted only with `triage_reason: modality-classification-failed` |
| `descriptor-shadow-tag` | PR | warn | Descriptor in `descriptors[]` conflicts with or duplicates a closed-enum value in `tags[]` |

### Continuity-Manifest + Handshake (1 — added S003 §3.5.e)

Per [protocols.md](../_handoff/VAULT/protocols.md) §11b.1 + §11b.2.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `handshake-receipt-presence` | per-session | warn | Every handoff has a paired receipt signature within 7 days OR explicit `unreceived-with-reason` declaration in next-session blockers file |

### Grandfather Backfill (3 — sourced from `qc-audit-system.md` Component 5)

Per [qc-audit-system.md](./qc-audit-system.md) Grandfather Backfill Protocol Layers 1/2/3.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `grandfather-list-age` | per-session | warn → error | Oldest grandfather artifact age tracked; warn at >30d (Layer 3 floor trigger), error at >180d hard SLA |
| `grandfather-ride-along-coverage` | PR | warn | Edits on pre-turn-10 grandfathered artifacts include ride-along RZF + CEC backfill in same save (Layer 1) |
| `grandfather-ceiling-3-per-session` | per-session | info | More than 3 backfills in one session deferred to next session (Layer 1 ceiling) |

### Closing-Summary (1 — added S002 turn 14)

Per [closing-summary-template.md](../_handoff/VAULT/closing-summary-template.md).

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `closing-summary-checklist-completeness` | per-session | error | Closing summary emits all required §10.1-§10.14 + §10.13b + §10.13c sections; empty section = `NOT_APPLICABLE_WITH_REASON` declared explicitly |

### Catch + Engraving (3 — added S002 turn 15+17; +1 S005 turn 20)

Per [behavioral-contracts.md](./behavioral-contracts.md) § B_CATCH_TO_ENGRAVING + § B_FIVE_SURFACE_ENGRAVING.

| Slug | Cadence | Severity | What it checks |
|---|---|---|---|
| `catch-engraving-completeness` | per-session | warn | Closing summary §10.13b "Catches engraved this session" has entries OR explicit `NO_CATCHES_THIS_SESSION` declaration |
| `single-surface-engraving-anti-pattern` | PR | error | New B_* contract introduced without ≥2 of 5 surfaces (schema/validator/hook/memory/contract) hit atomically |
| `positive-value-extraction-coverage` | per-session | warn | Closing summary §10.11b "Positive value extracted this session" has walk-trail entries for every significant positive event (insight/user-directive/improvement/ext-id/bug-fix/ai-self-correction/generator-output/meta-finding) OR explicit `NO_POSITIVE_EVENTS_THIS_SESSION` declaration. Per B_POSITIVE_VALUE_EXTRACTION + P-META-006 trigger-cadence amendment. **Registered S005 turn 20 atomically per FSE amendment. Build deferred week-4.** |

**Total: ~99 checks in semantic categories** (was 47 at S002 close; +66 from extended-S003 consolidation = 91 at S004 close; +5 from S005 = 96). S005 additions: `principle-count-staleness` (Meta 4 → 5; per ADR-0022 turn 8) + `pcr-completeness-on-decisions` (AI Behavior 3 → 4; per B_PCR_FOR_DECISIONS turn 5) + `pre-close-cycle-coverage` (Meta 5 → 6; per B_PRE_CLOSE_VERIFICATION turn 19) + `nominal-rzf-detection` (Meta 6 → 7; per B_PRE_CLOSE_VERIFICATION turn 19) + `positive-value-extraction-coverage` (Catch+Engraving 2 → 3; per B_POSITIVE_VALUE_EXTRACTION turn 20). Plus +30 from §C3.1 bulk-registration (S005 turn 21) — see "Behavioral Discipline Validators" consolidated section below. **Grand total: ~126 checks.**

### Behavioral Discipline Validators (consolidated S005 turn 21 — bulk-registration of refs accumulated S001-S005)

Audits that pillar leaves + behavioral-contracts.md + ai-behavior-spine.md cited by slug but had never been canonically registered in this leaf. The §C3.1 audit-registry validation pass surfaced these as dangling references; this section closes the dangling-ref backlog. Future PRs may promote entries to their semantic category (Meta / AI Behavior / Status / etc.) as build-out evolves; the registration here is the load-bearing fact (per FSE atomic-validator-registration amendment — registration mandatory atomic; implementation deferrable).

| Slug | Cadence | Severity | What it checks | Backing principle / contract |
|---|---|---|---|---|
| `active-stale` | nightly | warn | Items with `lifecycle_state: active` past 90-day inactivity flagged; per [tag-status-contract.md](../_intake/tag-status-contract.md) | P-META-004 stewardship |
| `assertion-without-evidence` | PR | error | Stop-hook scan: AI assertions of state ("I checked X" / "X is present") without paired tool-call output in same response | B_VALIDATE_BEFORE_ASSUME |
| `assertion-without-preceding-tool-call` | PR | error | Tool-call sandwich validator: `[tool-call invocation]` → `[verbatim output]` → `[assertion]` literal sequence required (turn 15 strengthening) | B_VALIDATE_BEFORE_ASSUME (turn 15) |
| `assumption-without-evidence` | PR | warn | AI assumes scope/intent without explicit ratification; surfaces in self-audit | B_VALIDATE_BEFORE_ASSUME family |
| `canonical-phrasing-drift` | PR | error | The 7 operating principles + new meta-principles byte-match across the documented places (template-governance + per-app docs) | P-META-003 + ADR-0013 |
| `catch-engraving-coverage` | per-session | warn | Closing summary §10.13b "Catches engraved this session" has entries OR explicit NO_CATCHES_THIS_SESSION declaration | B_CATCH_TO_ENGRAVING |
| `compressed-zero-findings-detection` | PR | error | Detects RZF compression under context pressure (e.g., closing-summary §10.10 with cycles_run < 1; abbreviated coverage tokens) | P-META-006 RZF anti-pattern |
| `concern-duplication` | PR | warn | Two artifacts claiming overlapping concern (e.g., two SKILL.md files claiming reuse-check) flagged for consolidation | P-OP-001 reuse-first |
| `cross-ref-resolution` | PR | error | Every `principles.yaml#P-XXX-NNN` / `audit-runner.md#<slug>` / `[link](path)` reference resolves to an existing artifact | P-META-001 |
| `discipline-engraving-completeness` | PR | warn | Iterates spine matrix; flags rows with surfaces_count < 4 (warn) or < 2 (error) | P-META-007 FSE |
| `enforcer-implementation-status` | nightly | info | Tracks per-enforcer build status (declared / implemented / under-test / deprecated); exposes dashboard | P-META-001 |
| `field-parity` | PR | error | Schema fields named in validator code exist in schema files (Zod ↔ ZModel ↔ Payload ↔ ts) | P-ARCH-003 + P-ARCH-004 |
| `force-fit-detection` | PR | warn | Extraction routed to nearest-existing leaf without `discovery_origin: true` flag when no clear path exists | B_NO_FORCE_FIT + unknown-path-protocol |
| `handoff-attestation-and-handshake-present` | per-session | error | Every handoff has paired §17 attestation + opening receipt within 7 days OR explicit unreceived-with-reason declaration | B_TWO_SIDED_HANDSHAKE |
| `handoff-section-zero-present` | per-session | error | Every handoff has §0 PASTE-TARGET BLOCK self-contained per protocols.md §11 | protocols.md §11 step 0 |
| `handshake-completion` | per-session | warn | Two-sided handshake: prior-session attestation has paired next-session receipt; absence flagged after 7 days | B_TWO_SIDED_HANDSHAKE |
| `inheritance-coverage` | nightly | warn | Per P-META-003: every app dir has principles inheritance via shared MCP / AGENTS.md cascade / Mastra BaseAgent | P-META-003 |
| `intent-to-impact-validation` | per-session | warn | Handoff §16 has prior-session-stated-intent + this-session-actual-impact + drift_assessment; warn when impact: pending > 3 sessions without rationale change | B_INTENT_TO_IMPACT |
| `manual-protocol-skipped` | PR | error | EXT-IDs surfaced in session log without paired manual-protocol.md walk + extraction-ledger entry | B_INTAKE_DISCIPLINE |
| `mastra-agent-count` | PR | error | Per ADR-0008: exactly ONE Mastra agent runtime exists; static-analysis catches additional Agent class instantiations | ADR-0008 + P-ARCH-020 |
| `memory-index-completeness` | per-session | warn | Every memory file in `~/.claude/.../memory/feedback_*.md` has corresponding entry in MEMORY.md index | P-META-007 FSE memory-surface |
| `precedent-check-coverage` | PR | error | Every new artifact carries `precedent_checked:` frontmatter field (closed enum: existing-csps / csp-carry-forward / industry-research / declared-novel) | B_NO_INVENTION_WITHOUT_PRECEDENT_CHECK |
| `principles-version-known` | PR | error | Every graduate's vendored principles.yaml has version + hash that traces to a CSPS commit | P-META-002 |
| `schema-gap-promotion-eligibility` | weekly | warn | Schema-gap registry entries with K=2 within 90 days auto-promote per learning-loop.md K=2 mechanism | _intake/unknown-path-protocol.md + P-META-005 |
| `single-surface-registration` | PR | error | Every "I built X" claim lands in TWO surfaces in same commit (file + catalog manifest); orphan files OR orphan catalog entries flagged | B_ATOMIC_DUAL_REGISTRATION |
| `state-declaration-format` | per-session | warn | Session-open AI emits fixed-format state block (token budget / validator state / pending count / open findings / scope confidence) | B_STATE_DECLARATION_AT_OPEN |
| `tag-status-mismatch` | PR | warn | Cross-validation: `pipeline_state: closed` AND `maturity: draft` is contradiction; per [tag-status-contract.md](../_intake/tag-status-contract.md) | tag-status-contract |
| `unanswered-questions-blocker` | per-session | error | Every question AI asked user without explicit reply (yes / no / drop / superseded) becomes BLK-S<NNN>-* row in blockers-S<NNN>.md; close blocked while open | B_BLOCKER_NO_SILENT_DROP |
| `validator-claim-without-rerun` | PR | error | RZF claim ("ZF-0 ACHIEVED") without paired this-session validator output; memory of earlier run ≠ validation | B_DONE / B_RZF |
| `wip-enforcer-tracking` | nightly | warn | Per P-OP-002 FWWS: `principles.yaml#P-OP-002.config.max_in_flight_slices` enforcement tracked across actual slice ledger | P-OP-002 FWWS |
| `wip-limit` | PR | error | New slice creation refused when WIP exceeded (3 slices / 2 apps default); pre-commit blocks; CI hard-fails | P-OP-002 FWWS |
| `template-citation-on-creation` | PR | error | Every commitment-layer artifact (.md/.yaml/code at boundary) has `template_used:` frontmatter field citing registered template OR `template_status: novel-pending-pattern-evaluation`; impl week-4 | B_TEMPLATE_FIRST_CREATION + P-META-015 |
| `template-registry-coverage` | PR | warn | Every entry in `_handoff/VAULT/template-registry.md` resolves to existing template file OR has `implementation_status: registered-pending-author`; impl week-4 | B_TEMPLATE_FIRST_CREATION + P-META-015 |
| `novel-pending-pattern-evaluation-staleness` | weekly | warn | Entries with `template_status: novel-pending-pattern-evaluation` sitting >5 sessions without K=2 evaluation flagged; impl week-4 | B_TEMPLATE_FIRST_CREATION + P-META-015 |
| `chat-transfer-protocol-completeness` | per-session | warn | Cross-chat task transfers use `chat-transfer-protocol.template.md` — DECLARE FIRST block, required-output per step, exact completion format, BLOCKED path present; missing = zero-drift failure; impl week-4. **Registered S012 atomically with §4 registry entry + template file.** | B_MUTUAL_UNDERSTANDING_VALIDATION + P-META-014 |
| `gradual-build-plan-coverage` | per-session | error | Multi-session topic mentioned in handoff/governor-prompts without paired plan file at `_handoff/VAULT/topic-plans/<topic-id>.md`; impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `priority-engine-depth-respected` | PR | error | Topic-plan declares depth_chosen ∈ {3, 4, 5}; free-form N rejected; impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `foundation-stability-before-layer-N` | per-session | error | L<N+1> work blocked until L<N> ZF cycle PASS; topic-plan §<N> exit_criteria all met; impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `humble-batching-required` | PR | warn | Batches without explicit composition rationale flagged; unrelated items in single batch = anti-pattern; impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `priority-engine-inputs-complete` | PR | warn | Topic-plan §<priority-engine> has all 6 inputs per level (leverage / dependency_satisfied / reversibility / risk_of_rework / multi_session_cost / priority_score); impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `backtrack-trigger-coverage` | per-session | warn | Topic-plan declares backtrack triggers in §<backtrack-register> + frontmatter; missing = nominal-RZF anti-pattern; impl week-4 | B_GRADUAL_BUILD_BY_FOUNDATIONS + P-META-016 |
| `inner-default-leak-detector` | per-session | error | Scans chat transcript + edited files for known training-default tells (regex + NLP heuristics); reports unaligned instance count to §10.0h closing-summary; impl week-4 | B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS + P-META-017 |
| `alignment-citation-on-substantial-output` | per-session | warn | Substantial AI outputs cite passed alignment-checks via §10.0i closing-summary header; uncited count > 0 flagged; impl week-4 | B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS + P-META-017 |
| `alignment-drift-over-time` | weekly | warn | Diffs current session output patterns vs 4-weeks-ago snapshot; surfaces evolved-default emergence; sister to per-major-model-update full re-registration sweep; impl week-4 | B_CSPS_ALIGNMENT_OVER_INNER_DEFAULTS + P-META-017 |
| `pe-alignment-guardian-coverage` | PR | error | Substantial responses cite PE_ALIGNMENT_GUARDIAN verdict (PROCEED / DEFLECT_SWIFT / DEFLECT_VAULT / BLOCK); CONSTITUTIONAL anti-sycophancy gate; impl week-4 | B_PE_ALIGNMENT_GUARDIAN + P-META-018 |
| `pe-trajectory-emitted-on-fire` | PR | warn | Every PE fire (PE-FULL or PE-QUICK on top-3 change) emits TRAJECTORY (multi-step lookahead); single-next-item = tunnel-vision anti-pattern; impl week-4 | B_PE_ALIGNMENT_GUARDIAN + P-META-018 |
| `pe-history-completeness` | per-session | warn | `_handoff/VAULT/pe-history.jsonl` has entries for known PE fires this session; missing = silent skip; impl week-4 | B_PE_ALIGNMENT_GUARDIAN + P-META-018 |
| `enhancement-proposal-coverage` | per-session | error | Closing-summary §10.0j has proposals OR explicit `zero_proposals_declaration` with reason for every skipped/late/partial enforcement detected this session; silent empty = anti-pattern; impl week-4 | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `structural-fix-vs-instance-fix-discipline` | per-session | warn | K=2 recurring patterns in continuous-drift-log produce structural fix proposal not instance-only patch; instance-only patch on K=2 = anti-pattern; impl week-4 | B_STRUCTURAL_PREVENTION_DISCIPLINE + P-META-019 |
| `corespine-layer-compliance` | PR | error | Declared `core_spine:` value ∈ {GVRN, ARCH, AI, OPER, VALD} canonical 5-set + corresponding `.claude/core-spines/L1_CORE_<SPINE>.md` exists; impl week-4 | B_CORE_SPINE_DISCIPLINE + P-ARCH-028 |
| `nothing-stands-alone-audit` | PR | error | Governed artifact has `core_spine:` (singular; REQUIRED) + `schema_anchor:` (REQUIRED) frontmatter; RED on ORPHAN_NO_CORE_SPINE / ORPHAN_NO_SCHEMA_ANCHOR; YELLOW on dangling parent_/extends_ refs; impl week-4 | B_CORE_SPINE_DISCIPLINE + P-ARCH-028 |
| `L1-do-not-expand-violation` | PR | error | Sealed L1_CORE_*.md files honor declared `do_not_expand:` list (no examples / no cross-references / no decomposition); body parser checks for code blocks + "for example" phrases + "see also" references; impl week-4 | B_CORE_SPINE_DISCIPLINE + P-ARCH-028 |
| `spine-precedence-conflict-detector` | PR | warn | Multi-spine artifacts with conflicting `governed_by:` references; cross-references precedence GVRN > VALD > ARCH > AI > OPER; YELLOW on lower-overrides-higher; impl week-4 | B_CORE_SPINE_DISCIPLINE + P-ARCH-028 |
| `git-pushed-state-clean` | per-session | error | `git log origin/<branch>..HEAD` empty before session-close; HPFA blocks handoff write while local commits unpushed; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `devcontainer-config-valid` | PR | error | `.devcontainer/devcontainer.json` valid per devcontainer JSON-schema; required fields present; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `bootstrap-script-fresh` | per-quarter | warn | `tools/bootstrap.ps1` last-tested timestamp <90 days; quarterly cadence; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `no-local-only-secrets-in-repo` | PR | error | Pre-commit + CI scan: .env / credentials.json / *.pem / *.key not committed; gitignore enforces; secrets in 1Password/Bitwarden only; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `multi-machine-parity-spec` | per-week | warn | devcontainer.json + bootstrap.ps1 produce same `pnpm --version` + `node --version` + 5 verify cycles; weekly cadence; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `android-workflow-documented` | per-quarter | warn | `pillar-6-operations-and-delivery/android-workflow.md` exists + last_validated <90 days; impl week-4 | B_ZERO_LAPTOP_DEPENDENCY + P-OPER-001 |
| `element-review-required-sections` | PR | warn | Element-review file at `_handoff/VAULT/element-reviews/<element-id>-S<NNN>.md` has §1 state-of-art + §2 enhancement opportunities + §3 priority placement + §4 attestation; impl week-4 | P-META-019 + element-reviews place |
| `element-review-staleness` | quarterly | warn | Platform element not reviewed in 90+ days flagged; quarterly cadence forces depth-3 element-reviews; impl week-4 | P-META-019 + element-reviews place |
| `naming-policy-compliance` | PR | warn | Every modified filename + frontmatter `name:` field passes 4-rule classifier (always-current / per-session / per-topic / layer-prefixed) + case-convention table + vocabulary rules; ratchet to ERROR after K=5 fires; impl week-4 | B_NAMING_POLICY + P-ARCH-029 |

**58 audit slugs registered atomically (S006 turn 24 — 1 NEW for naming-policy; was 57 at L2b close).** Each new audit cross-references its backing principle/contract; implementation deferred week-4 audit-runner ship per existing pattern; atomic registration is the load-bearing fact per FSE amendment (S005 turn 18).

> **§C3.1 audit-registry validation pass finding (S005 turn 8):** cross-check via `grep + diff` of pillar-leaf citations vs registry entries surfaced **32 dangling references** — audit slugs cited in pillar leaves with no canonical entry in this registry. Most are behavioral-discipline audits referenced in `ai-behavior-spine.md` / `behavioral-contracts.md` / `_intake/tag-status-contract.md`. Bulk-registration deferred as its own work item (carry to S006 §3); registering 32 entries cleanly per-category requires its own batch with PCR for category placements. See gaps-and-duplications-S005.md (created at session close) for full enumeration. The +66 extended-S003 consolidation closed dangling-ref debt for the audits IT ADDED, but did NOT consolidate the broader behavioral-discipline citations that pre-date S003 or were added in S002 turn 7 / 10 / 14 / 15 / 17 alongside spine-matrix engraving without registry entries.

> **Note: registry consolidation provenance.** The +66 audits added to this leaf in extended-S003 were already CITED in pillar leaves but had no canonical definition. The consolidation closes the dangling-reference debt that gaps-and-duplications-S003.md original-Gap-1 was a precursor to. Each new audit was sourced verbatim from the cited leaf's enforcement section (the leaf is the requirements doc; this leaf is the registry).

## Severity routing

Severities map to routing actions, not just labels:

| Severity | Routing |
|---|---|
| `info` | Log only; visible in dashboard but no alert |
| `warn` | Dashboard fact + scorecard impact; weekly digest |
| `error` | Opens Linear ticket (auto-created); blocks PR if PR-cadence |
| `critical` | Pages on Slack/email; blocks PR if PR-cadence; opens incident if real-time |

**Confidence threshold:** only checks scoring ≥0.9 confidence open tickets. *Why:* SAST false-positive rates run 68–78%. Uncalibrated audit systems destroy their own credibility — analysts grow desensitized. The confidence threshold is the anti-fatigue mechanism.

## Cadence model (the cron jobs)

| Cadence | When it runs | Mechanism |
|---|---|---|
| `PR` | On every pull request | GitHub Actions on `pull_request` trigger |
| `nightly` | 02:00 UTC daily | GitHub Actions on `schedule: '0 2 * * *'` |
| `weekly` | Mon 06:00 UTC | GitHub Actions on `schedule: '0 6 * * 1'` |
| `real-time` | On health endpoint hit | Checkly continuous monitoring + on-deploy webhook |
| `on-demand` | Admin "Re-run all" button | `apps/admin/api/audit/run/route.ts` |

## Live drift dashboard

`apps/admin/app/(admin)/audits/drift/page.tsx` shows continuous drift signals (not just cron-driven):

- Introspected Postgres schema ↔ ZModel-generated Prisma
- Payload collections ↔ ZModel
- Vale dict ↔ glossary codegen output
- ESLint forbidden-identifiers ↔ glossary codegen output
- Page files ↔ template catalog (which pages drifted from their declared template?)
- Catalog frontmatter ↔ file content (drift between declared metadata and reality)
- AGENTS.md ↔ principles.yaml codegen output

## Hotspot dashboard

`apps/admin/app/(admin)/audits/hotspots/page.tsx` — weekly recomputation of `churn × complexity` per file. Top decile listed with refactor priority. The architectural-refactor signal (per Adam Tornhill's research: ~1-2% of files account for ~70% of dev work).

## Main dashboard

`apps/admin/app/(admin)/audits/page.tsx` — Tremor `Grid` with:

- One big `ProgressCircle` (platform health 0–100, weighted average across categories)
- Category cards (Schema / Security / Sanity / Vocabulary / Quality / Catalog / Skills / Cost / Integrity / Meta) with current letter grade and 30-day sparkline
- "What changed since last run" panel — diff of new failures vs. fixed ones (the signal that makes the dashboard *checked*, not forgotten)
- Failing-check table sortable by severity / category / age
- "Re-run all" button (on-demand cadence trigger)

**Default category weights** (stored in DB; tune without redeploy):
- Security 0.20
- Schema 0.15
- Sanity 0.10
- Vocabulary 0.10
- Quality 0.10
- Catalog 0.10
- Skills 0.15
- Cost 0.05
- Integrity 0.05

## Severity → routing implementation

`libs/audits/runner/src/route-result.ts`:

```ts
async function routeResult(result: AuditResult): Promise<void> {
  // Confidence gate
  if (result.confidence < 0.9 && (result.status === "error" || result.status === "critical")) {
    return; // log only; below threshold
  }

  switch (result.status) {
    case "info":
      await logToFile(result);
      break;
    case "warn":
      await writeToDashboard(result);
      await maybeAddToWeeklyDigest(result);
      break;
    case "error":
      await writeToDashboard(result);
      await openLinearTicket(result, { priority: "Medium" });
      break;
    case "critical":
      await writeToDashboard(result);
      await openLinearTicket(result, { priority: "Urgent" });
      await pageOnSlack(result);
      if (result.check.cadence === "real-time") {
        await openIncident(result);
      }
      break;
  }
}
```

## Integration with the rule registry + principles.yaml

Per [rule-registry.md](./rule-registry.md), every rule has at least one enforcer; many enforcers are audit checks. The relationship:

- **Principles** (`packages/principles/principles.yaml`) — the "what" of the rules
- **Rules** (`docs/rules/RULE-NNNN.yaml`) — the PR-trackable derivative
- **Audit checks** (`libs/audits/checks/<slug>.ts`) — the executable enforcer for each rule

The audit runner verifies the bidirectional invariants:
1. Every principle has its required number of enforcers per severity (P-META-001)
2. Every `// @enforces:` annotation references a real principle (`enforcer-orphans` check)

When `principles.yaml` changes, `pnpm principles:codegen` may emit new audit check stubs for new enforcers; the audit runner picks them up automatically on the next cadence run.

## Anti-patterns to avoid

1. **Audit fatigue** — too many warnings → all ignored. Mitigation: confidence threshold ≥0.9 for tickets; weekly digest grouping.
2. **False positives** — bad checks erode trust. Mitigation: every check has a `mutedUntil` field and a `lastTunedAt` date; checks unchanged for 90 days are reviewed.
3. **Audit-of-audit drift** — the checker itself rots. Mitigation: meta-check `audit-of-audit` asserts every check ran in its expected interval.
4. **Compute cost** — recurring audits can dwarf the platform. Mitigation: `nx affected` for code audits, incremental Atlas for schema, cached facts (recompute only when source-of-truth hash changes).
5. **Dashboards checked once and forgotten** — Mitigation: weekly digest email with deltas; "what changed since last run" panel.
6. **Audits that can't be traced to a fix** — Mitigation: every failed check has `quickfix_url` (PR, migration, doc) or it's not a real check yet.

## Reuse-first applied to audit checks

Before adding a new audit check:

1. **Search `libs/audits/checks/`** for similar patterns
2. **Could the new check be a parameter on an existing check?** (e.g., new schema-drift target → enhance existing `schema-*-drift.ts` family)
3. **If genuinely new**, follow the slice contract (the audit slice itself meets it per P-ARCH-015) and register the check via principles.yaml or rule-registry

## Sources

- [Backstage Tech Insights](https://github.com/backstage/community-plugins/blob/main/workspaces/tech-insights/plugins/tech-insights/README.md) — the Facts/Checks/Scorecards model
- [Cortex Scorecards](https://docs.cortex.io/standardize/scorecards/scorecards-as-code) — commercial GitOps version
- [OpsLevel Rubrics](https://www.opslevel.com/resources/how-scorecards-work-in-opslevel-a-truly-flexible-model)
- [Spotify Soundcheck](https://backstage.spotify.com/docs/plugins/soundcheck/core-concepts/tech-insights) — the original
- [OpenSSF Scorecard Action](https://github.com/ossf/scorecard-action)
- [Squawk migration linter](https://squawkhq.com/)
- [Atlas schema drift](https://atlasgo.io/monitoring/drift-detection)
- [Snyk ToxicSkills](https://snyk.io/blog/toxicskills-malicious-ai-agent-skills-clawhub/) — informs the skill-prompt-injection-scan check
- Adam Tornhill — *Your Code as a Crime Scene* (hotspot analysis)
- [Contrast Security: AppSec noise & fatigue](https://www.contrastsecurity.com/infographics/appsec-noise-and-fatigue-by-the-numbers) — informs the confidence threshold
