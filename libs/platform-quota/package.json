{
  "name": "@csps/platform-quota",
  "version": "0.0.1",
  "private": true,
  "description": "Shared platform quota constants + enforcement types. SSoT for per-tenant limits across all 30 CSPS apps. Q6=A (shared lib, not per-app).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "echo 'no tests yet'"
  }
}
