# P-ARCH-018.yaml
# CSPS Principle Slice — generated by tools/split.mjs
# Source of truth: packages/principles/principles.yaml
# DO NOT edit this file directly — run `pnpm --filter @csps/principles split` to regenerate
# Phase 7 token-optimization §9.8 Candidate #1 (S010)
# Load this file (P-ARCH-018.yaml) instead of the full principles.yaml (~85K)
# when you only need this one principle in your AI session context.
id: P-ARCH-018
name: schema-per-app
category: architecture
severity: critical
statement: >-
  Shared kernel in public; each app's domain entities in app_<slug>. Use fully-qualified table names; never search_path
  with transaction pooling.
enforcers:
  - layer: ci-check
    location: tools/validators/validate-app-schema.mjs
    description: 'Slice contract check #13 — App slice → @@schema(''app_<slug>'')'
  - layer: eslint-rule
    location: tools/validators/eslint-rules/no-search-path-set.ts
    description: Forbids SET search_path in app code
  - layer: generator
    location: tools/migrate-multi-schema/migrate.ts
    description: Multi-schema migration tool templates schema names per app
  - layer: zenstack-policy
    location: libs/policies/base.zmodel
    description: >-
      Base ZModel datasource declaration scopes schemas array — schema-per-app boundary enforced at ZenStack codegen +
      Prisma multi-schema preview-feature; runtime queries use fully-qualified names per ADR-0002
enforcer_count: 4
cross_references:
  - P-ARCH-022
