# P-ARCH-021-ratchet-pattern.yaml
# CSPS Principle Slice — generated by tools/split.mjs
# Source of truth: packages/principles/principles.yaml
# DO NOT edit this file directly — run `pnpm --filter @csps/principles split` to regenerate
# Phase 7 token-optimization §9.8 Candidate #1 (S010)
# Load this file (P-ARCH-021-ratchet-pattern.yaml) instead of the full principles.yaml (~85K)
# when you only need this one principle in your AI session context.
id: P-ARCH-021
name: ratchet-pattern
category: architecture
severity: error
statement: >-
  Don't try to fix existing files. Block any change that makes a complexity/size metric worse on the diff. New code
  obeys; old code only gets cleaned when it's touched.
enforcers:
  - layer: ci-check
    location: .github/workflows/audit-pr.yml
    description: eslint --max-warnings 0 against changed files only
  - layer: pre-commit
    location: .husky/pre-commit (lint-staged)
    description: Same checks on staged files
  - layer: ci-check
    location: libs/audits/checks/file-size-ratchet.ts
    description: File size cannot regress on diff
  - layer: ci-check
    location: libs/audits/checks/cognitive-complexity.ts
    description: Function complexity cannot regress on diff
enforcer_count: 4
cross_references:
  - P-ARCH-022
enforcement_tier:
  tier1_hook: AGENTS.md enforcement + session-open.sh guidance (behavioral level)
  tier2_validator: pnpm verify full-pass (indirect coverage via contract validators)
  tier3_session: AGENTS.md hard-NO + session-open.sh principle injection
