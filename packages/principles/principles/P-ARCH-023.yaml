# P-ARCH-023.yaml
# CSPS Principle Slice — generated by tools/split.mjs
# Source of truth: packages/principles/principles.yaml
# DO NOT edit this file directly — run `pnpm --filter @csps/principles split` to regenerate
# Phase 7 token-optimization §9.8 Candidate #1 (S010)
# Load this file (P-ARCH-023.yaml) instead of the full principles.yaml (~85K)
# when you only need this one principle in your AI session context.
id: P-ARCH-023
name: variants-cascade-defaults-explicit-overrides-win
category: architecture
severity: warn
statement: >-
  Common context flows from glob-matched variants; per-file frontmatter declares only what's specific. The
  anti-tagging-tax that makes small files viable.
enforcers:
  - layer: ci-check
    location: tools/validators/validate-frontmatter.mjs
    description: Applies variants resolution before validation; reports effective frontmatter
  - layer: instruction-file
    location: tools/catalog/variants.ts
    description: Single source of truth for cascading defaults
enforcer_count: 2
cross_references:
  - P-ARCH-024
