---
id: csps.data.gap-recurrence-register
name: gap-recurrence-register
description: "Persistent register of governance gaps that have been observed multiple times. When k_count >= 2, structural fix is required. When k_count >= 3, P-META-019 structural prevention mandate fires. Never delete entries — mark status: resolved with evidence."
version: "1.0"
owner: group:finky
lifecycle: production
lifecycle_state: active
core_spine: VALD
cie_connection: "requires_promotion"
pe_connection: "input"
context_question: "How many times has this exact gap been observed without structural resolution? Is k_count >= 2 triggering a mandatory structural fix now?"
context_quote: "Listing what is missing is like a mirror of the gaps. If you go back, you see you keep missing and fixing the same things."
---

# Gap Recurrence Register

> Append-only. Every gap observed in any Opus "what's missing" analysis gets an entry.
> k_count tracks how many sessions have independently re-discovered the same gap.
> k_count >= 2: STRUCTURAL FIX REQUIRED (not another description).
> k_count >= 3: P-META-019 fires. Block session close without a committed fix.
>
> Status progression: open → structural_fix_proposed → fix_committed → behavioral_test_passing → resolved
> A gap is RESOLVED only when a behavioral test case exists that PROVES the fix catches violations.
> A gap with k_count >= 2 that is only "documented" is NOT improving — it is accumulating.

entries:

  - id: gap_T1_AI_CONCEPTION_VAULT
    observation: "T1 hook missing for AI Conception Vault entries (all 12 are T3-only)"
    first_seen: S040
    k_count: 5
    sessions: [S040, S046, S051, S052, S053, S053-turn-11]
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S042
    behavioral_test_exists: true
    behavioral_test_file: tools/tests/behavioral/ai-conception-enforcement-test.sh
    structural_fix_file: ".claude/hooks/pre-tool-use-zf-termination-gate.sh"
    note: "K=5 CLOSED S059 PROTO-G. T1 hooks installed for 3 highest-impact vault entries: pre-tool-use-zf-termination-gate.sh (BLOCKING on council writes), pre-tool-use-state-claim-gate.sh (ADVISORY), pre-tool-use-humble-step-gate.sh (ADVISORY). Vault entries updated with enforcement_tier T1+T2+T3. Behavioral tests: 5/5+4/4+3/3 passing. gap_T1_AI_CONCEPTION_VAULT = fix_committed."

  - id: gap_T2_ORPHAN_CONTRACTS
    observation: "9 constitutional B_* contracts have no T2 validator (B_PRACE, B_NO_AI_IMPERSONATION, B_APPS_ARE_TRIALS, B_DONE_RIGHT_FROM_THE_START, B_DEFINITION_BEFORE_ENFORCEMENT, B_AUTONOMY_4_CONDITIONS, B_CHECKPOINT_8_CATEGORIES, B_CONTEXTUAL_LOCALITY, B_DEVELOPMENT_VS_PRODUCTION)"
    first_seen: S052
    k_count: 2
    sessions: [S052, S053]
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S052
    behavioral_test_exists: true
    behavioral_test_files:
      - tools/tests/behavioral/apps-are-trials-test.sh
      - tools/tests/behavioral/contextual-locality-test.sh
      - tools/tests/behavioral/done-right-from-start-test.sh
    structural_fix_files:
      - tools/validators/validate-contextual-locality.mjs (S055)
      - tools/validators/validate-done-right.mjs (S055)
      - tools/validators/validate-prace-tiers.mjs (S061) — B_PRACE T2
      - tools/validators/validate-ai-honesty.mjs (S061) — B_NO_AI_IMPERSONATION T2
      - tools/validators/validate-dev-vs-prod.mjs (S061) — B_DEVELOPMENT_VS_PRODUCTION T2
      - tools/validators/validate-definition-before-enforcement.mjs (S061) — B_DEFINITION_BEFORE_ENFORCEMENT T2
      - tools/validators/validate-autonomy-conditions.mjs (S061) — B_AUTONOMY_4_CONDITIONS T2
      - tools/validators/validate-checkpoint-categories.mjs (S061) — B_CHECKPOINT_8_CATEGORIES T2
    note: "K=2. CLOSED S061. All 9 orphan contracts now have T2 validators wired in verify.mjs. Behavioral tests for prace/honesty/dev-vs-prod pending (S062 backlog). validate-prace-tiers finds 37 T3-only contracts — advisory backlog surfaced. validate-definition-before-enforcement finds 62 principles missing governing_intent — S062 backlog."

  - id: gap_ZF_NOMINAL_CYCLES
    observation: "AI writes nominal ZF cycles ('no new findings', 'areas', 'topics') instead of naming specific files and line numbers"
    first_seen: S037
    k_count: 6
    sessions: [S037, S040, S046, S050, S052, S053]
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S039
    behavioral_test_exists: true
    behavioral_test_file: "tools/tests/behavioral/zf-cycle-format-test.sh"
    behavioral_test_result: "INPUT A (nominal) → exit=1 ✓ | INPUT B (file-citing) → exit=0 ✓"
    current_fix: "T2: validate-zf-cycle-format.mjs LIVE S053-B (commit 13a3cef). T3: ZF template in session-open-context.mjs. Behavioral test passing."
    gap_found_during_tool_run: "validate-zf-cycle-format finds 0 blocks in sonnet-turn.md — ZF evidence written in chat not in council file. Sonnet reports should embed ZF blocks."
    note: "K=6. Fix committed. Behavioral test proves validator catches violations. NEW GAP: ZF written to chat, not sonnet-turn.md — T2 cannot scan it. Resolution: Sonnet report template must include ZF block in council file write."

  - id: gap_SETTINGS_LOCAL_BYPASS
    observation: "New tabs ask for write permissions despite bypassPermissions being set"
    first_seen: S040
    k_count: 4
    sessions: [S040, S046, S053, S053-T35]
    status: resolved
    behavioral_test_exists: false
    root_cause: "settings.local.json `permissions` object SHADOWED settings.json `permissions` object, losing additionalDirectories (csps-playground). Writes to playground prompted because directory not registered."
    permanent_fix: "settings.local.json now empty `{}`. ALL permissions live in git-tracked settings.json. No shadowing. Single source of truth."
    rule: "If settings.local.json must exist, it MUST include ALL fields from settings.json permissions block, or stay empty `{}`."
    note: "K=4. Previous 3 fixes added defaultMode to settings.local.json — STILL shadowed additionalDirectories. Correct fix removes local override entirely."

  - id: gap_HANDOFF_STARTUP_BLOCK
    observation: "HANDOFF files missing paste-ready startup block for new Sonnet tab"
    first_seen: S051
    k_count: 3
    sessions: [S051, S052, S053]
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    behavioral_test_exists: false
    current_fix: "sonnet-startup.template.md created (S053-A). validate-handoff-completeness.mjs updated to check."
    note: "Fix committed. Behavioral test: does a new Sonnet tab following the startup block actually produce correct INTENT ABSORBED format? Not yet tested."

  - id: gap_SESSION_INJECTION_COMPRESSION
    observation: "Session-open injection may be compressed in long sessions — critical templates not present in AI context"
    first_seen: S051
    k_count: 2
    sessions: [S051, S053]
    status: open
    prevention_class: unclassified
    structural_fix_plan_session: null
    prevention_design_pending: true
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "Deferred S067-S070 — behavioral test at turn 10+20 context survival is complex mechanically. T3 session-open injection is mitigation. No structural fix until K=3."
    fix_commit_sha: null
    structural_fix_triggered: true
    behavioral_test_exists: false
    note: "K=2. No measurement exists for whether injection content survives context compression. Structural fix: behavioral test that verifies injection content is present in fresh tab first response."

  - id: gap_STARTUP_BLOCK_NOT_SURFACED
    observation: "Session close response does not paste the SONNET STARTUP BLOCK inline — Governor has to navigate to HANDOFF to find it"
    first_seen: S054
    k_count: 2
    sessions: [S054]
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    behavioral_test_exists: false
    current_fix: "post-stop-session-close-gate.sh now extracts and surfaces startup block automatically. AGENTS.md hard NO added. sonnet-report.template.md v2.1 has mandatory PASTE section."
    note: "K=1. Root cause: B_ZERO_NAVIGATION_FOR_GOVERNOR does not explicitly cover session-close startup block surfacing. Fix: 5-surface FSE (hook + template + AGENTS + gap-register + this note). Behavioral test pending."

  - id: gap_CONCEPTUAL_CLOSURE_NO_TEST
    observation: "Solutions are designed and described (cognitive match) without being tested against known violations. The model of the solution is treated as the solution."
    first_seen: S053
    k_count: 2
    sessions: [S053]
    status: open
    prevention_class: unclassified
    structural_fix_plan_session: null
    prevention_design_pending: true
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: false
    behavioral_test_exists: false
    note: "K=1. This is the root cause behind gaps 1-6. Structural fix: behavioral test suite (tools/tests/behavioral/) + Gap Recurrence Register validator. This register IS the first step of that fix."

  - id: gap_DONE_CLAIM_BEFORE_VALIDATOR_CONFIRMS
    observation: "Sonnet declares STEP/SESSION COMPLETE/SEALED/DONE in commit message with exit_code=0 claim, but the close validator (validate-handoff-completeness / validate-permanence-coverage / validate-zf-cycle-format / validate-frontmatter) only confirms AFTER the close-commit lands. Each instance required Opus course-correct."
    first_seen: S062
    k_count: 4
    sessions: [S062, S062-C2, S062-C3, S062-C4]
    instances:
      - turn: STEP-1-RE-COMPLETE
        commit: 5db4e1b
        course_correct: validate-zf-cycle-format blocked — Cycle 2 used .txt extension not in regex
      - turn: tab-transfer-stability-analysis-commit
        commit: 8453591
        course_correct: file missing version/lifecycle/lifecycle_state frontmatter fields
      - turn: STEP-5b-APPLY-COMPLETE
        commit: 40e4dc5
        course_correct: triage classification borderline cases needed Opus glance before apply
      - turn: SESSION-CLOSE-CLAIMED
        commit: 91054a1
        course_correct: HANDOFF-S062-to-S063 missing Zone A title + ALIGNMENT QUESTIONS + SONNET STARTUP BLOCK
    status: fix_committed
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S062
    structural_fix_committed: 0fb5173
    behavioral_test_file: tools/tests/behavioral/done-claim-validator-gate-test.sh
    behavioral_test_exists: true
    proposed_fix: |
      Pre-commit hook .claude/hooks/pre-commit-claim-validator-gate.sh:
        IF commit message contains /\b(COMPLETE|SEALED|DONE|CLOSED|RATIFIED)\b/i AND
           latest THIS-HEAD run of the relevant close-validator (one of:
           validate-handoff-completeness, validate-permanence-coverage,
           validate-zf-cycle-format, validate-frontmatter) is exit=0
        THEN allow commit.
        ELSE BLOCK with: "Cannot claim <KEYWORD> without THIS-HEAD validator
                          passing. Run pnpm verify first; if PASS commit; if FAIL fix."
      Behavioral test: tools/tests/behavioral/done-claim-validator-gate-test.sh
        INPUT A (nominal — claim message without validator pass) → exit=1 ✓
        INPUT B (claim message with passing validator) → exit=0 ✓
    pipeline_owner: |
      Sonnet builds + Opus writes core seed (regex + validator matrix) + Governor ratifies activation
    proto_filed: PROTO-S063-DONE-CLAIM-VALIDATOR-GATE
    proto_filed_at: "2026-05-26"
    proto_pe_score: 95
    when: |
      S063 session-open will surface this K=4 entry as MUST FIX BEFORE new work
      (per session-open.sh T3 injection of K>=2 entries). First Sonnet directive in
      S063 must address before any new PROTO work begins.
      PROTO-S063-DONE-CLAIM-VALIDATOR-GATE filed S062-C5 — BATCH-L pe_score=95.
    forgetting_prevention:
      - validate-gap-recurrence.mjs surfaces this entry in pnpm verify per session (T2)
      - session-open.sh T3 injection lists K>=2 entries with MUST-FIX label
      - K=5 will fire P-META-019 BLOCKING — session close cannot happen without committed fix
      - improvement-register.yaml cross-reference (manual but supplementary)
      - this register is parsed at every session-start per always_include
      - PROTO-S063-DONE-CLAIM-VALIDATOR-GATE in unified-plan.yaml BATCH-L (pe_score=95 = must-fix-first)
    note: "K=4 in S062 alone (single session). Pattern is structural: both Sonnet AND Opus underweight 'validator must run before claim' because the commit message FEELS like proof. Only mechanical pre-commit gate breaks the loop. P-META-019 mandates structural fix at K=2; this is K=4 — overdue. The fix is small (~30 lines bash) but its absence costs ~3 turns per session to detect-and-course-correct."

  - id: gap_VALIDATOR_BEHAVIORAL_TEST_COVERAGE
    observation: "134 of 167 validators (80%) have no behavioral test proving they catch known violations. Only BLOCKING validators were tested; ADVISORY validators uniformly untested. Validators 'exist' but their catching-power is unverified."
    first_seen: S065
    k_count: 2
    sessions: [S065]
    status: open
    prevention_class: unclassified
    structural_fix_plan_session: null
    prevention_design_pending: true
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S065
    behavioral_test_exists: false
    source: "PAP Part 1A audit — commit 9e51f43, docs/plan/_handoff/VAULT/pap/part-1A-validators-audit.yaml"
    proposed_fix: |
      (a) Build behavioral tests for all 134 systematically (~3-5 sessions) OR
      (b) Triage: BLOCKING validators MUST have tests; ADVISORY-only marked
          behavioral_test_status: advisory-exempt with explicit reason.
      Recommended: Mixed — build tests for top-30 highest-impact ADVISORY validators;
      mark remaining advisory validators as exempt with documented reason.
    pipeline_owner: "Sonnet builds + Opus seeds test pattern per validator class + Governor ratifies activation"
    forgetting_prevention:
      - K-pipeline surfaces in pnpm verify per session
      - session-open T3 lists K>=2 as MUST-FIX
      - PAP Part 1A YAML documents the 134 specific validators
    note: "Largest single structural gap surfaced by PAP. Same severity-class as gap_DONE_CLAIM K=4. Platform had perceived quality (179 validators) but mechanical quality is 3.6% of perceived. Same pattern as PERMANENCE-DRIFT 100%→58%. Honest framing: advisory validators are aspirational, not mechanical."

  - id: gap_PREVENTION_COVERAGE_GAP
    observation: "Prevention coverage (moat elements with active T1+T2+output_signature / total moat elements) at 5% per PAP Part 5 (S065). Platform has 41 moat elements but only ~2 have fully wired T1+T2+output evidence. Third measurement-honesty correction: PAP keeps surfacing real mechanical gaps."
    first_seen: S065
    k_count: 1
    sessions: [S065]
    status: open
    prevention_class: unclassified
    structural_fix_plan_session: null
    prevention_design_pending: true
    must_address_by_session: "S076"
    age_escalation_status: on-time
    explicit_defer_reason: "S074 HARDWIRE focus; carried to S076"
    fix_commit_sha: null
    structural_fix_triggered: true
    structural_fix_required_since: S065
    behavioral_test_exists: false
    source: "PAP Part 5 audit — commit 2e3a9a8, docs/plan/_handoff/VAULT/pap/part-5-prevention-coverage-audit.yaml"
    proposed_fix: |
      Two-track: (a) systematic moat-by-moat wiring — assign each moat its T1+T2+output triple;
      ship hooks/validators for orphan moats (~3-5 sessions). (b) honest moat re-classification —
      aspirational moats marked status: aspirational (CSEP-pending); enforced moats with mechanical
      evidence marked status: enforced. Track and ratchet enforced count.
    pipeline_owner: "Sonnet builds wiring + Opus seeds T1+T2+output triple per moat + Governor ratifies"
    forgetting_prevention:
      - K-pipeline surfaces in pnpm verify per session
      - flow-activity-monitor expands to include moat enforcement_status per element
      - PAP runs per-session until coverage > 30%
    note: "Third measurement-honesty correction in S062-S065 arc (PERMANENCE_DRIFT=first, VALIDATOR_BEHAVIORAL_TEST=second, PREVENTION_COVERAGE=third). 5% is honest. K-pipeline owns the ratchet forward."

  - id: gap_DIM2_CORE_ID_UUID_UPGRADE
    observation: "Core id columns (User/Tenant/UserTenant/AuditEvent/Task) + their FK scalars are stored as TEXT holding valid UUID values; native-UUID column type (@db.Uuid) gives better storage/index efficiency at 30-app multi-tenant scale. Deferred by Governor decision, not an oversight."
    first_seen: S076
    k_count: 1
    sessions: [S076]
    status: resolved
    prevention_class: scale-readiness
    scheduled_by: "Governor S076 (2026-06-02) — explicit instruction: schedule + enforce, 2 weeks out"
    must_address_by_date: "2026-06-16"
    must_address_by_session: "S090"
    age_escalation_status: resolved-before-deadline
    fix_commit_sha: "b70caa02"
    structural_fix_triggered: true
    structural_fix_committed: true
    target_dimension: "dim-4 multi-tenant scale-readiness"
    done_definition: |
      A tested, data-preserving migration converts every core id column + each referencing FK scalar
      from text -> native uuid (valid because values already conform: USING id::uuid). Sequence in one
      transaction: drop dependent FK constraints -> ALTER COLUMN TYPE uuid USING ::uuid -> re-add FKs.
      schema.zmodel restores @db.Uuid on Base/AppendOnlyBase id + FK fields. Governor applies to live DB;
      verify=0; block-test proves all FK relationships intact and zero rows lost.
    behavioral_test_exists: true
    behavioral_test_file: "libs/policies/migrations/apply-uuid-migration.ts"
    behavioral_test_result: |
      S077 run 2026-06-02: OVERALL PASS — COMMITTED to database.
      CHECK 1 DATA INTACT: User:1→1 Tenant:1→1 Task:2→2 Project:0→0 ✅
      CHECK 2 UUID TYPES: 0 non-uuid id columns (all 14 tables) ✅
      CHECK 3 FK CONSTRAINTS: 20/20 present ✅
      RLS POLICIES: 12 policies recreated with uuid-compatible expressions ✅
    pe_urgency_input: false
    cie_observation_class: resolved
    resolution_session: "S077"
    resolution_date: "2026-06-02"
    source: "FINDING-S076-DIM2-03; Governor first live PART-3 run (S076); memory project-dim2-part3-id-type; PROTO-S076-DIM2-FIX (Option A keep-text)."
    note: "RESOLVED S077 (14 days ahead of 2026-06-16 deadline). Migration applied via apply-uuid-migration.ts on DIRECT_URL. 5 debugging iterations to handle: RLS policy blocking ALTER (solved: drop-alter-recreate), pg array type (solved: string_agg), policy expression regex (solved: ::uuid on JWT side + pg_get_expr ::text normalization + savepoints). dim-4 Surface 5 SEALED."

  - id: gap_INSTRUCTION_INTEGRITY
    observation: >
      Platform instructions (hooks / validators / principles / AGENTS.md rules) are applied as
      surface text without provenance or intent wrappers. Two linked risks: (1) ambient content
      (file pastes, tool output, external data) can carry instruction-like text that hijacks
      governing intent — no review-before-act exists for non-authoritative source detection;
      (2) rules applied as bare text misfire across contexts (rigid-rule-anti-pattern, D11).
      Both faces share ONE root: instructions lack source taxonomy + governing_intent + scope +
      escape-hatch wrappers. A properly wrapped instruction is simultaneously anti-rigid AND
      anti-rogue. Existing partial mitigations: P-META-025 C&I (governing_intent per rule) ·
      validate-context-wrapped-numbers.mjs (numbers wrapped) · B_CONTEXT_SENSITIVE_GOVERNANCE ·
      rigid-definition-drift feedback · HARDWIRE-007 (governing_intent field). Gap: no unified
      source taxonomy, no review-before-act gate for non-authoritative ambient instructions.
    first_seen: S076
    k_count: 1
    sessions: [S076]
    status: open
    prevention_class: instruction-integrity
    structural_fix_plan_session: null
    prevention_design_pending: true
    must_address_by_session: "S082"
    pe_urgency_input: true
    cie_observation_class: design_gap
    fix_commit_sha: null
    explicit_defer_reason: null
    behavioral_test_exists: false
    source: "Governor S076 — 'resolved = amazing; unresolved = suffer like other AI systems.' PROTO-S076-INSTRUCTION-INTEGRITY (Opus-17 directive). Both faces: anti-rogue (ambient injection) + anti-rigidity (bare-text misfiring) share ONE root."
    note: "S076 CQS-ABSORBED: gap_INSTRUCTION_INTEGRITY is now a FACET of the CQS Alignment Layer (PROTO-S076-CQS-ALIGNMENT-LAYER OPIA ✅ RATIFIED). Face A (anti-rogue) = CQS incoming-instruction set. Face B (anti-rigidity) = CQS rule set. Canonical home: SANDBOX-cqs-alignment-layer-S076.md + tools/config/cqs-sets.yaml. No standalone PROTO-S076-INSTRUCTION-INTEGRITY needed — absorbed."

  - id: gap_NO_LAPTOP_HARDWIRE_GAP
    observation: >
      Zero-Laptop policy (P-ZERO-LAPTOP, feedback_zero_laptop_dependency.md) is HALF-hardwired.
      validate-push-status.mjs catches unpushed COMMITS — but two blindspots remain:
      (1) Untracked/uncommitted files under docs/, tools/, .csps/, libs/ — governance artifacts
          created mid-session but never staged can be stranded on the laptop silently.
          Demonstrated S077: OPUS-TAB-TRANSFER-S077.md appeared as untracked, never committed.
      (2) ~/.claude/ memory store (MEMORY.md + individual memory files) — laptop-only,
          outside git, not synced. If laptop is lost, 5+ sessions of accumulated memory is gone.
          The irony: the memory file ABOUT zero-laptop dependency lives only on the laptop.
    first_seen: S077
    k_count: 1
    sessions: [S077]
    status: open
    prevention_class: zero-laptop-dependency
    structural_fix_plan_session: "S078+"
    structural_fix_triggered: false
    must_address_by_session: "S080"
    age_escalation_status: on-time
    fix_commit_sha: null
    behavioral_test_exists: false
    target_dimension: "governance-infrastructure"
    done_definition: |
      (a) Session-close gate flags any untracked governance files (docs/, tools/, .csps/, libs/)
          with clear warning: "X files untracked — stage before closing or stranded on laptop."
          Implemented as post-stop hook or pre-close validator. Block-test: create untracked
          governance file → validator catches it.
      (b) Memory store backed up: either (i) ~/.claude/projects/*/memory/ synced to a
          monitored git repo or cloud path, OR (ii) session-close writes a memory-manifest
          to the CSPS repo root so at minimum the INDEX is in git even if files are local.
    pe_urgency_input: true
    cie_observation_class: governance_gap
    source: >
      Opus-17 S077 directive: 'register in gap-recurrence-register (git-tracked, NOT just memory)'.
      Demonstrated live: OPUS-TAB-TRANSFER-S077.md untracked + ~/.claude memory store laptop-only.
      Reference: feedback_zero_laptop_dependency + AP-001 (exists≠active) + P-ARCH-024 (zero-laptop).
    note: >
      Structural fix DEFERRED per foundation-focus completion directive (S077). Both fixes
      are post-dim-4: (a) session-close uncommitted-file gate, (b) memory-store git backup.
      The gap is now in git (this entry) — survives laptop loss. memory entry also written
      (feedback_no_laptop_hardwire_gap.md) as cross-reference.

  - id: gap_DIM4_LIVE_LOAD_PROOF
    observation: >
      dim-4 Surface 4 (k6 load harness) was built and validated structurally (validate-load-test-harness.mjs
      blocking=0). The representative k6 run (scenario-a) was attempted against httpbin.org — which proves
      the script executes but pool_errors=0 is trivially true (no real Supabase connection pool was tested).
      The empirical live-load proof — scenario-a GREEN against a real CSPS app endpoint — is deferred because
      apps were gated on foundation, and foundation had no deployed app at S077 close.
      Honest resolution: the chicken-and-egg. You cannot load-test a platform with no app on it.
    first_seen: S077
    k_count: 2
    sessions: [S077]
    status: open
    prevention_class: scale-readiness
    structural_fix_plan_session: "S078"
    structural_fix_triggered: false
    must_address_by_session: "S079"
    age_escalation_status: on-time
    fix_commit_sha: null
    explicit_defer_reason: "Phase B deferred (S082). Structurally impossible before app#1 deployed — cannot load-test a platform with no app. Tracked as iob_DIM4_LIVE_LOAD_PROOF in tools/data/impact-obligation-register.yaml. Gate: app#1 Vercel test-drive = Phase B second step. PARK:obligation."
    behavioral_test_exists: false
    target_dimension: "dim-4 multi-tenant scale-readiness"
    done_definition: |
      scenario-a (tools/load-tests/k6/scenario-a-concurrent-burst.js) runs against a real CSPS
      app (app#1 Vercel URL — not httpbin.org or any stub). Output shows:
        Pool errors (42P05/P0002): 0 (expect 0) ✅
        HTTP failure rate: <1% (expect <1%) ✅
        p99 latency: <2000ms ✅
        OVERALL: ✅ PASS
      Governor pastes output to Opus → Opus issues OPIA → dim-4 empirically SEALED.
      GATE: must happen before app#2 build. In practice: app#1 test-drive IS the load proof run.
    pe_urgency_input: true
    cie_observation_class: deferred_gate
    source: >
      Opus-17 S077: 'pool_errors=0 against httpbin is trivially true — no real pool was tested.
      Register gap_DIM4_LIVE_LOAD_PROOF.' Rejected nominal seal on httpbin evidence. Correct call.
      Ref: dim-4 SANDBOX spec §DESIGN SURFACE 4, PASS BAR.
    note: >
      dim-4 declared MECHANISM-COMPLETE (not empirically sealed) at S077 close. All 5 surfaces'
      machinery built + validated. Live proof gated to app#1: scenario-a must run GREEN against the
      actual Vercel deployment before app#2 is authorized. This RESOLVES the chicken-and-egg honestly:
      foundation is complete enough to build app#1; app#1 test-drive provides the empirical seal.

  - id: gap_IZFC_COMPREHENSIVE_RENAME
    observation: >
      The platform has latent ZF/RZF/IZFC terminology ambiguity. RZF was ratified as RETIRED (S078),
      IZFC adopted as the governing concept, ZF kept as terminal-state shorthand. BUT the platform
      still has ZF/RZF references in: docs/plan principles, behavioral-contracts, AGENTS.md, memories,
      and hook FILENAMES (validate-nominal-rzf-detector.mjs etc. — names kept per Opus directive).
      Under context-pressure compression, the parallel ZF/RZF/IZFC terms can be mis-resolved and
      corrupt a major process — latent ambiguity. Eliminate, do not paper over.
    first_seen: S078
    k_count: 1
    sessions: [S078]
    status: open
    prevention_class: governance-terminology
    structural_fix_plan_session: "S079"
    structural_fix_triggered: false
    must_address_by_date: "2026-07-01"
    must_address_by_session: "S090"
    age_escalation_status: on-time
    fix_commit_sha: null
    behavioral_test_exists: false
    target_dimension: "governance-terminology"
    escalation_policy: >
      overdue+14d (2026-07-15) → auto-K2, blocks new work.
      K3 (k_count>=3 open) → session BLOCKS until structural fix exists.
      Governor-directed: insist until DONE COMPLETELY.
    done_definition: |
      Every ZF/RZF reference in docs/plan, principles, behavioral-contracts, AGENTS.md,
      and memory files → updated to "IZFC discipline" or explicit alias.
      Hook FILENAMES keep their names but each gets an IZFC header comment.
      Completeness proof: grep audit returns 0 un-aliased stale references (scripted, cited).
      verify=0 + committed.
    pe_urgency_input: true
    cie_observation_class: governance_debt
    source: >
      Opus-18 S078 PROTO-S078-IZFC addendum: 'register gap_IZFC_COMPREHENSIVE_RENAME with 2026-07-01
      deadline; escalation: overdue+14d → auto-K2 → K3 session BLOCKS; insist until DONE COMPLETELY.'
    note: >
      Hook filenames intentionally kept: validate-nominal-rzf-detector.mjs, validate-zf-cycle-format.mjs
      etc. — Opus directive S078. Only semantic references need updating; filenames are operational 
      shorthand. The injection (enforcement #6) already updated to IZFC. Memory + register = this gap.

  - id: gap_D_DEFAULT_SCHEME_CONSOLIDATION
    observation: >
      Two conflicting D-default numbering schemes exist in the platform:
      (1) VAULT ground truth (docs/plan/_handoff/VAULT/inner-ai-defaults/D*.md files):
          D1=humble-consolidation, D2=authority-pleasing, D3=surface-completeness,
          D4=pattern-match, D5=single-pass, D6=verbal-cleverness, D7=action-bias,
          D8=naming-novelty, D9=recency-bias, D10=cooperative-disagreement-aversion,
          D11A=debugging-wrong-layer, D11B=verbal-deferral-feels-like-action,
          D12=assumed-coverage, D13=doc-feels-like-mechanism.
      (2) Memory file (feedback_csps_alignment_over_inner_defaults.md, from S006):
          D1=eager-helpfulness, D5=single-source-navigation, D10=completion-pressure,
          D13=anti-conflict — conflicting with the vault on 4+ entries.
      ALSO: D11 collision — two distinct defaults share the same D11 slot.
      RESULT: Any reference to "D1 (eager helpfulness)", "D10 (completion pressure)",
      or "D13 (anti-conflict)" in platform docs is false. The vault is the ground truth.
    first_seen: S078
    k_count: 1
    sessions: [S078]
    status: open
    prevention_class: governance-terminology
    structural_fix_plan_session: "S079"
    structural_fix_triggered: false
    must_address_by_date: null
    must_address_by_session: "S085"
    age_escalation_status: on-time
    fix_commit_sha: null
    behavioral_test_exists: false
    target_dimension: "ai-default-registry"
    done_definition: |
      One canonical D-registry with no collisions:
        - D11 collision resolved (merge or renumber to D11 + D14, or suffix A/B formally)
        - Memory file feedback_csps_alignment_over_inner_defaults.md updated to match vault
        - All platform docs (reports, principles, behavioral-contracts) reference only vault names
        - grep audit: 0 stale D1=eager-helpfulness / D10=completion-pressure / D13=anti-conflict refs
        - verify=0 + committed
    pe_urgency_input: false
    cie_observation_class: governance_debt
    source: >
      Opus-18 S078: OPIA caught false D-default descriptions in docs/platform-intelligence/
      reports. AI Profiling report §2 table was entirely wrong, sourced from S006 memory file
      instead of VAULT ground truth. Fixed in S078 report edits; underlying scheme conflict remains.
    note: >
      S078: Fixed the 4 platform-intelligence reports to use vault D-names correctly.
      The underlying scheme consolidation (one canonical registry, D11 collision fix,
      memory file update) is deferred per no-date-pressure constraint from Opus-18.
      This entry tracks the deferred obligation.

  - id: gap_RESIDUE_HOOK_FALSE_POSITIVE
    observation: >
      post-stop-decision-residue-capture.sh (P-META-033 advisory hook, S078) uses loose pattern
      matching for PCR/option-set detection. Many turns use PCR language ("should we", "options:") 
      without needing residue registration (e.g., within a larger DONE response, clarifying questions).
      The hook flags these as advisory but the signal-to-noise ratio is unknown. Pattern refinement
      needed when the hook has collected 1-2 weeks of real data (post-journeys phase).
    first_seen: S078
    k_count: 1
    sessions: [S078]
    status: open
    prevention_class: false-positive-advisory
    structural_fix_plan_session: "refine-when-touched"
    structural_fix_triggered: false
    must_address_by_session: "S083"
    age_escalation_status: on-time
    fix_commit_sha: null
    behavioral_test_exists: false
    target_dimension: "governance-tooling"
    done_definition: |
      After 2+ weeks of hook data: analyze ai-behavior-signals.jsonl for p_meta_033 signal rate.
      If signal_class: p_meta_033_uncaptured_residue fires on >30% of turns that contain PCR language
      but no actual decision → refine the pattern to require 'chose' + 'alternative' context.
      Behavioral test: turn with PCR + explicit register entry → no hook flag; turn with PCR + no
      register entry → hook flag.
    pe_urgency_input: false
    cie_observation_class: tooling_gap
    source: >
      S078 building post-stop-decision-residue-capture.sh. Pattern matching noted as potentially
      loose during IZFC sweep. Registered per P-META-033 (no-lost-threads).
    note: "Advisory only (exit 0). Low urgency. Refine pattern after observing real data."

  - id: gap_CYCLE_COUNTER_DISCREPANCY
    observation: >
      verify.mjs skip_reason for pnpm-verify-cycles reads "at hard_limit 200 — deferred" but the HANDOFF-S078
      parked-chain item #1 (A2-cycles-audit) described cycles as "199/200 approaching." Discrepancy: either
      the counter is 199 and hard_limit triggers at 199 (off-by-one in the description), or it is at 200 and
      the handoff was stale. Must reconcile in A2: read the actual cycle-count logic in verify.mjs, audit which
      validators are STANDARD vs EXTENDED, confirm the exact number before any new standard validator is added.
    first_seen: S079
    k_count: 1
    sessions: [S079]
    status: open
    prevention_class: state-discrepancy
    structural_fix_plan_session: "S080-A2"
    structural_fix_triggered: false
    must_address_by_session: "S080"
    age_escalation_status: on-time
    fix_commit_sha: null
    behavioral_test_exists: false
    pe_band: 2
    note: "A2-scope. Reconcile before any new standard validator. back-ref: S079 close (PROTO-S079-CLOSE)."
